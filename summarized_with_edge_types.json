{"sub_140015A20": "Function sub_140015A20:\n--- Block sub_140015A20 @ 0x140015a20 ---\nMOV             W0, #0\nRET"}
{"sub_140012D08": "Function sub_140012D08:\n--- Block sub_140012D08 @ 0x140012d08 ---\nMOV             X0, X1\nADRL            X1, unk_1400192AA\nMOV             W2, #0x18\nB               sub_140011FA4"}
{"sub_140004020": "Function sub_140004020:\n--- Block sub_140004020 @ 0x140004020 ---\nLDR             X1, [X0,#0x18]\nCMN             X1, #1\nB.EQ            locret_14000403C\n\n--- Block sub_140004020 @ 0x14000402c ---\nCBZ             X1, locret_14000403C\n\n--- Block sub_140004020 @ 0x14000403c ---\nRET"}
{"memcmp": "Function memcmp:\n--- Block memcmp @ 0x140015320 ---\nADRP            X16, #__imp_memcmp@PAGE\nLDR             X16, [X16,#__imp_memcmp@PAGEOFF]\nBR              X16"}
{"sub_140014EB0": "Function sub_140014EB0:\n--- Block sub_140014EB0 @ 0x140014eb0 ---\nLDRB            W8, [X0]\nADRL            X9, unk_140019E40\nLDP             X0, X10, [X1,#8]\nLDR             X2, [X9,X8,LSL#3]\nADRL            X9, unk_140019F90\nLDRSW           X8, [X9,X8,LSL#2]\nLDR             X3, [X10,#0x18]\nADD             X1, X9, X8\nBR              X3"}
{"sub_14000104C": "Function sub_14000104C:\n--- Block sub_14000104C @ 0x14000104c ---\nB               sub_140002C1C"}
{"sub_1400040C4": "Function sub_1400040C4:\n--- Block sub_1400040C4 @ 0x1400040c4 ---\nSTP             X29, X30, [SP,#-0x10+var_s0]!\nMOV             X29, SP\nLDR             X10, [X0]\nLDP             X9, X8, [X10]\nSTR             XZR, [X10]\nCBZ             X9, loc_140004110\n\n--- Block sub_1400040C4 @ 0x140004110 ---\nADRL            X0, off_140017D98; \"<redacted>\"\nBL              sub_140016E48\nBRK             #1"}
{"memset": "Function memset:\n--- Block memset @ 0x140015308 ---\nADRP            X16, #__imp_memset@PAGE\nLDR             X16, [X16,#__imp_memset@PAGEOFF]\nBR              X16"}
{"sub_140007AE8": "Function sub_140007AE8:\n--- Block sub_140007AE8 @ 0x140007ae8 ---\nSTP             X19, X20, [SP,#-0x10+var_10]!\nSTP             X29, X30, [SP,#0x10+var_s0]\nADD             X29, SP, #0x10\nADRP            X8, #GetProcessHeap@PAGE\nMOV             X19, X2\nMOV             W20, W1\nLDR             X8, [X8,#GetProcessHeap@PAGEOFF]\nBLR             X8\nCBZ             X0, loc_140007B28"}
{"ExReleaseFastMutexUnsafeAndLeaveCriticalRegion": "Function ExReleaseFastMutexUnsafeAndLeaveCriticalRegion:\n--- Block ExReleaseFastMutexUnsafeAndLeaveCriticalRegion @ 0x1400155d8 ---\nPACIBSP\nSTP             X29, X30, [SP,#-0x10+var_s0]!\nMOV             X29, SP\nBL              sub_140015938\nBL              sub_140015438\nLDP             X29, X30, [SP+var_s0],#0x10\nAUTIBSP\nRET"}
{"sub_1400127B4": "Function sub_1400127B4:\n--- Block sub_1400127B4 @ 0x1400127b4 ---\nLDP             X8, X2, [X0]\nMOV             X0, X1\nMOV             X1, X8\nB               sub_140011FA4"}
{"memcpy": "Function memcpy:\n--- Block memcpy @ 0x14001532c ---\nADRP            X16, #__imp_memcpy@PAGE\nLDR             X16, [X16,#__imp_memcpy@PAGEOFF]\nBR              X16"}
{"_guard_check_icall_nop": "Function _guard_check_icall_nop:\n--- Block _guard_check_icall_nop @ 0x140001048 ---\nRET"}
{"sub_140013BA0": "Function sub_140013BA0:\n--- Block sub_140013BA0 @ 0x140013ba0 ---\nLDR             W0, [X0]\nMOV             X2, X1\nMOV             W1, #1\nB               sub_1400135B8"}
{"sub_140015220": "Function sub_140015220:\n--- Block sub_140015220 @ 0x140015220 ---\nADRP            X16, #dword_14001D070@PAGE\nLDR             W16, [X16,#dword_14001D070@PAGEOFF]\nTBZ             W16, #0, loc_140015234\n\n--- Block sub_140015220 @ 0x140015234 ---\nCBZ             X16, loc_14001524C\n\n--- Block sub_140015220 @ 0x14001524c ---\nADR             X15, sub_140015220\nB               loc_1400152B0"}
{"sub_1400127D4": "Function sub_1400127D4:\n--- Block sub_1400127D4 @ 0x1400127d4 ---\nLDP             X0, X8, [X0,#8]\nLDR             X2, [X8,#0x20]\nBR              X2"}
{"sub_140016BE4": "Function sub_140016BE4:\n--- Block sub_140016BE4 @ 0x140016be4 ---\nSTP             X29, X30, [SP,#-0x10+var_s0]!\nMOV             X29, SP\nBL              sub_1400036A8\nBRK             #1"}
{"sub_1400127C4": "Function sub_1400127C4:\n--- Block sub_1400127C4 @ 0x1400127c4 ---\nLDP             X8, X1, [X1,#8]\nLDP             X2, X3, [X0]\nMOV             X0, X8\nB               sub_1400124BC"}
{"sub_1400069D0": "Function sub_1400069D0:\n--- Block sub_1400069D0 @ 0x1400069d0 ---\nSTP             X29, X30, [SP,#-0x10+var_s0]!\nMOV             X29, SP\nADRP            X8, #SetLastError@PAGE\nMOV             W0, #0x78 ; 'x'\nLDR             X8, [X8,#SetLastError@PAGEOFF]\nBLR             X8\nMOV             W0, #0x80004001\nLDP             X29, X30, [SP+var_s0],#0x10\nRET"}
{"sub_140015AE8": "Function sub_140015AE8:\n--- Block sub_140015AE8 @ 0x140015ae8 ---\nB               sub_140015A20"}
{"_ZN3std3sys12thread_local6native5eager7destroy17h3e9b8d3528646f61E": "Function _ZN3std3sys12thread_local6native5eager7destroy17h3e9b8d3528646f61E:\n--- Block _ZN3std3sys12thread_local6native5eager7destroy17h3e9b8d3528646f61E @ 0x140001478 ---\nLDR             X8, [X0]\nMOV             W9, #2\nSTRB            W9, [X0,#8]\nCBZ             X8, locret_1400014A4\n\n--- Block _ZN3std3sys12thread_local6native5eager7destroy17h3e9b8d3528646f61E @ 0x140001498 ---\nB.NE            locret_1400014A4\n\n--- Block _ZN3std3sys12thread_local6native5eager7destroy17h3e9b8d3528646f61E @ 0x14000149c ---\nDMB             ISHLD\nB               sub_140002D90\n\n--- Block _ZN3std3sys12thread_local6native5eager7destroy17h3e9b8d3528646f61E @ 0x1400014a4 ---\nRET"}
{"sub_140015CE8": "Function sub_140015CE8:\n--- Block sub_140015CE8 @ 0x140015ce8 ---\nADRP            X8, #dword_14001D0C8@PAGE\nLDR             W8, [X8,#dword_14001D0C8@PAGEOFF]\nCMP             W8, #0\nCSET            W0, NE\nRET"}
{"sub_1400036A8": "Function sub_1400036A8:\n--- Block sub_1400036A8 @ 0x1400036a8 ---\nSTP             X29, X30, [SP,#-0x10+var_s0]!\nMOV             X29, SP\nBL              sub_140003780\nBRK             #1"}
{"sub_140005AB8": "Function sub_140005AB8:\n--- Block sub_140005AB8 @ 0x140005ac0 ---\nTBNZ            W8, #0x1A, loc_140005ACC\n\n--- Block sub_140005AB8 @ 0x140005ac4 ---\nB               sub_140013BCC"}
{"sub_140016484": "Function sub_140016484:\n--- Block sub_140016484 @ 0x140016484 ---\nSTP             X29, X30, [SP,#-0x10+var_s0]!\nMOV             X29, SP\nADRL            X1, aFailedToGenera; \"failed to generate unique thread ID: bi\"...\nADRL            X2, off_140017640; \"<redacted>\"\nMOV             W0, #0x6F ; 'o'\nBL              sub_140016DAC\nBRK             #1"}
{"__alloca_probe": "Function __alloca_probe:\n--- Block __alloca_probe @ 0x1400151b0 ---\nLDR             X17, [X18,#0x10]\nSUBS            X16, SP, X15,LSL#4\nCSEL            X16, XZR, X16, CC\nCMP             X16, X17\nB.CC            loc_1400151C8"}
{"sub_140015AE0": "Function sub_140015AE0:\n--- Block sub_140015AE0 @ 0x140015ae0 ---\nBRK             #0xF003"}
{"sub_140003BBC": "Function sub_140003BBC:\n--- Block sub_140003BBC @ 0x140003bbc ---\nLDP             X9, X8, [X0]\nMOV             X2, X1\nMOV             X0, X8\nMOV             X1, X9\nB               sub_140012B6C"}
{"sub_140013BB0": "Function sub_140013BB0:\n--- Block sub_140013BB0 @ 0x140013bb0 ---\nLDR             W8, [X0]\nMOV             X2, X1\nMVN             W9, W8\nCMP             W8, #0\nLSR             W1, W9, #0x1F\nCNEG            W0, W8, MI\nB               sub_1400135B8"}
{"_initialize_onexit_table": "Function _initialize_onexit_table:\n--- Block _initialize_onexit_table @ 0x140015e04 ---\nADRP            X16, #__imp__initialize_onexit_table@PAGE\nLDR             X16, [X16,#__imp__initialize_onexit_table@PAGEOFF]\nBR              X16"}
{"sub_140016BC0": "Function sub_140016BC0:\n--- Block sub_140016BC0 @ 0x140016bc0 ---\nSTP             X29, X30, [SP,#-0x10+var_s0]!\nMOV             X29, SP\nADRL            X1, aCapacityOverfl; \"capacity overflow\"\nADRL            X2, off_140018FA8; \"<redacted>\"\nMOV             W0, #0x23 ; '#'\nBL              sub_140016DAC\nBRK             #1"}
{"sub_1400132E0": "Function sub_1400132E0:\n--- Block sub_1400132E0 @ 0x1400132e0 ---\nMOV             X3, X2\nMOV             X2, X1\nTBNZ            W2, #0, loc_1400132F8\n\n--- Block sub_1400132E0 @ 0x1400132ec ---\nADRL            X1, unk_1400193A0\nB               sub_1400124BC\n\n--- Block sub_1400132E0 @ 0x1400132f8 ---\nLSR             X2, X2, #1\nMOV             X1, X3\nB               sub_1400130C8"}
{"sub_140006048": "Function sub_140006048:\n--- Block sub_140006048 @ 0x140006048 ---\nTBNZ            W1, #0, loc_140006050\n\n--- Block sub_140006048 @ 0x14000604c ---\nB               sub_1400074D0\n\n--- Block sub_140006048 @ 0x140006050 ---\nLSR             X8, X1, #1\nMOV             X1, X2\nMOV             X2, X8\nB               sub_140005F00"}
{"sub_140016D8C": "Function sub_140016D8C:\n--- Block sub_140016D8C @ 0x140016d8c ---\nSTP             X29, X30, [SP,#-0x10+var_s0]!\nMOV             X29, SP\nMOV             W8, #1\nORR             X8, X8, X1,LSL#1\nMOV             X1, X0\nMOV             X0, X8\nBL              sub_140016DAC\nBRK             #1"}
{"sub_140012400": "Function sub_140012400:\n--- Block sub_140012400 @ 0x140012400 ---\nLDP             X0, X8, [X0,#8]\nLDR             X3, [X8,#0x18]\nBR              X3"}
{"sub_140016BA4": "Function sub_140016BA4:\n--- Block sub_140016BA4 @ 0x140016ba4 ---\nSTP             X29, X30, [SP,#-0x10+var_s0]!\nMOV             X29, SP\nCBNZ            X1, loc_140016BB8\n\n--- Block sub_140016BA4 @ 0x140016bb0 ---\nBL              sub_140016BC0\nBRK             #1\n\n--- Block sub_140016BA4 @ 0x140016bb8 ---\nBL              sub_140016BE4\nBRK             #1"}
{"sub_140003780": "Function sub_140003780:\n--- Block sub_140003780 @ 0x1400037b4 ---\nCBNZ            W10, locret_1400037C8\n\n--- Block sub_140003780 @ 0x1400037b8 ---\nDMB             ISH\nADRP            X8, #dword_14001D118@PAGE\nLDR             W8, [X8,#dword_14001D118@PAGEOFF]\nCBZ             W8, loc_1400037CC\n\n--- Block sub_140003780 @ 0x1400037c8 ---\nRET\n\n--- Block sub_140003780 @ 0x1400037e4 ---\nCMN             W0, #1\nB.EQ            locret_1400037C8"}
{"sub_140002158": "Function sub_140002158:\n--- Block sub_140002158 @ 0x140002158 ---\nMOV             W0, #1\nRET"}
{"sub_140016E48": "Function sub_140016E48:\n--- Block sub_140016E48 @ 0x140016e48 ---\nSTP             X29, X30, [SP,#-0x10+var_s0]!\nMOV             X29, SP\nMOV             X2, X0\nADRL            X0, unk_1400192C4\nMOV             W1, #0x2B ; '+'\nBL              sub_140016D8C\nBRK             #1"}
{"sub_1400167F8": "Function sub_1400167F8:\n--- Block sub_1400167F8 @ 0x1400167f8 ---\nSTP             X29, X30, [SP,#-0x10+var_s0]!\nMOV             X29, SP\nLDR             X1, [X0]\nBL              sub_14001692C\nCMN             X1, #1\nB.NE            loc_140016818\n\n--- Block sub_1400167F8 @ 0x140016818 ---\nBL              sub_140016BA4\nBRK             #1"}
{"sub_140015E34": "Function sub_140015E34:\n--- Block sub_140015E34 @ 0x140015e34 ---\nSTP             X29, X30, [SP,#-0x10+var_s0]!\nMOV             X29, SP\nADRL            X1, (aFunctionStubIs+0x2D); \"[!] Not implemented yet<redacted>\"\nADRL            X2, off_140017560; \"<redacted>\"\nMOV             W0, #0x2F ; '/'\nBL              sub_140016DAC\nBRK             #1"}
{"?AddGammaXfrm@Color@Art@@$$hQEAAXXZ": "Function ?AddGammaXfrm@Color@Art@@$$hQEAAXXZ:\n--- Block ?AddGammaXfrm@Color@Art@@$$hQEAAXXZ @ 0x140015410 ---\nPACIBSP\nSTP             X29, X30, [SP,#-0x10+var_s0]!\nMOV             X29, SP\nBL              sub_140015B70\nBL              sub_140015A20\nBL              _set_new_mode\nLDP             X29, X30, [SP+var_s0],#0x10\nAUTIBSP\nRET"}
{"sub_140001458": "Function sub_140001458:\n--- Block sub_140001458 @ 0x140001458 ---\nLDR             X1, [X0,#0x10]\nCMN             X1, #1\nB.EQ            locret_140001474\n\n--- Block sub_140001458 @ 0x140001464 ---\nCBZ             X1, locret_140001474\n\n--- Block sub_140001458 @ 0x140001474 ---\nRET"}
{"sub_140013BCC": "Function sub_140013BCC:\n--- Block sub_140013BCC @ 0x140013bcc ---\nLDR             X0, [X0]\nMOV             X2, X1\nMOV             W1, #1\nB               sub_1400135B8"}
{"sub_140016C2C": "Function sub_140016C2C:\n--- Block sub_140016C2C @ 0x140016c2c ---\nSTP             X29, X30, [SP,#-0x10+var_s0]!\nMOV             X29, SP\nBL              sub_140011134\nBRK             #1"}
{"sub_140004B90": "Function sub_140004B90:\n--- Block sub_140004B90 @ 0x140004b90 ---\nLDP             X0, X8, [X0]\nLDR             X2, [X8,#0x18]\nBR              X2"}
{"sub_140012B6C": "Function sub_140012B6C:\n--- Block sub_140012B6C @ 0x140012b6c ---\nMOV             X8, X1\nMOV             X1, X0\nMOV             X0, X2\nMOV             X2, X8\nB               sub_140011FA4"}
{"sub_140016DE0": "This function sets up a stack frame, initializes local variables with addresses (likely forming a structure or context), calls `sub_140016DAC`, then triggers a breakpoint (`BRK #1`). It appears to be a stub or diagnostic function—possibly part of a debugging, instrumentation, or error-handling path—where `sub_140016DAC` likely performs some operation using the initialized data, and the `BRK` ensures control returns to a debugger."}
{"___scrt_release_startup_lock": "This function releases a startup lock, conditionally performing cleanup if the lock was held (i.e., if the input byte in `W0` is zero). It preserves caller-saved registers (`X19`), calls `sub_140015CE8` to check/release the lock, and if the result indicates success (`W0 == 0`) and the original input was zero, it further initializes/cleans up a static object via `sub_140015220` (likely `memset` or similar). Ends by restoring the frame and returning."}
{"sub_14000695C": "This function resolves `SetThreadDescription` from `kernel32.dll` (if available), then calls a local stub (`sub_1400069D0`) that wraps it, or directly calls `SetThreadDescription` if the stub isn’t needed. It saves the result (either the stub or the resolved function pointer) to a global variable (`off_14001D060`) for later use, and finally jumps to that function pointer with the original arguments (`X0`, `X1`). Effectively, it’s a lazy-initialization trampoline for `SetThreadDescription`."}
{"sub_140010FD8": "This function appears to be part of a string-to-hex-byte conversion routine (e.g., parsing a hex string like `\"0123456789ABCDEF\"` into bytes). It prepares arguments and calls `sub_140011760`, likely the core parser — itself likely a standard helper for decoding hex strings. The use of `ADRL` to load the hex string address and the initial setup of `X0=X1`, `W1=1`, `W3=2` suggests it configures parsing options (e.g., case sensitivity, length). The final stack cleanup and `RET` confirm ABI compliance and normal return."}
{"sub_140006C38": "This function dynamically resolves and invokes Windows Debug Help Library (DbgHelp) APIs—specifically `SymFromInlineContextW`, `SymGetLineFromInlineContextW`, `SymAddrIncludeInlineTrace`, and `SymQueryInlineTrace`—to perform symbol resolution for an inline call stack. It conditionally calls these APIs based on the `W26` flag (bit 0) and processes the results to expand inline call frames. It allocates stack space, loads or delays-loads the required symbols via `GetProcAddress`, and performs character encoding conversion (`WideCharToMultiByte`) when outputting formatted strings. Ultimately, it iterates over inline frames, resolving symbols and printing formatted trace output (likely for stack walking/debugging), returning early on failure or exhaustion."}
{"sub_1400150A4": "This function performs a lookup in a precomputed table (`unk_14001A338`) to map an input value `W0` (likely an opcode or similar) to an index, then uses that index to access a second table (`unk_14001A038`) of byte-sized weights. It accumulates these weights until the cumulative sum exceeds a computed threshold derived from `W0`. Returns 1 if the threshold is exceeded, else 0 (after masking the index with 1 at the end).\n\n**High-level summary:**  \nA table-driven decision logic: based on `W0`, computes a scaled comparison value, does a binary-search-like table walk, and checks if cumulative byte weights exceed a threshold — returning a boolean flag."}
{"sub_140004538": "This function performs a **guarded one-time initialization** of a global flag (at `byte_14001D11C`), using a compare-and-set loop with `LDAXRB`/`STXRB` (load-acquire exclusive / store-release exclusive).  \n\n- It attempts to atomically set the flag (value `1`) if it's currently zero.\n- If the flag is already set, it jumps directly to `sub_1400164A8` (likely a slow-path initializer, e.g., constructor or setup routine).\n- After ensuring the flag is set (or confirming it was already set), it returns — passing the flag address in `X0`, likely as part of a C++ static initializer pattern (e.g., `__cxa_guard_acquire`/`release` semantics).\n\nIn short: **`__cxa_guard_acquire`-style lock-free initialization guard**."}
{"sub_14000D3F8": "This function parses a tokenized input stream (likely Rust-style type expressions or syntax) using a recursive-descent parser. It maintains state in a struct (pointed to by `X0`), tracks recursion depth (incrementing/decrementing at `#0x18`), and checks for limits (`#0x1F5`). It decodes characters like `'B'`, `'C'`, `'I'`, `'L'`, `'M'`, `'N'`, `'W'`, `'X'`, `'Y'` into specific parsing branches—some involving recursion (`sub_14000D3F8`), error handling via string formatting (`sub_140012B6C`), and type/variant construction (`sub_14000B5BC`, `sub_14000BFEC`, `sub_14000DAE4`, etc.). Error conditions trigger diagnostics and early returns (often `BRK #1` in impossible cases). The function returns zero on success, non-zero on error or recursion limit exceeded."}
{"___scrt_initialize_onexit_tables": "This function initializes `onexit` tables used for managing cleanup handlers (e.g., `atexit`/`onexit` register callbacks). It checks an initialization flag (`byte_14001D208`); if already initialized, returns success (`W0=1`). Otherwise:\n\n- Validates the input parameter `W0` (must be ≤1; else triggers error `sub_140015AE0(5)`).\n- Calls `sub_140015CE8` (likely CRT startup guard or state check).\n- Initializes two `onexit` tables via `_initialize_onexit_table` (pointers at `qword_14001D1D8` and `qword_14001D1F0`).\n- On failure of either init, zeros out the table headers (marks as uninitialized/faulty via `0xFFFFFFFFFFFFFFFF`).\n- On success, sets the initialization flag (`byte_14001D208 = 1`) and returns success.\n\nReturn value: `1` on success or already initialized; error code otherwise.\n\nEssentially a one-time CRT initializer for `onexit` table management."}
{"sub_140013E4C": "This function performs a **fast byte search** in a memory buffer for a target byte value (passed in `W0`), with special optimizations for larger buffers:\n\n- It first aligns the search start address to an 8-byte boundary and computes the number of bytes remaining before that alignment (`X8` = padding to align).\n- If the buffer size `X2` is ≤ padding, or empty, it falls back to a simple byte-by-byte scan.\n- For larger buffers, it uses a SIMD-ish optimization (via `LDP`, `EOR`, `SUB`, `ORR`, `AND`, `BICS`) to check **16 bytes at a time** using bit-twiddling to detect the target byte (similar to classic SIMD fallbacks like `pcmpeqb` + `pmovmskb`).\n- After processing aligned 16-byte chunks, it handles any remaining tail bytes individually.\n- Returns:\n  - `X0 = 0` if the byte is **not found**,\n  - `X0 = 1` and `X1 = pointer to first occurrence` if **found**.\n\nIn short: **`memchr`-like function — finds first occurrence of a byte in memory, with 16-byte vectorized scan for speed.**"}
{"sub_140016DAC": "This function sets up a stack frame, initializes local variables (including storing the original stack pointer and some arguments), calls `sub_1400016F8`, and then triggers a breakpoint (`BRK #1`)—suggesting it's a stub or debugging wrapper, likely for hooking or tracing purposes."}
{"sub_1400016F8": "This function initializes a local structure on the stack by copying two 8-byte values from offsets `0x18` and `0x8` of the first argument (`X0`), then calls `sub_140001600` with a pointer to that structure. The `BRK #1` indicates a breakpoint/debug trigger—likely a placeholder or diagnostic hook."}
{"___scrt_initialize_crt": "This function initializes the C runtime, handling both single-threaded and multi-threaded scenarios based on the input flag (`W0`). It sets a global flag (at `byte_14001D1CC`) indicating initialization state, then calls `sub_140015A20` (likely CRT core setup) and `sub_140002158` (likely TLS/thread-local storage setup). If `sub_140002158` fails (return value bit 0 clear), it retries once; if it fails again, it sets exit code to 1, otherwise 0. Finally, it restores the frame and returns."}
{"__onexit": "This function implements `__onexit`, which registers a function to be called at program exit (similar to `atexit`). It checks if the exit table pointer (loaded from `qword_14001D1D8`) is null (via `CMN X8, #1`, i.e., `X8 == -1`). If it is, it falls back to calling `_crt_atexit`. Otherwise, it calls `_register_onexit_function` with the function pointer (`X0` input) and a pointer to the exit table. The return value is the function pointer on success (or zero on failure, as indicated by `CSEL X0, X19, XZR, EQ`)."}
{"windows::main()": "This `windows::main()` function performs the following:\n\n1. Allocates 0x3000 bytes of RWX memory using `VirtualAlloc`.\n2. If allocation fails, prints an error and breaks into the debugger.\n3. Uses `NtProtectVirtualMemory` (via `sub_140015E34`, inferred from signature and parameters: handle, base address, size, protection) to change the memory protection to read-write (0x40).\n4. Logs success or failure of the protection change.\n5. Returns.\n\nIn short: **It allocates executable memory and then downgrades its protection to RW (non-executable), likely for self-modifying or reflective code loading purposes, with error handling and debug output.**"}
{"___scrt_uninitialize_crt": "This function is the CRT (C Runtime) cleanup/uninitialization routine, likely called during process termination or DLL detachment.\n\n- It begins by preserving registers and checking a global flag (`byte_14001D1CC`)—if zero, it skips cleanup.\n- If the flag is non-zero:\n  - It examines bit 0 of the input `w1`:\n    - If set, it branches to a special cleanup path (likely for thread exit).\n  - Otherwise, it calls `sub_14002158` **twice** with the original first argument (`w19`, which is preserved from `w0`).\n- Finally, it restores registers, returns with `w0 = 1`.\n\nGiven the double call to the same destructor helper and standard ABI usage, this strongly resembles `_cexit()` or `_crt_exit_thread()`-style logic, ensuring deterministic destruction of C++ statics and atexit handlers—possibly for thread-local cleanup.\n\nIn short:  \n**CRT uninitialization with conditional full cleanup (on flag) and dual destructor invocation, returning 1 on success.**"}
{"sub_140016C3C": "This function appears to be a **stub or trampoline** that prepares arguments and calls `sub_140016DAC`, likely for exception/debug handling (evidenced by the `BRK #1` immediately after the call). The arguments suggest it's setting up a structured exception or callback context:  \n- `X0` (stored at `[SP+var_40]`) is the return address or context pointer  \n- `X1`, `X2`, `X3`, `X4` are passed to the callee as usual  \n- `X8` is set to `SP` (stack pointer), likely for unwinding or stack trace purposes  \n- Several addresses are loaded (`sub_140004B90`, `sub_1400127B4`, `unk_140019191`) and stored on the stack, possibly as part of an exception record, unwind info, or callback table.  \n\nThe `BRK #1` indicates this is likely a **debug breakpoint or crash handler stub** — execution will not resume after the call."}
{"sub_14000BFEC": "This function processes a structure (passed in `X0`) that appears to represent a parser state or input buffer with fields at offsets `+8` (likely a pointer to input data), `+0x10` (current position/index), `+0x20` (end position), and `+0x28` (some callback or context). It scans the input buffer for the ASCII character `'E'` (0x45), starting from the current position.\n\n- If `'E'` is found at the current position (`X9`), it increments the end position (`+0x20`) and exits with `X19 = 0`.\n- If not, and if the current position has reached the end (`X9 >= X10`), it checks a callback (loaded from `+0x28`) via `sub_140012B6C`; depending on result, it may call another cleanup/advance function (`sub_14000EBCC`) and loop or exit.\n- The loop continues until `'E'` is found or a termination condition is met.\n\nIn short: **This function scans an input buffer for the character `'E'`, updating internal state as it progresses, and handles buffer refill or error conditions via callbacks.**"}
{"sub_14000D08C": "This function parses a base64-encoded string (or similar 6-bit encoded data), validating and decoding it into an output buffer pointed to by `X0`, returning a success/failure status in `X0[0]`. It reads input from a struct at `X1` (containing a pointer to data in `[X1]`, length in `[X1,#8]`, and current offset in `[X1,#0x18]`).  \n\n- It first checks if at least two characters remain (`offset < len`).  \n- Validates first two characters: must be `'s'` followed by `'_'`.  \n- Then decodes subsequent base64-like characters (`[A-Za-z0-9_+]`) into 6-bit values, accumulating into a 64-bit state.  \n- On encountering an invalid character (or padding-like condition), it either aborts (stores 0 in output, returns failure) or continues until end-of-input.  \n- At end-of-input, it stores decoded byte(s) (if any) to `X0`, sets success flag, and returns.\n\nThe function resembles a specialized base64 decoder that enforces a specific prefix (`\"s_\"`) and uses a custom alphabet (likely RFC 4648 with `'_'` instead of `'+'` or `'/'`, and missing `'/'`)."}
{"sub_140002CC0": "This function performs a bounds-checked memory allocation and copy operation, likely implementing a safe string or buffer copy (e.g., `strncpy`/`wcsncpy`-like behavior), followed by heap deallocation of an input buffer.\n\n**Summary:**\n- Validates that input length (`X2`) is ≤ 0x11; if not, jumps to `loc_140002D10`.\n- Calls `sub_140007AE8` (likely `HeapAlloc` or similar) with size `X2 + X0` (where `X0` is first arg, `X3` is fourth arg) and zero-initialization.\n- On allocation success:\n  - Computes a masked length (`X8 = (len-1) & something`, possibly alignment or clamp).\n  - Chooses source/dest pointers conditionally (`CSEL`).\n  - Copies `X0` bytes via `memcpy`.\n  - Frees original source buffer (loaded from `[X19,#-8]`) using `HeapFree`.\n- On allocation failure (or early exit), returns `NULL` (`XZR`).\n\n**Calling convention:** ABI-compliant; `X0`–`X3` are args, return in `X0`.  \n**Likely purpose:** Safe string/buffer copy with heap-managed temporary storage and cleanup."}
{"sub_140001C98": "This function performs a version/checksum validation and possibly sets up a TLS-based structure before calling a helper function (`sub_140002A40`), returning 0 (success) unconditionally.\n\n### Summary (behavior):\n1. Allocates stack frame.\n2. Loads a 32-bit value from `[X0]`, compares it with `0xC00000FD`. If not equal, jumps directly to cleanup/return (W0 = 0).\n3. If equal, retrieves a TLS index, uses it to load a pointer, adds a fixed offset (`#0x18`), and compares the loaded value (likely a version or count field) with `2`.\n   - If ≤ 2, jumps to clear X0 = 0 (i.e., early exit with 0).\n   - Else, subtracts 16 from that value (possibly computing an offset or array index), stores it on stack, and proceeds.\n4. Calls `sub_140002A40`, passing address of local `var_8` (possibly buffer or structure) in X0.\n5. Always returns 0.\n\nIn short:  \n**Validates a magic constant; optionally uses TLS to fetch a versioned structure, adjusts an offset if version > 2, and invokes a helper. Always returns success (0).**"}
{"sub_140011EA8": "This function performs a conditional sequence of operations involving two calls to `sub_140012D1C`, followed by inspecting flags from a data structure (possibly a control or state object) and conditionally invoking a third function via a vtable-like dispatch, returning a final flag (`W0 & 1`). It appears to be part of a serialization, validation, or control-flow guard routine — likely related to decoding, checking, or formatting structured data, where `sub_140012D1C` is probably a parser or validator, and the final dispatch (`BLR X8`) handles a special case (e.g., closing/ending a nested structure). The return value is `1` if either of the two prior calls succeeded (or had certain flags set) and the final condition holds.\n\nKey clues:\n- Two `BL sub_140012D1C` calls with different argument sets.\n- Flags stored in `var_67` and `var_68` (probably status flags from the calls).\n- A conditional branch to either skip or perform a final vtable call based on those flags.\n- The final vtable call uses the second load from `[X8,#8]`, suggesting an object with a function pointer at offset `0x18`.\n- Returns `(flag1 | flag2) & 1`, likely indicating success/failure of the overall operation."}
{"sub_140016B04": "This function appears to perform bounds-checked allocation or size computation (possibly for a buffer or structure), with strong assertions:\n\n- Validates `X3` (likely a size or count) is non-zero (else hits `BRK`).\n- Computes `X8 = X2 + X1` (possibly size + offset) and checks for overflow (branch on carry).\n- Computes a scaled size: `X9 = X2 << 1` (doubled), and adjusts based on `X3` (e.g., 4 or 8-byte alignment/element size).\n- Chooses maximum between `X8` and scaled/adjusted `X9`, then calls `sub_14001699C` — likely an allocation function (e.g., `malloc`, `VirtualAlloc`) — passing this computed size.\n- After allocation, if the result (at `[SP+...var_28]`) equals 1 (possibly a status or sentinel), it jumps to a failure handler (`loc_140016B98`), which again calls `sub_140016BA4` (likely a logging or assertion helper) and then triggers a breakpoint (`BRK #1`) — strongly suggesting this is an assertion or bounds-violation trap.\n\n**Summary**: A size-validation and allocation function (likely `malloc` wrapper), enforcing alignment and overflow checks; aborts on invalid input or allocation failure."}
{"sub_140001FB4": "This function processes a buffer in 16-byte chunks, copying or shifting data based on the size provided in `X2` (multiplied by 16), against a structure pointed to by `X0` (likely a ring buffer or circular queue), where:\n\n- `[X0]` holds the current read position (head),\n- `[X0+0x10]` holds the write position (tail),\n- `X1` points to the source data.\n\nIt ensures that:\n- If `X2 == 0`, it returns 0 immediately.\n- If `X2 > 9`, it's out-of-bounds for the internal lookup/table (so diverges — likely error path or switch dispatch on count).\n- It checks available space (`tail - head`) and:\n  - If insufficient, it calls `sub_140016758` (likely a flush or rotation helper) before proceeding.\n  - Then performs `memcpy` of up to 16 bytes at a time into the buffer.\n  - Updates the head pointer (`[X0] += len`) and loops until all `X2 * 16` bytes are processed.\n\n**Conclusion**: This is likely a **buffer ingestion/insertion routine**, such as for a ring buffer or queue, where up to 16×N bytes are copied from a source into a buffer while updating internal state, possibly with rotation or flushing logic when full."}
{"sub_140002E80": "This function performs a thread-local storage (TLS)-based initialization check and potential call to `sub_140015E58`, returning 1 on success or 0 on early exit.\n\n**High-level summary:**\n\n- Sets up stack frame and checks a global flag (`byte_14001D100`). If unset, it sets the flag and proceeds.\n- Retrieves TLS index and uses it to compute a TLS data pointer (`X19 = tls_ptr + 8`), then loads a 1-byte value at offset +8 from that.\n- Depending on that byte’s value:\n  - **If 1**: Performs an atomic load barrier (`DMB ISHLD`) and calls `sub_14002D90` with a stack address (likely a TLS-related setup or initialization hook), then returns 1.\n  - **If 2**: Saves `X0`, calls `sub_140015E58(X19)` (likely a TLS destructor or cleanup routine), then restores `X0`, clears the TLS pointer slot at `[X19]`, and returns 0.\n  - **If 0 or other**: Returns 0 without doing anything meaningful.\n- If `X0 == 0` (null input) early on, it also returns 0.\n\nThe function effectively implements a one-time TLS initialization guard with different behavior depending on a state byte in TLS metadata."}
{"sub_140007FD0": "This function encodes a Unicode code point (passed in `W1`, with pointer to some structure in `X0`) into UTF-8 bytes in a local buffer, then copies those bytes using `memcpy`, and performs cleanup involving reference counting or object finalization (likely part of a string/UTF-8 conversion utility in a runtime or library). It returns 1 if the copy size was less than the required size (i.e., overflow), 0 otherwise.\n\nKey behaviors:\n- Encodes `W1` (a code point) into 1–4 UTF-8 bytes into `[SP+var_34]`, depending on its value:\n  - ≤0x7F → 1-byte sequence (ASCII)\n  - ≤0x7FF → 2-byte sequence\n  - ≤0xFFFF → 3-byte sequence\n  - >0xFFFF → 4-byte sequence\n- Copies the encoded bytes from the stack buffer to a destination provided via a structure (first element is destination pointer, next is range/limit).\n- Updates the structure’s internal write pointer (`[X21,#0x10]`).\n- Conditionally calls destructors/finalizers on an object at `[X19,#8]` if it exists and meets alignment/validity criteria.\n- Finalizes/sets `[X19,#8]` to a known value (`unk_1400187A8`).\n- Returns 1 if the encoded length < requested size (i.e., insufficient space), 0 otherwise."}
{"sub_140011134": "This function performs **UTF-8 validation and decoding on a substring** of a null-terminated byte string pointed to by `X0`, bounded by indices in `X1` (start) and `X2` (end), with `X3` used as an intermediate index. It checks whether the characters in the range are valid ASCII letters (A–Z, a–z) and/or valid UTF-8 sequences (2–4 bytes). If validation fails, it calls an error-reporting function (`sub_140016DAC`) and triggers a breakpoint (`BRK #1`). If valid, it decodes the UTF-8 characters into codepoints and either:\n\n- Invokes a callback (`sub_140016E48`) for equality/identity cases (`X2 == X3`),  \n- Or passes the decoded codepoint(s) and indices to another handler (`sub_140016C2C`) for further processing.\n\nThe repeated structure suggests a loop over characters or ranges, with careful boundary checking (indices vs length in `X1`), character classification (using `LDRSB`, `CMN #0x41`, `CMN #0x40`), and UTF-8 multibyte decoding logic (checking lead bytes like `0xE0`, `0xF0`, masking continuation bytes with `& 0x3F`, shifting, and composing via `ORR`/`BFI`). The `BRK #1` instructions indicate intentional halts — likely used for debugging or error enforcement in a hardened build."}
{"sub_1400135B8": "This function converts a 64-bit unsigned integer input (`X0`) into its **decimal ASCII string representation**, storing the result on the stack, then calls another function (`sub_140011760`) to likely handle output (e.g., printing or buffering the string).  \n\n### Key Observations:\n- **Stack frame**: Allocates 0x30 bytes, saves `X29/X30`.\n- **Digit extraction**: Repeatedly computes decimal digits via multiplication-based division (using `UMULH` with precomputed magic constants), subtracting off multiples of powers of 10, starting from the most significant digit (10¹⁴ down to 10⁰).\n- **Conditional digit storage**: Uses `B.CS` (branch if carry set, i.e., value ≥ threshold) to decide whether to store a digit at each position; else it falls through to store a leading `'1'` for very large numbers.\n- **Final call**: Calls `sub_140011760` with:\n  - `X0`: pointer to first digit (likely address of `var_14`, the last/stored digit),\n  - `W2`: length (0x14 = 20 digits max),\n  - `X3`: 0 (likely flags),\n  - likely treating the result as a null-terminated string (though not explicitly null-terminated here—might be handled by callee).\n\nThis is a **fast decimal conversion routine**, avoiding expensive division instructions by using multiplicative inverses (a common optimization in format/string functions)."}
{"sub_140001234": "This function performs **hook detection for a `LoadLibraryA` stub**, likely as an anti-analysis or integrity-checking mechanism. It:\n\n1. **Loads `ntdll.dll`** using `LoadLibraryA`.\n2. **Retrieves the address of `LoadLibraryA`** via `GetProcAddress`.\n3. **Validates the first 4 bytes** of the retrieved `LoadLibraryA` entrypoint: checks for the byte sequence `0x4C 0x8B 0xD1 0xB8`, which corresponds to the canonical Windows `LoadLibraryA` stub on x64 (e.g., `mov r10, rcx; mov eax, 0xXXXX` — the actual dispatch stub before jumping to the real implementation).\n4. If validation fails, it **triggers a debug break** (`BRK #1`) after logging a hook detection message.\n5. If validation passes, it:\n   - Calls a helper (`sub_14000314C`, likely `RtlInstallFunctionTableCallback` or similar) with a patch/redirect setup involving `sub_1400110C0`.\n   - Later calls `sub_140001040` (likely a logging or initialization function) if successful.\n   - Eventually **calls `sub_140012B6C`**, passing a code pointer (possibly a trampoline or redirector), suggesting it’s installing or preparing a hook or callback.\n\nIn short:  \n**This function verifies the integrity of `LoadLibraryA`'s implementation, alerts if it's been hooked, and proceeds to install or prepare a custom hook/call redirect.**"}
{"sub_140007F28": "This function implements a bounds-checked memory copy operation, likely part of a dynamic buffer or array growth routine.\n\n**Summary:**\n\n- Takes three arguments: `X0` (likely a pointer to a structure containing a buffer pointer and capacity), `X1` (destination pointer), `X2` (requested byte count).\n- Checks the least-significant bit of `X2` (via `TBNZ`); if set, it falls into an error-handling path (`sub_1400124BC`), possibly indicating an invalid/odd-sized request.\n- On the normal path:\n  - Loads `buffer_ptr` (`[X0]`) and `current_end`/`capacity` (`[X0+0x8]`, `[X0+0x10]`).\n  - Computes remaining capacity (`capacity - current_end`) and compares against requested size (`X2 >> 1` — half the input, suggesting word-granularity copy or 2× size unit).\n  - If `requested > capacity`, it calls `sub_140016758` (likely a reallocation helper) to grow the buffer, then retries the copy logic.\n  - Copies `requested` bytes from `X1` to `buffer_ptr + current_end` using `memcpy`.\n  - Updates `current_end` (stored back to `[X0+8]`) and returns.\n\n**In short:**  \n`sub_140007F28` copies up to `X2>>1` bytes into a buffer managed by a struct at `X0`, resizing the buffer if needed, and aborts (via external helper) if the low bit of `X2` is set."}
{"sub_14000C410": "This function parses a string (likely a punycode-encoded IDN label) starting at `X0`, validating and decoding it, updating a counter at `X0`, and returning a success/failure status in `W0`.\n\nKey observations:\n- Accepts input string pointer in `X0`, with embedded length/position fields (`[X0+8]`, `[X0+16]`, `[X0+20]`, `[X0+28]` — likely a structure tracking buffer, size, offset, and possibly error/state).\n- Checks for `'G'` (0x47) at current position, then `'_'` (0x5F) at next — suggests detecting the `\"_.-\"` or `\"G_\"` prefix (e.g., `\"g_\"` likely part of a punycode marker like `\"xn--\"` or a custom variant; `'G'` may be a typo for `'x'` in disassembly, but `'G'` is explicit).\n- Parses alphanumeric characters and `'_'` to build a numeric value (base-36-like decoding), updating position and checking overflow.\n- On failure paths, invokes error-reporting function `sub_140012B6C` with strings like `\"?`, `\" + : punycode{}\"`, etc.\n- On success: advances input position, decrements recursion depth if needed, and returns `W20` (0 for success, 1 for error).\n- Has recursion-handling logic with a counter at `[X0]`, incrementing before recursive calls to `sub_14000EFD0`.\n\nIn summary:  \n**This is a punycode decoding subroutine that validates and decodes a punycode label (e.g., `\"xn--...\"`), using recursion with depth tracking, returning success (0) or failure (1).**"}
{"sub_1400016AC": "This function appears to be a **debug breakpoint stub** that sets up arguments and calls `sub_140006048` (likely a logging or debug setup function), then calls `sub_1400013BC`, and finally triggers a **BRK #0xF003** debug exception with `W0 = 7`. The use of `BRK #0xF003` (a known ARM debug exception code used by Android/system debuggers like `ptrace` or `gdb`) indicates intentional entry into a debugger trap, possibly for crash reporting or intentional halting in debug builds. The stack initialization and `ADRL` loads suggest preparation for a structured debug context (e.g., capturing frame info or exception state)."}
{"sub_140014EDC": "This function (`sub_140014EDC`) appears to be a **base16 (hex) encoder**, likely converting binary data to uppercase hexadecimal ASCII string representation.\n\n### Key observations:\n- It saves callee-saved registers (`X19`–`X21`, `X29`, `X30`) and allocates stack space.\n- Uses `TBNZ` on bit 0x19 and 0x1A of `W8` (loaded from `[X1]`) — likely flags indicating source format or size.\n- Repeatedly extracts bytes via shifting/masking (`AND X9, #0xF`, `LSR #4`) — classic hex encoding pattern.\n- Loads a static hex digit string `\"0x0123456789ABCDEF\"` (via `ADRL`) and writes nibbles to destination buffer.\n- Calls `sub_140011760` (likely `snprintf`-like or custom hex formatter) with mode flags (`W1=1`, `W3=2`), possibly indicating uppercase hex output.\n- One path calls `sub_1400135B8` (twice — once before `B 0x1508C`, once at end) — likely a lower-level helper; might be the actual encoder or memory ops.\n- Final result returned in `W0` (status code or byte count).\n- Ends by restoring stack and returning.\n\n### Conclusion:\nA **hex-encoding wrapper**, handling branching based on flags (maybe case sensitivity or length), delegating to helper(s) (`sub_140011760`, `sub_1400135B8`) depending on input properties. Standard hex-encode behavior: input buffer → uppercase hex ASCII string (e.g., `\"A1B2C3...\"`)."}
{"sub_140012D1C": "This function appears to be a **recursive JSON/document serializer or pretty-printer**, likely part of a custom JSON library or runtime, operating on a structured object graph passed in `X0` (and possibly `X1`, `X2`, etc., depending on context). Key observations:\n\n- Saves callee-saved registers and allocates stack space (~0x80 bytes), with stack frame pointer initialized.\n- Reads flags from `X0[9]` (bit 0) — likely indicates whether the object is a container (e.g., object/array).\n- Loads a primary structure from `X0` (`X21 = *[X0]`) and inspects flags in `X21[2]` (bit 7) — possibly indicating nested state or formatting options.\n- Conditionally calls function pointers at offsets like `X8[0x18]` (likely vtable or handler table), passing formatted string literals (e.g., `\" { , : { \\n, (( \\n\\n}),] }\"`) and context.\n- Some paths construct a stack buffer (e.g., saving registers and a struct pointer), then call `sub_1400130C8`, which seems to handle low-level serialization (possibly printing or accumulating output).\n- Uses `CINC`/`CSEL` to conditionally choose format strings or values based on prior comparisons (`W8 == 0`, etc.).\n- Final result is written back to `X0[8]` (set to `W24 = 1`) and `X0[9]` (set to `W0`, the return code), then restores registers and returns.\n\n**Summary**:  \nA recursive serializer function that walks a structured object tree (possibly JSON-like), invoking appropriate handlers for elements, applying formatting (e.g., braces, commas, newlines), and returning a status (nonzero on error). Supports nested structures and conditional behavior based on flags in the object header."}
{"sub_14000C744": "This function performs **parsing of a punycode-encoded string**, specifically validating and decoding a sequence expecting the pattern `\"U...K...C...\"` (Unicode escapes like `\\uXXXX`), with recursion and syntax checking.\n\n### Summary:\n- **Input**: Pointer in `X0`, likely to a struct containing:\n  - `X0[0]`: pointer to input buffer\n  - `X0[1]`: buffer length\n  - `X0[2]`: current index (updated during parsing)\n  - `X0[4]` (`+0x28`): error context/reporting callback or flag\n- **Control Flow**:\n  - Skips if no buffer (`X0+8 == 0`) → short-circuit.\n  - Parses ahead, expecting `'U'` → `'K'` → `'C'` sequence (ASCII 0x55, 0x4B, 0x43), likely representing `\\u` and a following character (e.g., `\\uXXXX`).\n  - On mismatch or out-of-bounds → error handling.\n  - On successful match, advances index and calls helper(s) (e.g., `sub_14000F56C`, `sub_14000DAE4`) for further decoding/recursion.\n  - Handles recursion depth and syntax validation, emitting errors like `{invalid syntax}`, `{recursion limit reached}`, etc.\n  - Returns failure (W0=1) on fatal error, success (W0=0) otherwise.\n\n### High-level role:\nA **recursive descent parser** for a specific punycode fragment (possibly part of a Unicode escape handling within a larger lexical analyzer), with strong diagnostic/reporting hooks.\n\n*(Notable: The “U K C” sequence strongly suggests decoding `\\uXXXX` escapes — ‘U’ = `\\u`, ‘K’/‘C’ likely represent continuation markers or internal state flags.)*"}
{"sub_140014054": "This function performs **string matching and substring search with bounds checking**, likely implementing a variant of the **Boyer-Moore or Knuth-Morris-Pratt (KMP) algorithm**, possibly used for **pattern matching within a larger buffer**. It takes:\n\n- `X0`: pointer to data buffer (source),\n- `X1`: pointer to pattern (target),\n- `X2`: length of source,\n- `X3`: length of pattern (if non-zero; otherwise default logic or early exit),\n- `X8`: additional context (stored in `X20`).\n\nKey observations:\n- Saves many callee-saved registers and vector registers → function is non-trivial and may be performance-critical.\n- When `X3 == 0`, jumps to early initialization code that writes a fixed struct to `X20`.\n- Computes candidate match positions using loop structures reminiscent of KMP failure function computation (e.g., updating indices like `X8`, `X10`, `X11`, `X12`, etc., with `CSINC`, `ADD`, `SUB`).\n- Compares bytes via `LDRB` + `CMP`, handles mismatches with index resets or advances.\n- Uses SIMD (`ORR`, `FMOV`, `EXT`) for fast batch comparisons when lengths ≥ 32 bytes (e.g., `0x20`).\n- Writes structured results to memory pointed to by `X20` (first 0x60 bytes) — possibly a `match_result` or context state.\n- On validation or error conditions (e.g., buffer overflows, mismatch during comparison), triggers `sub_140016E68`/`sub_140016C8C` (likely logging/assertion helpers) followed by `BRK #1` — i.e., debug traps.\n\nIn summary: **String matching routine with SIMD acceleration for large inputs, returning match metadata or asserting on invalid cases.**"}
{"sub_14001692C": "This function appears to be a helper that computes and returns a value based on an input pair (likely a pointer and a count/size), potentially related to memory allocation or buffer sizing. Here's a concise summary:\n\n- It takes two arguments: `X0` (likely a pointer to a struct or pair `{count, something}`), and `X1` (an index or offset).\n- Loads a 64-bit value (`X8`) and a 64-bit value (`X2`) from `[X0]` — likely `{count, base}` or similar.\n- Computes `X11 = X1 + 1`, then `X10 = X8 << 1` (i.e., `2 * count`), and sets `X10 = max(2 * count, X1 + 1)`.\n- Calls `sub_14001688C(X0 = &local_buffer, X1 = count, X2 = base, X3 = max(2*count, X1+1))`, probably to allocate/prepare a buffer of size `max(2*count, idx+1)`.\n- After the call, reads a status flag from `[SP+var_28]`. If bit 0 is **unset**, branches to cleanup/return-success path; if set, stores `-1` (i.e., `0xFFFFFFFFFFFFFFFF`) as the second return value and also overwrites `[X19]` (original first input) with two values (`X20`, `X8`) — possibly updating the original struct with computed values.\n- Returns via standard epilogue.\n\n**Likely purpose**: A bounds-checked or growth-preparation helper (e.g., vector/list resizing, string buffer expansion), returning either success with a computed size/index or an error code (e.g., `-1`) on failure, while conditionally updating the input structure.\n\nNote: `sub_14001688C` is the key allocator/helper — but its role is external; this wrapper handles setup, branching, and cleanup."}
{"sub_1400026BC": "This function retrieves a TLS (Thread Local Storage) value via `X18`-based TLS pointer, checks if it's at least 2, and conditionally adjusts it before calling `sub_140002714`:\n\n- If TLS value ≥ 2: subtract 0x10, store it on stack, and call `sub_140002714`.\n- Else (TLS value < 2): pass zero (`XZR`) as the first argument to `sub_140002714`.\n\nE.g., likely part of a TLS stack guard or cookie initialization logic."}
{"sub_140010274": "This function appears to implement **decoding/processing of a variable-length encoded integer or structure**, likely part of a **debugging or formatting subsystem**, given the error-handling (`BRK #1`) and string references to \"a formatting trait implementation return\". It:\n\n1. **Allocates stack space** and saves callee-saved registers.\n2. **Checks** if `X0` (likely an input pointer) or `X1` (maybe size/flag) is zero → early exit paths (`loc_1400103B8`, `loc_140010328`).\n3. **Iteratively processes input bytes** starting at `X0`:\n   - If bit 7 of a byte is clear: add byte value to accumulator `X8`, advance pointer.\n   - If bit 7 is set, interpret higher bits to determine if it's a 16-bit or variable-length encoding, updating pointer and accumulator accordingly.\n   - Accumulator `X8` seems to represent a decoded offset/length.\n4. Validates result (`X8 >= 0x10`), possibly checking for overflow or malformed input.\n5. Calls `_guard_check_icall_nop`, then `sub_14000104C` — likely allocate or prepare a buffer.\n6. Invokes `sub_1400124BC`, passing stack-allocated data and the original input — possibly a formatting or trait registration call.\n7. Stores decoded result (Q0 + X8) to location pointed by `X19` (saved from original `X8` register).\n8. On error paths (e.g., `X8 == 0`, validation failure), triggers `BRK #1` and calls error-reporting routine `sub_140016C3C` with a redacted message.\n\nIn short:  \n**This is a decoder function for a compact (likely variable-length) encoded integer or structure, used during formatting or metadata processing — likely in a structured exception or type trait system — with strict validation and debug assertions.**"}
{"sub_1400165A0": "This function checks a flag (likely a lock or initialization state) at offset `0x48` from the input pointer `X0`. If the flag is zero, it returns immediately. If non-zero, it prepares and passes a stack-allocated structure (containing `X8`, a derived address `X9`, and a pointer to a redacted string) to `sub_140016244`, possibly registering or initializing some debugging/logging resource, then returns. Essentially, it performs a conditional registration/initialization only when the flag is set."}
{"sub_140008738": "This function (`sub_140008738`) appears to be a **path separator validation or normalization routine**, likely processing a string path and modifying an output structure based on the separator type and content.\n\n### High-level summary:\n- It takes three arguments (likely `X0`, `X1`, `X8` per calling convention):  \n  - `X0`: pointer to output structure (or buffer)  \n  - `X1`: some count/offset value  \n  - `X8`: pointer to input path string (or metadata)  \n- Calls `sub_140007628` early — possibly to parse or preprocess the path string.\n- Reads a byte (`W10`) from a local buffer (likely the separator character or type).\n- Based on `W10`, it sets `X0` (the return value) to values like `+2`, `+4`, `+8`, or returns `2`/`6` for special cases — likely representing **sizes or offsets**.\n- Computes `X8 = X1 - X0`, and if unsigned comparison `X1 < X0`, it triggers a crash (`BRK #1`) after redacting info.\n- Otherwise, it writes a small struct to the output buffer pointed to by `X19` (preserved from `X0`). This struct contains:\n  - A boolean (`W8`) indicating whether the character at `[X9]` (computed from input) equals `\\` or `/`.\n  - Two quadwords (Q0, Q1) and other fields at offsets 0x18, 0x28, 0x38, etc.\n- Restores stack and returns.\n\n### Inference:\nThe function likely **analyzes and normalizes Windows-style path separators (`\\` or `/`)** and populates a structured output (e.g., a descriptor or token), returning a status or size. The `BRK` on insufficient space (`X1 < X0`) suggests bounds checking before writing — common in secure path handling.\n\nA probable internal helper in a path-handling library (e.g., for resolving, validating, or converting paths)."}
{"sub_140004764": "This function is a **Windows NT stack unwinding and exception handling helper**, likely implementing part of a **low-level exception handler or debugger/debug support routine**, possibly related to structured exception handling (SEH) or a custom crash/diagnostic logging mechanism.\n\n### Key Observations:\n- Saves many callee-saved registers and allocates a large stack frame (~1232 bytes).\n- Calls `sub_140006A64`, which likely initializes context/capture state.\n- Loads a function pointer from `[X25,#0x28]`, checks if it’s `-1` (`CMN X20,#1`), and branches accordingly — possibly checking for an invalid/unset handler or unwind entry.\n- Performs a series of checks on the top entry of an internal data structure (e.g., based on low 2 bits of an address → alignment/flag check), and calls `sub_140001040` (likely a `OutputDebugStringA`/logging function).\n- Invokes `RtlCaptureContext`, `RtlLookupFunctionEntry`, and `RtlVirtualUnwind` — core Windows unwinding APIs — to walk the call stack.\n- Uses a mutex (`ReleaseMutex`) and likely a critical section or lock mechanism (`sub_140003BD0`, possibly `EnterCriticalSection`-like).\n- Contains a loop to walk multiple unwind entries (counted in `X27`, up to 0x64), comparing function addresses (`X20`, `X22`, `X23`) to detect infinite loops or recursion in the unwind.\n- At the end, may print diagnostic info (e.g., `\"stack backtrace:\\n\"` or `\"note: Some details are omitted...\"`) via a function pointer from a context structure.\n- Returns in `W0` the value of `W19` (likely 0 or 1 indicating success/failure or handled/unhandled).\n\n### Summary:\nThis function is a **stack unwinder/dumper** — possibly used for crash reporting, debugging, or exception processing — that uses Windows RTL APIs to unwind the call stack, record context, and optionally log details. It guards against infinite loops during unwind and synchronizes access via a mutex."}
{"sub_140013F28": "This function performs a **byte-wise string search for a specific byte pattern (likely a terminator or key byte) in a buffer**, returning 1 if found, 0 otherwise, with abortive error handling via `BRK #1`.\n\n### Key Observations:\n- **Signature (likely)**: `int sub(void* buf, size_t len, uint8_t pattern)` — or similar (`W0` = pattern, `X1` = buf, `X2` = len).\n- **Alignment & bounds checks**: Uses `CMP X2, X9` where `X9 = (pattern_len_padded - buf_len)`, implying a check for sufficient buffer size.\n- **Core search logic**:\n  - If `X0 == X2` (end of buffer scanned), jumps to success (`B.EQ loc_140013FA4` → eventually sets `W0 = 1`).\n  - Otherwise, scans backward byte-by-byte (`LDRB`, compare with `W10 = pattern & 0xFF`), using a negative offset counter (`X8 = -X12`).\n- **SIMD-style optimization path** (blocks `0x13fb8`–`0x13ff4`):\n  - Uses broadcast of pattern (`X12 = pattern * 0x101010101010101`), loads 16-byte chunks, XORs, and uses `BICS` with magic constants to check for presence of pattern in chunk (a common SIMD-free vectorized byte-scan trick).\n  - Falls back to byte loop if no match in chunk.\n- **Error paths**:\n  - `loc_140014028` / `loc_140014040`: trigger `sub_140016E68(\"<redacted>\", ...)` (likely logging/assertion) followed by `BRK #1` — hard crash/debug trap.\n  - Called on invalid length or mismatched bounds.\n\n### Summary:\nThe function searches for a given byte (`W0`) in a buffer (`X1`, length `X2`). Returns `1` if found, `0` otherwise, with crash-on-error behavior for boundary violations or unexpected states."}
{"sub_140014940": "This function performs **ASCII/Unicode validation and decoding**, likely part of a **UTF-8/UTF-16 string parsing or validation routine**.\n\n### Key Observations:\n- **Initial check**: Input (`W0`) must be between `0x20` (space) and `0x7F` (DEL), or else it branches into alternative handling (e.g., `loc_140014A54` for larger values).\n- Two main decoding paths based on value range:\n  - **0x20 ≤ input < 0x1000**: Uses table `unk_140019BAD`/`unk_140019BF9`.\n  - **0x1000 ≤ input < 0x20000** (≈80K range): Uses table `unk_140019885`/`unk_1400198E1`.\n- **Table-driven decoding**:\n  - Looks up 2-byte entries (first byte → comparison key, second byte → length/continuation flag).\n  - Iterates to match a sequence of bytes against input, validating against expected byte pattern.\n  - Supports multi-byte sequences (up to 4 bytes) by checking remainder and performing byte-by-byte or block comparisons.\n- **Fallback/error handling**:\n  - If decoding fails (mismatch or bounds violation), branches to `loc_140014BD0`/`loc_140014C28` for special handling (possibly surrogate or escape sequences).\n  - Other error paths (e.g., invalid lengths/indices) trigger `sub_140016E68` with \"<redacted>\" and a `BRK #1`, indicating debug/hardcoded assertion failure.\n\n### Conclusion:\nThe function **validates and decodes a character code point (likely UTF-8/16) from a 16-bit integer input**, checking ASCII ranges first, then falls back to table-driven multi-byte decoding. It appears to be part of a **character encoding/decoding subsystem**, possibly in a Unicode-aware library or runtime. Return value is `0` or `1` (via `AND W0, W8, #1`) — likely indicating success/failure."}
{"sub_14000CD90": "This function parses a UTF-8-encoded string (passed in `X0`) of up to 16 bytes (or fewer, depending on content), converting it to a `u32` via base-16 (hex) decoding, and returns a boolean success indicator in `W0`. It also computes a derived value (possibly checksum or adjusted index) stored in `X1`.\n\n**Key observations:**\n- On entry, it sets up a stack frame and calls `sub_140014054`, likely for string validation or conversion prep (e.g., trimming whitespace, checking format).\n- It loads a pointer to a debug string `\"0[]::::{closureshim:#< as >& mut *const\"`—likely not executed during normal flow.\n- `sub_14000B048` is called, which likely determines string length or validates encoding; the result (`X8`) is checked:\n  - If `X8 == 0`, it returns success (`W0=1`, `X1=0`).\n  - If `X8 == 1`, it continues; else jumps to fail (`X0=0`).\n- The core decoding loop (`loc_14000CE24` onward) processes up to 16 bytes of input, extracting UTF-8 characters and decoding them as hex digits (`'0'..'9'`, `'A'..'F'`, `'a'..'f'`).\n- It builds the result in `X1` via shifting (`X1 = (X1 << 4) | nibble`).\n- If invalid characters or out-of-range digits are found, it triggers a panic-like sequence (calls `sub_140016E48` with `\"<redacted>\"`, then `BRK #1`).\n\n**Summary:**  \nThis function decodes a short UTF-8 string (≤16 bytes) as a hexadecimal literal into a `u32`. It returns `W0 = 1` on success (with decoded value in `X1`), or `W0 = 0` on failure (e.g., too long, invalid chars, or length >16). On invalid input, it aborts execution. The function is likely part of a debug/intermediate representation parsing routine (e.g., Rust `\"0x\"` or `\"0[]\"`-prefixed hex parsing)."}
{"sub_140004AC8": "This function conditionally calls one of two helper functions based on the first byte of the data pointed to by `X0`:\n\n- It loads a pointer from `[X0]`, then reads the byte it points to (`*(*(char**)X0)`).\n- If that byte equals `1`, it calls `sub_140011C78` with arguments suggesting a “Some” value (likely constructing an Option/Some variant), passing `\"Some\"` (length 4) and a pointer to the original string (`X8`).\n- Otherwise, it jumps to `sub_140012400` with `\"NoneSome\"` (also length 4), likely constructing a “None” variant.\n\n**Summary**: This function appears to be a *constructor or parser for an Option type*, interpreting a leading byte tag (`1` → `Some`, else → `None`) and dispatching to appropriate handler functions to build the corresponding variant."}
{"sub_140004CA8": "This function is a **UTF-8 validation and string comparison routine**, likely implementing part of a **string processing or parsing engine**, possibly for handling **multipart data, URLs, or protocol fields**.\n\n### High-Level Summary:\n- Accepts input strings (`X0`: destination buffer? `X1`: source string, `X2`: source length, `X3`: possibly max length or flags).\n- Performs **longest-prefix match** against a repeated byte pattern (via SIMD-optimized `memcmp` and custom byte-wise comparison loops).\n- Validates UTF-8 encoding in trailing segments (multi-byte sequences, check continuation bytes, lengths).\n- Uses SIMD (NEON) for fast comparison of 16-byte chunks against repeated byte patterns.\n- Falls back to scalar loops for mismatch detection and precise position tracking.\n- On validation errors or special control characters, triggers error handling (e.g., `sub_140016C2C`, `sub_140016C8C`, and `BRK #1` – likely debug/assert hooks).\n\n### Key Sections:\n1. **Prefix Check**:\n   - Compares first 5 bytes of the string against a repeated byte (`W8`) using scalar comparisons.\n   - If all match, delegates to `memcmp` for full-length comparison.\n\n2. **SIMD Search Loop**:\n   - Sets up 16-byte repeated masks (from `XMMWORD_140017D50`) for fast 16-byte chunk comparisons.\n   - Uses `CMEQ`, `AND`, `UMAXV`, `ADDV` to find matches and track progress in 16-byte strides.\n\n3. **UTF-8 Validation**:\n   - Parses multi-byte UTF-8 sequences (2–4 bytes), reconstructing code points and checking continuation bytes.\n   - Validates length calculations and writes/moves pointers accordingly.\n\n4. **Error/Redaction Paths**:\n   - On failures or edge cases, prints `<redacted>` and triggers `BRK #1`, suggesting this is a **debug/internal use** function that panics on malformed input.\n\n### Likely Use Case:\n> Parsing HTTP headers, URL path segments, or structured data where a repeated byte prefix (e.g., `'/'`, `' '`, or control chars) must be stripped and the remainder validated as UTF-8 before further processing.\n\nNotable: `sub_140014054` (called early) likely initializes or resets internal state; `sub_140016654` is a SIMD utility for popcnt or mask reduction."}
{"sub_140005C88": "This function appears to be an error handler or cleanup routine that:\n\n1. Checks if `X9` (likely a counter or pointer offset) is zero.  \n2. If non-zero, it loops:  \n   - Loads a 32-bit value (`W2`) from memory at `[X8]`, post-incrementing `X8` by 0x10.  \n   - Decrements `X9` by 0x10.  \n   - If `W2` is zero, jumps back to continue the loop; otherwise, proceeds.  \n3. Then loads an 8-byte value (`X1`) from just before the current `X8` (`[X8, #-8]`), sets `W0 = -0xC` (i.e., `0xFFFFFFF4`), and calls `sub_140005768`.  \n4. If `X9` was zero initially, it sets `X2 = 0`, `W0 = -0xC`, and calls the same `sub_140005768`.  \n\nGiven the consistent use of `W0 = 0xFFFFFFF4` and shared call target, this likely signals a specific error condition (e.g., `-0xC = -12`, possibly indicating a canonical error like `STATUS_INVALID_BUFFER_SIZE` or similar, depending on context). The loop suggests iteration over a list or array of 16-byte entries, possibly discarding or processing entries until a null sentinel is found or count expires.\n\nIn short: **This function processes a 16-byte-aligned buffer/queue (pointed to by `X8`, with count `X9`), reads entries, and upon termination (either by exhaustion or hitting a zero entry), calls `sub_140005768` with an error code in `W0`.**"}
{"sub_1400124BC": "This function parses a custom-encoded string or data blob (likely a protocol or binary format), processing variable-length instructions based on the high bits of each byte. It iterates through an input buffer (`X0` → `X3`), checking flags in the top bits (bits 7–4) to determine decoding behavior:\n\n- Bit 7 set ⇒ special handling (cases for `0x80`, `0xC0`, and other bit patterns),\n- Bit 6–4 used to select operand size/stride (e.g., 1-, 2-, or 4-byte operands),\n- For non-flag bytes, it reads a length byte and advances the pointer accordingly.\n\nIt uses callback/function pointers stored in a struct (offset `+0x18` from `X1`, i.e., `X20`) and dispatches operations via `BLR` based on flags and operand size. Returns `0` on null input or end-of-data, `1` on success/error path (nonzero return from callback).\n\nIn short: **a decoder/parser for a custom instruction format**, interpreting length-prefixed or flag-encoded opcodes with indirect callbacks for processing."}
{"sub_14000E9D0": "This function parses a token (likely a identifier or keyword) from a string buffer pointed to by `X0`, starting at an offset stored in `[X0+0x20]`, up to a length in `[X0+0x10]`. It validates characters (`[0-9a-zA-Z_]`), handles case-insensitive comparison for keywords (e.g., signed/unsigned int types like `i8`, `u32`, etc.), and dispatches parsing behavior accordingly — either calling a type-resolution or error-reporting function.\n\n- On success: returns 1 in `W0`.\n- On failure: returns 0, clears state (`X0+8`, `X0+16`), or raises a breakpoint (`BRK #1`) after reporting an error.\n\nIt behaves like a **tokenizer/parser for a compiler or interpreter**, specifically handling identifier lexing and primitive type recognition."}
{"___scrt_acquire_startup_lock": "This function acquires a startup lock, likely for thread-safe initialization in the CRT runtime. It:\n\n1. Saves callee-saved registers and calls `sub_140015CE8`, which appears to be a TLS or startup lock initialization function.\n2. If that call fails (W0 == 0), it jumps directly to return 0 (success/failure likely based on return value).\n3. Otherwise, it retrieves a lock variable (from `x18+8`, probably TLS or module-specific data), loads an address into `X0`, and calls `sub_1400151E0` — likely an atomic compare-and-swap or spinlock acquisition routine.\n4. If the lock acquisition fails (`X0 != 0`), it loops back to retry; otherwise, it continues to return.\n5. Final return: `0` if lock was successfully acquired (or initialization succeeded), `1` otherwise.\n\nThe function implements a retry loop for acquiring a lock, returning `0` on success and `1` on failure (likely indicating another thread is initializing or lock contention)."}
{"sub_14000865C": "This function performs conditional initialization or validation of a structured object (likely a virtual method table or similar), branching based on the least significant bit of the second argument (`W2`). If bit 0 is set, it jumps to a simple call site; otherwise, it proceeds to check and possibly finalize a pointer stored at offset 8 of the object pointed to by `X0`, involving dereferencing, function-pointer calls, and memory copying via `sub_140001040`. It returns 1 (success) in `W0` at the end. The control flow suggests a guard or setup routine for an object with vtable-like semantics."}
{"sub_140002400": "This function implements a conditional initialization or update of a 16-byte value (likely a 128-bit integer or two 64-bit values), possibly representing a counter or accumulator, based on bit conditions in a field pointed to by `X0+0x18`. Here's a concise summary:\n\n- If `*(X0+0x10) == -1` (CMN X8, #1 → ZF set), skip processing and jump directly to epilogue.\n- Otherwise:\n  - Load a function pointer table or vtable from `*(X0+0x18)`, then a struct from that table.\n  - Load `W2` (likely flags or a method table) and:\n    - **If bit 0 set (TBNZ)**: Call `sub_1400124BC` with a stack-allocated buffer (initialized to zero and `{0,1}`), then proceed to store the result.\n    - **Else if `(W2 >> 1) != 0`**:\n      - Call `sub_140016758` with arguments derived from `X0`, `X8` (shifted), and constants.\n      - Then `memcpy` `X20 + X8` bytes (i.e., sum of two values) from a saved location into a destination.\n      - Jump back to accumulate/continue.\n    - **Else (if `(W2 >> 1) == 0`)**: Set `X9 = 0`, then add `X8` (i.e., `W2 >> 1`) to `X9`, store result.\n- Finally, store a 128-bit value (Q0) and a 64-bit value (X8) from the stack into the destination pointed to by `X0`, then return.\n\n**High-level purpose**:  \nIt appears to initialize or update a 16-byte state field (e.g., a counter, vector, or custom struct) at `[X0]`, conditionally invoking helper functions based on flags in `*(X0+0x18)`, likely part of an arithmetic, cryptographic, or data-structure operation."}
{"sub_14000AF24": "This function sets up a stack frame, loads two 64-bit values from `[x0]` into `x21` and `x20`, then moves `x1` into `x0` and calls `sub_14001171C` with the stack pointer as an additional argument (in `x8`). If `x20` is zero after the call, it branches elsewhere; otherwise, it continues—suggesting early-exit on `x20 == 0`. Likely performs some initialization or setup operation based on a struct or pair of values pointed to by `x0`, possibly validating or preparing data before delegating to `sub_14001171C`."}
{"sub_1400019D8": "This function performs a **magic-number-based validation/checksum** on two values loaded from the stack (likely from a prior call's return), using two different constant seeds depending on a prior condition.\n\n### High-level behavior:\n1. Allocates stack frame and saves callee-saved registers (`X19`, `X20`, `X29`, `X30`).\n2. Loads a function pointer from `[X1+0x18]` into `X20`, then calls it via `BLR X20` (first call).\n3. Immediately after the call, reads two 8-byte values from the stack into `X9`, `X8`.\n4. Compares these two values against a hardcoded 128-bit magic value (`X10:X8 == 0x24C161F26C70C1F1:0x6C70C1F161F224C1?` — exact constants built via MOVK sequence).\n5. If they match:  \n   - Loads `X0, X1 = *[X19]` and jumps to epilogue.  \n6. Else (mismatch in first check):  \n   - Re-calls the function pointer at `[X1+0x18]` (second call).\n   - Reads stack values again.\n   - Compares against a *second* magic constant (`0x690D78A3C8F78FFC:0xA2D776DDF6E94A1F`).\n   - If match: loads `X1, X0 = *[X19]` (note swapped order).\n   - Else: loads address `unk_1400178F0` into `X0` and `0xC` into `W1`.\n7. Epilogue: restores registers, returns.\n\n### Interpretation:\n- The function appears to be a **signature or integrity-check dispatcher**, possibly related to PE/VM/anti-analysis, or a bytecode/virtual machine dispatch where two return values must match expected constants to determine control flow.\n- The repeated call to `X20` (function pointer from `[X1+0x18]`) suggests a *callback* or *vtable method* invoked twice (possibly once to produce a pair of outputs, then again to produce another).\n- The two magic constants strongly resemble **endianness-swapped 128-bit signatures** (e.g., possibly a hash or verifier token).\n- Final path loads `unk_1400178F0` + `0xC`, likely signaling failure (error code or pointer to error object).\n\n**TL;DR**: Validates two 64-bit values returned from one or two calls to a vtable function at `[X1+0x18]` against embedded magic signatures — differing behavior depending on which signature matches or if neither does."}
{"sub_14000629C": "This function is a **path-processing routine** that normalizes or manipulates Windows-style Unicode paths (e.g., handling separators `\\`/`/`, trimming/locating segments, and possibly appending or prepending components), likely part of a Rust `std::path`-like implementation.\n\n### High-Level Summary:\n- It begins by computing the length of the input Unicode string (`lstrlenW`) and checks for overflow (bit 31 set → panic via `BRK #1`).\n- If non-empty, it scans the string for the **last path separator** (`\\` or `/`) using a loop.\n- Once a separator is found, it compares prefixes of the input path against a list of known base paths (loaded via `memcmp`), likely to determine a canonical or relative form.\n- It then manipulates a path array (pointed to by `X19`, which holds the original `self` or input context), updating length and content (e.g., truncating, inserting, or appending segments) — indicated by writes to `[X19]`, `[X19+8]`, `[X19+0x10]`.\n- In key branches, it appends a semicolon (`;`, likely a path separator in some contexts, though unusual on Windows; more typical for list-like paths like `PATH`) — suggesting this may be handling **multiple paths in a list** (e.g., `PATH`-style).\n- A call to `sub_140016820` (likely a panic/unwrap handler) and `sub_140016758` (possibly a vector/array append or bounds-checked write) indicate interaction with a path buffer or vector.\n\n### Calling Convention Clue:\n- `X0` is reused as both input (path string) and output register (often `&mut self` in Rust ABIs).\n- `X3` → input path pointer (as `X19`), `X0` → possibly `&mut Self` or output buffer.\n- Stack frame size `0x70`, save callee-saved registers `X19`–`X28`, consistent with Rust ABI or generic AArch64 call frame.\n\n### Conclusion:\nThe function **normalizes or appends a path segment to a path list**, possibly part of a `PathBuf::push()` or `Path::join()`-like operation, while handling both Windows-style (`\\`/`/`) separators and potentially multiple paths (semicolon-separated). It includes defensive checks (length overflow, empty input) and panics via `unreachable!()`-style traps."}
{"sub_140011C78": "This function performs conditional validation or parsing logic on a structure (pointed to by `X0`), likely related to decoding or verifying some format (e.g., JSON-like, given string literals such as `\"((\\n\\n}),] }\"` and `\"),] }\"`). It:\n\n1. Saves callee-saved registers and sets up a stack frame.\n2. Extracts fields from the input object (`X0`) — notably loading two function pointers (from offsets `8` and `0x18` of the first field, then another from offset `0x18` of the second).\n3. Calls the first function pointer (via `X25`) — probably a method like `BeginObject` or `ParseStart`.\n4. If that returns zero (error), it exits early with `W0 = 1` (likely success flag).\n5. Otherwise, checks bit 7 of byte `[X0+2]` (likely a flags byte) to decide between two parsing paths:\n   - If clear: calls the same function again with `\"((\\n\\n}),] }\"` (possibly object end/deserialize).\n   - If set: calls with `\"(\\n\\n}),] }\"` and does additional stack-based setup before calling another function (possibly `Deserialize` or similar) on `X21` with `X19` as context.\n6. After several conditional branches and deserialization calls, checks `X20` (saved arg) and bit 7 again to possibly invoke a final cleanup/confirmation step (`\",] }\"` or `\"),] }\"` strings).\n7. Returns with result in `W0`.\n\nIn short: **A structured parsing/validation routine (possibly JSON-like), branching based on a flag byte, invoking deserialization callbacks and validation steps, returning success (1) on error or failure, and continuing on success.**"}
{"sub_140010A10": "This function implements a **safe copy-then-advance pointer operation**, likely part of a dynamic string or buffer management system.\n\n### Summary:\n- **Input**: \n  - `X0` points to a struct containing two fields: a base pointer (`[X0]`) and a size/length field (`[X0+0x10]`).\n  - `X1` is a source buffer.\n  - `X2` is the number of bytes to copy.\n- **Behavior**:\n  - It first checks if the requested copy size `X2` exceeds the available buffer space (`size = [X0+0x10] - [X0]`). If so, it branches to `loc_140010A6C`, which appears to reallocate or resize the buffer (via `sub_140016B04`, likely a grow/shrink helper).\n  - If space is sufficient, it copies `X2` bytes from `X1` to the current buffer tail (`[X0] + offset`), updates the base pointer + size (or used length), and returns.\n- **Output**:\n  - Updates the struct at `X0` to reflect the new end position (by storing `base + copied_bytes` in `[X0]`), and returns 0 (`W0 = 0`).\n\n### Key Insight:\nThis is a typical **append** operation in a growable buffer implementation: it ensures capacity, copies data, and advances the \"used\" pointer."}
{"sub_14000CEE0": "This function parses a Base64-like encoded string (with alphabet `0-9a-zA-Z_>`), decoding it into a binary buffer pointed to by `X0`, updating the current position (index) in a context struct at `[X1]`. It processes characters until:\n\n- A non-alphanumeric, non-`_`/`>` character is encountered (early exit),\n- An overflow would occur during accumulation (checked via `UMULH` against `X12 = 0x3E` — the base),\n- Or the end of the encoded data is reached.\n\nAt success, it writes `1` to `[X0]` (likely a success flag) and `0` to `[X0+1]`, returning.\n\nThe decoding logic:\n- Skips leading `_` characters (`0x5F`).\n- Accumulates result in `X10`, base-62-like: `0-9`, `a-z`, `A-Z`, with `_` and `>` as extra symbols.\n- Each digit updates the accumulator as `acc = acc * 62 + digit`.\n- Base is 0x3E (`62`), as `0-9` (10), `a-z` (26), `A-Z` (26) → total 62, with `_` treated as digit 62 (handled in `0xcfac` block via CMN/XZR check).\n- `UMULH` checks for 64-bit overflow on `acc * 62`.\n\nThe function returns after normal termination or error, setting a success marker in the output buffer."}
{"sub_1400018C0": "This function implements a synchronization primitive—likely a **conditional wait or signal** operation on a variable, possibly part of a futex-like mechanism.\n\n### High-level behavior:\n- **Input**: `X0` points to a structure (likely containing a condition variable or wait key).\n- **Check**: First byte of input (`[X0]`) determines whether to proceed with some initialization (`sub_140005548`) or skip it.\n- **Setup**: Allocates stack space and prepares arguments (likely addresses and a mask `0xFF`) for `sub_1400019D8`, then calls `sub_140002E80`—likely a wait/signal API (e.g., `WakeByAddressSingle`/`WaitOnAddress` wrapper).\n- **Branch on result of wait/signal**:\n  - If zero (success), falls through to check `X1`, which appears to be a result code or pointer (maybe a linked wait list node). If non-null, it calls `sub_140001720` and `sub_1400014DC`, possibly to update state.\n  - Compares `W8` with `2` and if equal, calls `WakeByAddressSingle` on `X20` (possibly a wait key), then loops back to `sub_140002E80`.\n- On error paths or cleanup, it initializes a zeroed local buffer and calls `sub_1400014DC` again.\n- Cleanup restores stack and returns.\n\n### Key insight:\n- Use of `DMB ISHLD` before `sub_140002D90` suggests memory ordering for lock-free or synchronization logic.\n- Use of `WakeByAddressSingle` indicates Windows-style address-based synchronization (Windows 10+ `WaitOnAddress` API family).\n- Pattern matches typical condition-variable or event-wait implementation using atomic loads, compare-and-wait, and wake operations.\n\n**Conclusion**: This function is a **wait or signal handler** in a low-level synchronization library (e.g., condition variable wait, spinlock yield, or futex-style wait), likely part of a runtime or threading library on Windows/arm64."}
{"sub_140011760": "This function processes a structured input (likely a parsed instruction or data descriptor) pointed to by `X0`, performing validation, computation, and optional recursive dispatch based on flags and field values.\n\n**High-level behavior:**\n1. **Initial setup**: Saves callee-saved registers and extracts a 32-bit header from `[X0]`. Uses bits of this header (especially bit 0x200000 and bit 0x17) to branch into two major paths:\n   - If bit 0x17 is **clear**: performs a short-circuit path involving length check against `[X0,#4]` and calls `sub_14001266C`.\n   - If bit 0x17 is **set**: handles a more complex case involving potential re-initialization (`sub_14001266C` with patched header) or recursive processing loops.\n\n2. **Core logic**:\n   - Compares a computed offset (`X27`, derived from header fields and `X5`) against a stored length (`[X0,#4]`). If within bounds, calls `sub_14001266C` with arguments drawn from `[X0,#8]`, passing `W23` (a `+` or `-` character, depending on header bit 0x15) and other derived values.\n   - If `W0` from that call is non-zero, error flag (`W19`) is set.\n\n3. **Post-processing branches**:\n   - Bit 0x18 of the header decides further handling:\n     - If **clear**, extracts a 2-bit field (bits 0x1D..0x1E), uses it to set up a loop or recursive call to `sub_14001266C` or a function at `[X25,#0x18]` (using `X28`, likely a count or offset).\n     - If **set**, reinstates part of the header and calls `sub_14001266C` again.\n\n4. **Loops**:\n   - Two main loops iterate (`W20` / `W21` up to a limit `W28` / `W21`), calling function pointers at `[X25,#0x20]` or `[X25,#0x18]`, likely for deserialization or decoding of nested structures.\n   - Bit-twiddling and conditional increments (`CINC`, `CSET`) suggest counting matches or characters (possibly for sign handling or tag matching).\n\n5. **Return**: Returns `W19` (0 or 1), indicating success/failure of all substeps.\n\n**Conclusion**: This is a **parsing/decoding routine**, likely for a custom binary format or instruction set, branching based on header flags to handle different data layouts or encoding schemes, delegating core work to `sub_14001266C` and indirect function pointers."}
{"sub_140002160": "This function performs a bounded memory copy (like `memcpy`) with additional validation and possible reallocation logic, likely part of a dynamic buffer expansion or managed memory system.\n\nKey observations:\n\n- **Prologue**: Saves callee-saved registers (`X19`–`X24`, `X29`, `X30`) on stack.\n- **Early exit**: If `X2 == 0` (`CBZ X2`), jump to exit (`X0 = 0`, restore stack, `RET`).\n- **Index/size computation**: `X22 = X2 << 4` → suggests number of items ×16 (e.g., 16-byte entries), and `X2` compared against `#9` (likely bounds check on index/count).\n- **Core loop (blocks `0x230`–`0x2b0`)**:\n  - Loads base and current size from `[X0]` and `[X0+0x10]`, computes size diff (`X9 = size - base`).\n  - Compares input size (`X8`) to diff; if `X8 > diff`, branch to reallocation logic (`sub_140016758`, likely a grow/resize function).\n  - Otherwise, copies `X21` bytes (`W21` from `[X23-8]`, i.e., possibly element size or segment length) using `memcpy`, updates stored base pointer, decrements counter (`X22 -= 0x10`), advances pointers, loops.\n  - Tail branch checks if remaining length (`W21`) is non-zero; if so, continue copy; else exit.\n\n- **Reallocation path (`0x2cc`)**: Calls `sub_140016758` with hints (`W3 = W4 = 1`) — likely requests growth/alignment — then continues copying.\n\n- **Epilogue**: Clears return value (`X0 = 0`) and restores stack.\n\n**Summary**:  \nThis is a **safe, potentially resizably-buffered copy routine**—likely `memcpy`-like but integrated with a managed heap (e.g., growth on demand). It copies `X21` bytes at a time from source to a memory region starting at `X0`, potentially extending the region if needed, and returns `0`. Parameter roles:  \n- `X0`: pointer to a struct containing `(base_ptr, size)`  \n- `X1`: destination offset/pointer base  \n- `X2`: index/count (multiplied by 16 → byte offset or count of 16-byte units)  \n- `X8`: requested copy size  \n- `X21`: per-chunk size (from `[X23-8]`)  \n\nStandard ABI."}
{"sub_14000D1A4": "This function parses and validates a string (pointed to by `X1`) for use as an identifier, likely in a custom bytecode or language runtime. It enforces strict formatting rules:\n\n- Starts with `'u'` (ascii 0x75) at current offset.\n- Followed by one or more decimal digits (0–9), accumulating into a numeric value in `X1` (using decimal multiplication).\n- Optionally continues with underscores (`_`) after digits.\n- Then expects an ASCII uppercase letter or underscore after the initial numeric part, potentially for validation or dispatch.\n\nThe parsed start/end positions of the identifier are stored back into the struct at `X9` (likely an `identifier_state` or similar), and the numeric value is computed.\n\nIf any validation fails (e.g., non-digit, non-uppercase letter where expected), it triggers redacted error reporting (`sub_140016C2C`) followed by a `BRK #1` (debugger break), indicating a crash-on-error behavior in debug builds.\n\nIn short: **This function validates a string format like `\"u[0-9]+_?[A-Z_]\"`, parses the numeric suffix, and records the consumed range — failing with a debug break on invalid input.**"}
{"sub_14001688C": "This function performs a bounds-checked heap reallocation, likely part of a dynamic array or buffer resizing routine.  \n\n- **Inputs** (per calling convention):  \n  - `X0`: pointer to a struct (to store result and status)  \n  - `X1`: current buffer pointer  \n  - `X2`: old size  \n  - `X3`: requested element count (or size-related value)  \n\n- **Logic**:  \n  - Checks high bits of `X3` for overflow/invalid values (`LSR X3, #60`, compares to 0).  \n  - Computes scaled size (`X3 << 4`, i.e., `X3 * 16`).  \n  - Validates the scaled size against a threshold (`0x7FFFFFFFFFFFFFF9`).  \n  - If `X1 == 0`, calls `HeapReAlloc` (with `HEAP_ZERO_MEMORY`, i.e., `W1 = 0`) on a process heap to allocate/resize.  \n  - If `X1 != 0`, calls `sub_140007AE8` (likely a helper to compute or validate size), and proceeds on success.  \n  - On success: stores new buffer pointer and size (`X0`, `X20`) in the struct at `[X19,#8]`; sets status flag in `[X19]` to 1.  \n  - On failure (null return from `HeapReAlloc` or helper): sets size to 8 bytes and buffer pointer to 0 (or similar fallback), then sets status to 0.\n\n- **Output** (via `X19` struct, which aliases `X0`):  \n  - `[X19]` = 1 on success, 0 on failure  \n  - `[X19,#8]` = {new buffer pointer, size/element count}  \n\nIn short: **Safe heap buffer resizing with overflow checks and structured status reporting.**"}
{"sub_14000166C": "This function checks the least significant bit of the byte pointed to by the first argument (`X0`). If that bit is clear (i.e., the byte is even), it returns normally via `sub_140003218`. If the bit is set (i.e., the byte is odd), it logs a \"<redacted>\" message using `sub_140016E48` and triggers a breakpoint (`BRK #1`).  \n\nIn effect, this is a runtime assertion or check that fails (with a crash/debugger break) when the target byte’s LSB is 1."}
{"sub_140001D2C": "This function appears to be a **dispatch or wrapper routine** that conditionally either:\n\n1. **Directly forwards** its arguments (`X0`, `X1`) to another function (`sub_140012400`) if a reference count (at `*X0+0x10`, interpreted as signed) is **non-zero** (via `CMN X8, #1` = `X8 + 1 == 0` ⇒ `X8 == -1` → branch taken to `loc_140001D48` only if `X8 == -1` — but actually, `CMN` sets Z=1 when X8 == -1, so `B.EQ` branches when X8 == -1; but wait: `CMN X8, #1` sets Z iff X8 == -1, so `B.EQ` jumps when **X8 == -1**, i.e., *reference count is -1* — possibly indicating an invalid/uninitialized or special state), and then handles an alternative code path when that condition holds.\n\nHowever, re-analyzing the branch condition:\n- `LDR X8, [X0,#0x10]`\n- `CMN X8, #1` → sets Z = 1 if X8 == -1\n- `B.EQ loc_140001D48` → taken **if X8 == -1**\n\nSo:\n- If **X8 ≠ -1** (normal case), fall through to `loc_140001D38`, where it calls `sub_140012400(X1, *X0+8)` — suggesting an object method dispatch: `X0` is an object, `X1` is perhaps a selector or function pointer, and `*X0+8` is passed as second arg.\n- If **X8 == -1**, it goes to `loc_140001D48`, where it loads from `[X0+0x18]`, dereferences, loads two words, tests bit 0 of the first word (`TBNZ W2, #0`), and if clear, falls through to call `sub_1400124BC`.\n\nGiven the control flow and usage of `X0`/`X1`, this is likely a **generic method dispatcher**, possibly for a COM-like interface or Objective-C-style message sending, where:\n- `X0` = object/self,\n- `X1` = method selector or function pointer,\n- `+0x10` is a refcount or flags field,\n- A special refcount value `-1` triggers alternate lookup logic (e.g., static or class-level dispatch),\n- Then possibly branches to a fallback or error handler (`sub_1400124BC`) if bit 0 of some descriptor is clear.\n\nMost concise summary:\n> **Method dispatcher** that forwards calls to `sub_140012400` unless refcount at `[X0+0x10]` is -1, in which case it performs an alternate dispatch via a nested structure and calls `sub_1400124BC` conditionally."}
{"sub_140012B80": "This function appears to perform a two-stage validation or parsing operation on data, likely related to file or protocol parsing (e.g., checking magic bytes or headers). It:\n\n1. Sets up a stack frame and saves callee-saved registers.\n2. Loads a function pointer (`X21`) from `[X22+#0x20]`, and calls it with `X19` as the first arg and `W1 = 0x27 (''')` as the second.\n3. If that call returns zero (`TBZ`), it returns `1` (success).\n4. Otherwise, it calls `sub_140011494` (possibly a hex dump or buffer inspection helper), then inspects a byte at `[SP+#...-0x23]`:\n   - If the byte < `0x81`, it falls through to re-call the initial function (`X21`) on `X19`, and if *that* returns non-zero, returns `1`; else continues.\n   - If the byte ≥ `0x81`, it computes a difference `X8 - X9` (bytes at offsets `-0x23` and `-0x24`), then calls another function pointer (`X10` from `[X22+#0x18]`) with computed args.\n   - If *that* returns non-zero, it again jumps to early success (`W0 = #1`). Otherwise falls through.\n\n5. Finally, re-calls the first function (`X21`) and returns its result.\n\n**High-level summary**:  \nThe function performs conditional validation of data pointed to by `X19`, using two dispatchable function pointers from `X22`. It returns `1` on certain success conditions; otherwise, returns the result of the final call to the first dispatcher. Likely part of a binary format parser (e.g., checking a magic byte or tag `0x27`, then possibly decompressing/validating payload based on header bytes)."}
{"sub_14000ADB0": "This function implements a **size-limited formatter adapter**, likely part of a formatting library (e.g., `fmt`), wrapping an underlying formatter and enforcing a maximum output size.\n\n### High-Level Behavior:\n1. **Setup & Entry Check**:\n   - Saves callee-saved registers and allocates stack space.\n   - Loads two fields from `this` (passed in `X0`): likely a pointer to inner formatter (`X19`) and a `size_limit` field (`X1` in second block).\n   - If `size_limit - 1 == 0` (i.e., limit is 1), jumps to a special case handling (`loc_14000AE28`).\n\n2. **Main Formatting Logic**:\n   - Loads a format string ID (`unk_140018958`) and two format spec-related addresses (`unk_1400188AB` or `unk_1400188AD` depending on bit 0x17 of the format flags).\n   - Calls `sub_1400124BC` — likely a helper to create a `size_limited_writer` or similar adapter around the original formatter.\n   - Checks the return status (`W0`) and proceeds conditionally:\n     - If formatting succeeded:\n       - If the output size counter (`W8`, likely from the adapter state) is zero, return early success (`loc_14000AE90` → `loc_14000AEAC`).\n       - Else, if bit 0 is *not* set, proceed to report size limit exceeded (see below).\n     - If formatting failed (nonzero `W0`):\n       - Possibly checks again for zero size (`loc_14000AE70`), then on failure, emits a `\"size limit reached\"` error via `sub_140012400`.\n\n3. **Error Handling**:\n   - If size limit is exceeded (`W0` nonzero after format attempt, or `size_limit == 1` path), logs `\"size limit reached\"` and may call the underlying formatter’s error handler (`loc_14000AEA0` → `sub_140012400`).\n   - In a critical assertion/failure path (`loc_14000AEBc`), triggers a `BRK #1` and attempts to invoke a custom error handler for `\"fmt::Error from SizeLimitedFmtAdapter\"`, likely indicating a logic or invariant violation.\n\n4. **Cleanup**:\n   - Restores registers and returns `X0` — result code: `1` on early success or error branch (`loc_14000AE94`), or whatever `sub_140012400` returns otherwise.\n\n### Summary:\nThis function wraps a formatter to enforce a size limit on its output, delegating to a helper (`sub_1400124BC`) to wrap the underlying formatter in a size-tracking adapter, and handles size-exceeded and error cases. It returns `1` on success/early exit, else propagates underlying formatter results.\n\n**Likely Use Case**: Enables safe formatting into bounded buffers (e.g., for logging or serialization) with graceful truncation or error reporting."}
{"sub_140016820": "This function appears to be a **debug or instrumentation stub** that validates or prepares some input structure before calling into `sub_14001699C` and possibly `sub_140016BA4`, then triggers a breakpoint (`BRK #1`) — strongly suggesting it's an **assertion or guard function** (e.g., for runtime validation or debugging), not production logic.  \n\n### Key observations:\n- It reads two values from `[X0]` (likely a struct or pair of fields).\n- Computes `X9 = X2 << 1` (multiply second field by 2), compares to 4, and conditionally selects `min(X9, 4)` into `X20`.\n- Calls `sub_14001699C` with a stack-allocated buffer (possibly for output/validation).\n- Loads a result from the buffer (`X8 = [SP+...var_28]`), and if it equals 1, jumps to a block that:\n  - Reads two more values from `[SP+...var_20]`.\n  - Calls `sub_140016BA4`.\n  - **Issues `BRK #1`**, indicating intentional halt/debug trap — likely an unhandled assert or unexpected condition.\n\nThe presence of `BRK #1` (a software breakpoint instruction) implies this is part of a debug build or a runtime assertion guard — not intended for normal execution flow."}
{"sub_1400022F4": "This function implements a safe `realloc`-like operation or memory expansion for a structure that contains a pointer and a size (likely a dynamic buffer or string). It reads `[X0]` (data pointer) and `[X0+0x10]` (size), computes the difference (`[X0+0x10] - [X0]`, i.e., current buffer capacity), and checks if the requested size (`X2`) fits.\n\n- If `X2 <= capacity`, and `X2 > 0`, it copies `X2` bytes from `*[X0+8] + capacity_start` (likely current buffer start + old size offset) to `*[X0]`, then updates `[X0]` to point past the new size (`capacity_start + new_size`) and returns.\n- If `X2 > capacity`, it calls `sub_140016758` (a likely memory allocator or buffer expander) to reallocate or grow the buffer, then falls through to the same final logic.\n\nEffectively, it appends or ensures enough space for `X2` bytes at the end of a managed buffer and returns a pointer to that new space (via `X0`). The callee is responsible for initializing the appended bytes."}
{"sub_140001D70": "This function encodes a 32-bit unsigned integer value (`W1`) into a variable-length UTF-8-like byte sequence stored at memory address `[X0] + X20` (where `X0` is the first argument, `X20` is the offset), and updates the stored pointer at `[X0]` to point past the encoded bytes.\n\n### High-level behavior:\n- Checks if the input value `W1` exceeds 0x7F (127); if so, branches to multi-byte encoding logic.\n- For values ≤ 0x7F: writes a single byte directly.\n- For larger values (up to ~0x7FFFFFFF), performs multi-byte UTF-8-style encoding:\n  - Encodes into 2–4 bytes depending on magnitude.\n  - Each continuation byte is masked with `0x80` (top bit set), and the leading byte has appropriate leading 1-bits (`0xC0`, `0xE0`, `0xF0`) with payload bits.\n- Before encoding, it may call `sub_140016758` with arguments (`X0 + 8`, offset `X20`, `1,1,1,1`) — likely a buffer growth or validation routine (e.g., expanding the buffer if needed), inferred from side-effect pattern.\n\n### Structure summary:\n```\nif (W1 <= 0x7F) {\n    *(byte*)[X0 + X20] = W1;\n    len = 1;\n} else if (W1 <= 0x7FF) {\n    // 2-byte UTF-8\n} else if (W1 <= 0xFFFF) {\n    // 3-byte UTF-8\n} else {\n    // 4-byte UTF-8\n}\nwrite encoded bytes to [X0 + X20], advance pointer, return 0\n```\n\nThis is a typical variable-length integer encoding routine, likely for protocol serialization (e.g., protobuf varint or similar)."}
{"sub_140015AF0": "This function checks whether the current process module (obtained via `GetModuleHandleW(NULL)`) is a 64-bit PE executable (PE32+). It validates the DOS header signature (`0x5A4D`), the PE signature (`0x4550`), the Magic field (`0x20B` for PE32+), and confirms that the `NumberOfRVAAndSizes` field is greater than 14 (to ensure `DATA_DIRECTORY[IMAGE_DIRECTORY_ENTRY_SECURITY]` exists — though this is likely a proxy for verifying the presence of a security directory, possibly as part of integrity or signature checking logic). If all checks pass, it returns 1 (nonzero) if the security directory entry is non-null (i.e., `Size != 0`), otherwise 0. If any check fails or `GetModuleHandleW` fails, it returns 0.\n\nIn short: **`Is64BitPEWithSecurityDirectory()`** — returns 1 if the current module is a 64-bit PE with a non-empty security directory (i.e., signed), else 0."}
{"sub_140007BB8": "This function implements a **buffer growth/resizing operation**, likely for a dynamic array or string buffer.\n\n### High-level behavior:\n- Accepts a buffer descriptor (passed in `X0`) and a requested size (`X2`).\n- Reads current buffer state (size, capacity, data pointer) from the descriptor.\n- If current capacity is insufficient, it:\n  - Allocates a new buffer (using `sub_140001040`, likely a heap allocator or `realloc`-like function).\n  - Copies existing data to the new buffer (`memcpy`).\n  - Updates internal fields (size, capacity, pointer).\n- Ensures new buffer size is aligned or padded appropriately (e.g., `& 3`, branching on remainder).\n- Calls optional callback functions stored in the buffer (e.g., destructor/initializer hooks).\n- Returns 1 if reallocation occurred (i.e., capacity grew), else 0.\n\n### Key details:\n- `X0`: pointer to buffer descriptor struct (contains: data ptr, size, capacity, maybe callbacks).\n- The check `CMP X23, X19; CSET X0, CC` sets return value to 1 only if new size is less than requested — suggesting return value indicates *whether reallocation happened* or *request was satisfied* depending on signedness.\n- Complex control flow handles alignment/offsets (`AND X8, #3`, `SUB ..., #2`, range checks `CMP ..., #2`) likely validating pointer alignment or type tags.\n\nIn short: **a standard buffer reallocation helper with callback support and size alignment logic**."}
{"sub_140012FFC": "This function checks a flag (bit 0 of byte at `[X0+9]`). If clear, it returns `1`; if set, it calls a function pointer at `*(*X0 + 0x18)` (after loading context from `[X0+8]`) and returns its result. The return value (0 or 1) is written back to `[X0+9]` before restoring callee-saved registers and returning.\n\nIn short: **It conditionally executes a callback based on bit 0 of a status byte, caches the result there, and returns it.**"}
{"sub_140015E58": "This function appears to be a **TLS (Thread-Local Storage) initialization/registration hook**, likely part of Rust’s runtime TLS teardown logic. It:\n\n1. **Looks up a TLS slot** via `__tls_get_addr`-like mechanism (using `TlsIndex`, thread pointer `X18`, and `lsr#3` indexing).\n2. Checks if the TLS block (at offset `+0x28` in the TLS base) is already initialized (`CBNZ X8, loc_140015EF0`).\n   - If **not initialized**, it:\n     - Writes a sentinel (`0xFFFFFFFFFFFFFFFF`) to mark in-progress initialization.\n     - Calls `sub_1400037F8` (likely `__cxa_thread_atexit` or similar registration/initialization).\n     - Then checks if the TLS block’s self-pointer (at offset `+0x10`) matches the block start — likely validating initialization consistency.\n     - If mismatch, jumps to a standard library destructor (`std::sys::thread_local::native::eager::destroy`).\n     - Else, calls `sub_1400167F8` (likely a destructor registration or finalization routine).\n   - If **already initialized**, it goes to the error path:\n     - Prints `\"fatal runtime error: the System allocat...\"` (truncated).\n     - Triggers a fatal error via `BL sub_140006048` (likely `rust_panic` or `abort`), followed by `BRK #0xF003` (a deliberate crash/fault).\n\n**In short**: This is a TLS registration/initialization guard—ensuring TLS blocks are initialized exactly once; duplicate or inconsistent initialization leads to a fatal crash. Likely called during thread startup or static TLS setup/teardown in a Rust program."}
{"sub_14000314C": "This function appears to be a debug or logging helper that conditionally initializes and calls another function (likely a formatted output or tracing routine), and on failure paths, triggers a breakpoint (`BRK #1`) after setting up debugging context.\n\n**High-level flow:**\n\n1. **Setup & early check**: Saves callee-saved registers, then calls `sub_140002F44` (possibly a standard initialization or string validation routine, with `\"stdout<redacted>\"`). If that call returns zero (bit 0 clear), it proceeds; else skips to epilogue (exit).\n\n2. **Second conditional check**: Loads and loads atomically (`LDAR`) a global dword (`dword_14001D050`). If non-zero, it jumps to `sub_1400031C8`, which calls `sub_1400165A0` (likely a crash/diagnostic initialization) and falls through to block 3.\n\n3. **Core work**: Calls `sub_140003474` (with arguments including saved inputs `X0`, `X1`, and local stack slots). If it returns non-zero (error), control jumps to the failure handler.\n\n4. **Error handler**: Stores debug info (e.g., return address, data pointers, redacted strings) and calls `sub_140016DAC` (likely an exception or assert reporter), then triggers a breakpoint (`BRK #1`) to halt execution—typical in debug builds.\n\n**Conclusion**:  \nA debug/assertion/logging function that logs or forwards arguments to a subsystem, aborts with a breakpoint on error, and ensures initialized state before proceeding."}
{"sub_14000B798": "This function parses a hexadecimal string (possibly prefixed with `0x` or similar) into a numeric value, stored in the structure pointed to by `X0`. It expects the input string starting at `X0+8`, with current position at `X0+0x20` and end position at `X0+0x10`. It updates the position (`X0+0x20`) as it consumes characters, and supports recursive parsing (e.g., for nested expressions), enforcing a recursion limit (at `X0+0x18`).\n\nThe function:\n- Skips if no input (`X0+8 == 0`) → returns 0.\n- Checks if current position ≥ end → exits early.\n- If current char is `_`, treats it as a special case (possibly a placeholder or literal), increments position.\n- Otherwise, validates and converts hex digits (`0-9`, `a-f`, `A-F`), computing the accumulated value in `X10`.\n- Uses `UMULH` to detect overflow during base-16 accumulation.\n- On error or recursion limit exceeded, calls `sub_140012B6C` to report an error and returns failure (0 or 1 via `W8`).\n- On success, stores the parsed value and position back to the struct and returns 1 (via `W8`).\n\nEfficiently uses conditional sets (`CSEL`, `CSET`, TBZ/TBNZ) for branchless conditionals where possible.\n\n**In short**: This is a recursive hexadecimal parser with recursion limit, overflow protection, and error handling — likely part of a custom expression evaluator or virtual machine."}
{"sub_140005AD0": "This function implements a **buffered input reading routine with newline handling**, likely for parsing line-oriented input (e.g., `fgets`-like or stream reader).  \n\n### High-level behavior:\n- Accepts three arguments: `X0` (some buffer/state pointer), `X1` (input string/buffer), `X2` (length/count).\n- Calls `sub_140013F28` to validate or prepare — likely checks if newline (`0xA`) is present in input (return `1` if found).\n- If newline **not found** (`B.NE` → `loc_140005B54`):\n  - Checks for empty buffer or trailing newline at current position.\n  - Appends partial data via `memcpy`, updates buffer state (advances read pointer).\n- If newline **found** (`loc_140005B00` path):\n  - Reads current buffer pointer.\n  - Checks if enough space remains in buffer for new data (`CMP` against buffer end offset).\n  - Copies data up to (and possibly including) the newline via `memcpy`.\n  - Updates the internal read pointer.\n- A fallback path (`loc_140005BB0`) handles allocation or resizing via `sub_140006060`, including validation of return value (e.g., alignment or size checks).\n- Various failure paths (e.g., insufficient space, allocation failure, malformed input) lead to `sub_140016A50`, which likely triggers error handling (possibly logs `<redacted>` and breaks into debugger via `BRK #1`).\n\n### Key components:\n- **State tracking**: Uses `[X21, #8]` as current read offset, `[X21, #0x10]` as buffer start, `[X21, #0x18]` as buffer end → standard buffer structure.\n- **Copy logic**: Uses `memcpy(X0 = buffer + offset, X1 = input, X2 = count)`.\n- **Cleanup**: Restores callee-saved registers and returns `X0 = 0` on success.\n\nIn summary: **a line-buffered input parser with growth and newline-handling support**, likely part of a stream I/O or shell/command-line parsing subsystem."}
{"sub_1400110C0": "This function sets up a stack frame and calls `sub_140011760` with arguments prepared as follows:\n\n- `X0 = X1` (original first arg)\n- `X1 = 1`\n- `X2` points to the string `\"0x0123456789ABCDEF\"` (at offset +0x10 from label `a0123456789abcd`)\n- `X3 = 2`\n- `X4 = SP + 0x18` (computed as `SP + 0x10 + var_8 + X8 + 8`)\n- `X5 = -X8`\n\nThen it restores `X29`, `X30`, cleans up the stack (`SP += 0x20`), and returns.\n\nGiven the context (stack pointer manipulation, `ADRL`, string pointer, small integer args), this is likely a helper involved in formatting or decoding something (e.g., converting bytes to hex string), possibly within a larger cryptographic or encoding routine. The `NEG X5, X8` suggests `X8` holds a signed length or offset, used as `-len` or similar — common in loop counters.\n\nMinimal and focused:  \n**Allocates stack space, prepares arguments for `sub_140011760`, then returns.**"}
{"sub_140015B88": "This function performs validation of a structure (likely a PE or COFF header or similar), checking three fields for expected values:\n\n1. Reads a 32-bit value from `[X0]` (offset 0) and verifies it equals `0xE06D7363`.\n2. Reads a 32-bit value from `[X0 + 0x18]` and checks it equals `4`.\n3. Reads a 32-bit value from `[X0 + 0x20]`, subtracts `0x19930520`, and checks the result is ≤ `2`. Then compares the value against `0x1994000` using unsigned comparison (HI condition), requiring exact match if previous check passed.\n\nIf **all checks pass**, execution falls through to `loc_140015BEC` (not shown), presumably returning success; otherwise, it returns `0` (failure). The PAC/AUT instructions indicate pointer authentication is used for control-flow integrity.\n\n**Summary**: Validates a data structure (e.g., PE loader structure) by checking magic number, version, and subsystem fields—likely part of a PE/COFF loader or parser."}
{"sub_140007628": "This function analyzes a buffer (pointed to by `X0`, length in `X1`) and classifies whether it represents a **Windows-style path** or a **network path** (UNC), returning a status via a pointer in `X8`. It normalizes backslashes (`\\`) to forward slashes (`/`) and vice versa as needed, and checks for drive letters (`[A-Z]:`) or UNC prefixes (`\\\\?\\` or `\\\\`). The function outputs one of three patterns:\n\n- `0xFF` → unrecognized/not a path\n- `0x00, X9, X10` → simple local path (e.g., `C:\\`, `C:`, or relative)\n- `0x03, X9, X10` → network path (UNC) or relative with `..\\`\n- `0x00, ...` with additional info for special UNC (`\\\\?\\`)\n\nIt is effectively a **path prefix classifier**, likely used in path canonicalization or normalization logic."}
{"sub_14000BBF4": "This function processes a structure (likely a string or buffer descriptor) passed in `X0`, which contains pointers to a buffer (`[X0+8]`), a current index/offset (`[X0+0x20]`), and optionally a length (`[X0+0x10]`) and another pointer (`[X0+0x28]`).  \n\n- It iterates over the buffer (pointed to by `X0+8`) starting at index `X0+0x20`.  \n- For each character, it checks if it equals `'E'` (0x45).  \n  - If `'E'` is found, it updates the stored offset to `index+1` (`[X21,#0x20] = X9+1`) and exits early via `loc_14000BC80`, returning `(0, index+1)` in `(X0, X1)`.  \n- Otherwise, it repeatedly calls `sub_140012B6C` and `sub_14000E044` (likely validation or processing functions), incrementing the counter (`X19`) and buffer index on each loop iteration (`X9` from `[X21,#0x10]`, then updated via `[X21,#0x20]`).  \n- If no `'E'` is found and the loop finishes (e.g., buffer length exceeded or null terminator), it exits with `(0, 0)` in `(X0, X1)`.\n\n**Summary**: Scans a buffer (likely UTF-8 string) starting at a stored index, looking for `'E'`. If found, updates and returns the next index; otherwise, performs some validation/processing loop and returns zeroed outputs."}
{"sub_140001EB4": "This function performs a bounds-checked copy into a buffer described by a struct pointed to by `X0`. Specifically:\n\n- `X0` points to a struct containing two fields: a base pointer at offset 0 (`X21`) and a size/capacity at offset 8 (`X8`).  \n- The function checks if the requested copy size (`X2`) exceeds the available space (`X8 - X21`). If it does, it jumps to handle overflow (likely resizing the buffer via `sub_140016758`, which appears to reallocate/move data).\n- If within bounds, it copies `X2` bytes from `X1` into the buffer starting at `X21`, using `memcpy`.\n- On success (either path), it updates the base pointer in the struct (if resized) and returns 0 (`W0 = 0`).\n\nIn short: **This is a safe buffer append or copy function with potential reallocation (e.g., for a dynamic string or buffer), equivalent to something like `memcpy_to_resizable_buffer(dst, src, len)` with bounds checking and auto-resize.**"}
{"sub_140005768": "This function implements UTF-8 validation and console-mode handling for a Windows CLI application—likely part of Rust’s standard library (`std::io::stdout()` or similar), given the call to `_ZN4core3str8converts9from_utf8...` (Rust’s `from_utf8` implementation).  \n\n### Summary:\n- **Entrypoint behavior**:  \n  - If `X2` (likely `stdin` handle or buffer length) is zero, returns success (`X0 = 1`).  \n  - Otherwise, retrieves the stdout handle via `GetStdHandle(STD_OUTPUT_HANDLE)`; if invalid, sets error code and returns `1` (error).  \n  - If stdout handle is valid, checks for `GetConsoleMode` failure; if failed, calls a cleanup/destructor (`sub_1400061A8`) and returns.  \n\n- **Main logic (console mode detected)**:  \n  - Validates the current code page (`GetConsoleOutputCP`)—if not `0xFDE9` (UTF-8), proceeds to UTF-8 decoding/analysis.  \n  - Parses input (`X22` likely points to a byte buffer; `X20` to a pointer into it):  \n    - Checks for incomplete/invalid UTF-8 sequences.  \n    - Attempts `from_utf8` decoding on the input range.  \n    - On decode success/failure, invokes `sub_140005564` (likely a console-specific writer or encoder).  \n    - On various error conditions (e.g., malformed input, internal invariants violated), triggers a debug breakpoint (`BRK #1`) after emitting an error message (via `sub_140016...`).  \n\n- **Return values**:  \n  - Returns `1` (success) for valid inputs or early exits.  \n  - On severe internal errors, returns `0xFFFFFFF4` (=-12, possibly `ERROR_INVALID_FUNCTION` or similar) after a panic-like `BRK`.  \n\nIn short: **This is a Windows-specific UTF-8 console output handler that validates input encoding, attempts UTF-8 processing, and panics on assertion failures.**"}
{"sub_14000B5BC": "This function parses a Base64-encoded string with extended alphabet support (including `_` and `-`替代 `+` and `/`) and converts it into binary data stored in a struct pointed to by `X0`. It handles recursion limits and invalid syntax, returning `0` on success or `1` on error.\n\nKey observations:\n- Uses a `struct` at `X0` with fields:\n  - `X0+8` → current pointer (updated as it consumes input),\n  - `X0+16` → start of buffer / some limit,\n  - `X0+20` → current offset/index in input,\n  - `X0+24` / `X0+28` → likely end pointer and callback/parameter for error handling.\n- Parses characters: `_` (treated specially as `0x3F`), digits `0-9`, letters `a-zA-Z`, and `>`.\n- Accumulates 6-bit groups into 24-bit chunks, outputting 3 bytes at a time using `UMULH` + shifts to avoid overflow in multiply.\n- Supports nested/recursive parsing (via a callback `sub_14000D3F8`) when certain conditions are met — probably for nested structures or padding-free Base64 variants.\n- Enforces recursion depth limit (0x1F4).\n- Outputs error strings via `sub_140012B6C` when encountering syntax errors or recursion limit exceeded.\n\nIn short: **This is a Base64 decoder with custom alphabet (likely \"Base64URL\" or similar), recursion support, and error handling.**"}
{"sub_14000F78C": "This function performs validation and parsing of a format specifier string (likely for a `fmt::format`-style library), starting at address `X0`, with length `X1`. It checks for specific prefixes (`\"R\"`, `\"R_\"`, `\"__\"`, `\"R__\"`) and validates subsequent characters as uppercase ASCII letters (`A-Z`), interpreting them as indices or selectors. On success, it stores computed offsets or indices; on failure or invalid input, it calls error-handling routines (likely logging/redacted assertions) and returns with a status flag (via `X8+8`). The function includes multiple checks branching on input length and content, followed by a complex post-processing path that appears to interact with a format context or buffer (via stack and `sub_14000D3F8` calls), possibly for range or bounds checking or for constructing a format argument."}
{"sub_14000237C": "This function implements a safe memory copy or zero-initialization for a struct field (likely a pointer or size), bound-checked against a stored range.\n\n- Takes `X0` (struct pointer), `X1` (source buffer), `X2` (requested copy size).\n- Loads base (`X21`) and limit (`X8 = *X0 + 0x10`) from the struct.\n- Computes available size: `X8 = limit - base`.\n- If `X2 > available_size`, branches to `sub_140016758`, likely a bounds-check failure handler or sanitizer (passing base, source, size, and flags `1,1`).\n- Otherwise:\n  - If `X20` (requested size) is zero, skips copy.\n  - Else calls `memcpy(dst = base + X21, src = X1, size = X2)`.\n- Updates the stored base pointer in the struct to `base + copied_size`.\n- Epilogue restores callee-saved registers and returns.\n\n**Summary**: Safely copies up to `X2` bytes into a managed buffer (starting at `*(X0+8)`), advancing the buffer pointer; aborts via `sub_140016758` if size exceeds available range."}
{"sub_14001240C": "This function appears to be a utility that conditionally initializes or validates a context (stored in `X19`/`X1`), then calls `sub_140011760` — likely a hex-printing or buffer formatting routine — passing the stack pointer as a destination buffer and the string `\"0x0123456789ABCDEF\"` as a reference (possibly for hex digit lookup). The branch on bit 0x17 of the value at `[X1]` skips this work if the flag is set, falling through to a fast path (at `0x12480`) that always performs the call. The function preserves callee-saved registers (`X19`, `X20`, `X29`, `X30`) and restores them before returning. In short: **it performs early-exit validation and then formats or initializes a context using a known hex string, likely for debug output or protocol consistency.**"}
{"sub_140006FC0": "This function implements a retry mechanism for `WaitOnAddress`, with validation and overflow checks on its arguments. Specifically:\n\n- It validates the second argument (`W2`) by checking that it is not `-1` (CMN W2, #1 → BEQ → fail).\n- It performs a multiplication-based overflow check on the third argument (`X3`) using a magic constant multiplier (`0x3E8`) and subsequent correction steps (a common pattern for division/magnitude validation).\n- If any validation fails or overflow occurs, it returns `0xFFFFFFFF` (−1) as the result.\n- Otherwise, it proceeds to call `WaitOnAddress` with the validated arguments (passing a pointer to the stack-local variable as the address, value size 4 bytes).\n- After `WaitOnAddress`, if the return value is `1`, it calls `GetLastError` and returns `0` if the error code is **not** `0x5B4` (0x5B4 = `ERROR_TIMEOUT`); otherwise, it returns `1`.\n- The function uses standard prologue/epilogue and returns in `X0`.\n\n**High-level summary**:  \nThis is a wrapper around `WaitOnAddress` that validates its arguments for overflow/special cases, returns `−1` on failure, or performs a timed wait, returning `0` on timeout or `1` on signal — with special handling for `ERROR_TIMEOUT`."}
{"sub_140001000": "This function is a thin wrapper calling `sub_14000176C`, passing:\n- `X0` = address of local stack slot (`SP+0x8`)\n- `X1` = original `X1` (likely a pointer, unchanged)\n- `X2` = sign-extended `W0` (likely an `int` or `size_t` input)\n- `W4` = 0 (likely a flag/length)\n- It also saves/restores frame pointer (`X29`) and link register (`X30`), and stores a code address (`windows::main__`) into the local stack.\n\nMost likely, it initializes a context or invokes a Windows-style entrypoint helper, preparing arguments for `sub_14000176C`, which likely handles actual Windows API interaction or CRT startup logic."}
{"sub_1400037F8": "This function implements a **thread-local storage (TLS) initialization and cleanup routine**, likely for a runtime or library that uses Windows-style FLS (Flexible Local Storage) via `FlsAlloc`, `FlsSetValue`, and `FlsFree`.\n\n### High-level summary:\n- Allocates or initializes a TLS slot using `FlsAlloc` with a custom cleanup callback (`sub_140003994`).\n- Stores the allocated index in a global variable (`dword_14001D058`) via atomic CAS loop.\n- Registers the cleanup callback using `_atexit` (or equivalent) to ensure FLS cleanup at process exit.\n- Decrement a global counter (atomic RMW) on thread teardown.\n- On error or early exit paths (e.g., failure to allocate FLS index or set value), triggers a `BRK` debug trap with specific diagnostic values (e.g., `#0xF003`), likely indicating fatal initialization/teardown errors.\n\n### Key actions:\n1. Checks and sets a TLS flag (`TlsIndex` + 0x20) atomically (with `TBNZ`/`STRB`).\n2. Ensures singleton-like TLS setup with atomic compare-and-swap on `dword_14001D058`.\n3. Allocates FLS index; if zero (invalid), aborts (`BRK`).\n4. Stores the index; on failure, frees FLS index and aborts.\n5. Registers cleanup callback for process exit.\n6. Decrements a shared TLS counter on thread exit.\n7. Sets TLS value for current thread to 1 via `FlsSetValue`.\n\nThe function appears to be a **TLS initialization/teardown stub**, possibly part of CRT or runtime startup/shutdown logic, coordinating FLS-based TLS with `_atexit` for global cleanup."}
{"sub_140016758": "This function appears to be a **debug or validation routine** that checks memory contents and triggers breakpoints (`BRK #1`) on failure conditions.\n\n### High-level behavior:\n- Allocates a small stack frame and saves callee-saved registers (`X19`, `X20`, `X29`, `X30`).\n- If `X3 == 0`, it immediately calls `sub_140016BA4` (likely a logging/debug helper), triggers `BRK #1`, and returns — suggesting early failure.\n- Computes `X8 = X2 + X1`; if this overflows (carry set), it branches back to the early failure path (`BRK #1`).\n- Loads two values from `[X0]` into `X1, X2`.\n- Performs a series of comparisons and conditional selections to compute a value `X19` based on `X8`, `X9`, and `X3`.\n- Calls `sub_14001699C`, likely to validate or transform data.\n- Loads a result from stack into `X8`; if `X8 == 1`, it proceeds to call `sub_140016BA4` and triggers `BRK #1` — again indicating failure/debug assertion.\n\n### Summary:\nThis is a **debug assertion function** — possibly part of a bounds or validity check — that calls a helper on error conditions (via `BRK #1`) and does not return normally on failure. It’s not production code; it's likely instrumented for debugging or runtime verification."}
{"sub_1400151E0": "This function implements a **compare-and-exchange atomic operation**, specifically an `atomic_compare_exchange_strong` for a 64-bit value, likely `std::atomic<T>::compare_exchange_strong`.\n\n### Summary:\n- **Inputs** (per AArch64 ABI):\n  - `X0`: pointer to atomic variable  \n  - `X1`: desired new value  \n  - `X2`: expected old value  \n  - `X15`: return address (function tail-calls to itself or reenters after setup)\n- **Core logic**:\n  1. Checks current exception level (`CurrentEL`) — if not in EL1 (i.e., hypervisor or kernel), it instead reads a hardware feature flag from a BIOS/mem-mapped location (`0x7FFE0000 + 0x296`) to determine lock-free atomic support.\n  2. If lock-free atomics are supported (e.g., via `ID_AA64ISAR0_EL1`), sets a flag at `dword_14001D070`.\n  3. Loops using `LDAXR`/`STLXR` (load-exclusive/store-exclusive with acquire/release semantics):\n     - Compares memory value (`[X0]`) with expected value (`X2`)\n     - If equal: attempts to store new value (`X1`) and exits on success\n     - If not equal or store fails: reloads actual value and returns it in `X0`\n- Returns `true` implicitly (via success path), or the **actual current value** in `X0` on failure — consistent with LLVM/MSVC-style `compare_exchange_strong` semantics where the out-parameter (typically `X3`/`X0` reused) holds the loaded value.\n\nThe recursion (`B loc_1400152B0`) handles fallback cases (e.g., non-lock-free atoms or incorrect EL), reinitializing support flags."}
{"sub_140016BF4": "This function sets up a stack frame, saves arguments `X1` and `X2` to the stack, prepares arguments for a call to `sub_140016CC8` (likely a structured exception handling or unwind helper), then triggers a debug break (`BRK #1`). It falls through to `sub_140016C2C`, suggesting it's part of an exception handling prologue or debugger trap—*not* standard function logic."}
{"sub_1400126E8": "This function is a **hexadecimal encoder** (likely for bytes to ASCII hex string conversion), likely named something like `bytes_to_hex` or `bin_to_hex`.\n\n### Summary of behavior:\n- Takes two arguments: `X0` (output buffer pointer) and `X1` (input data + metadata).\n- Loads a `W8` from `[X1]`, which appears to be a **count or size field** (used in a loop).\n- Checks bits 0x19 and 0x1A of `W8` to decide between two encoding paths:\n  - If **bit 0x19 is set**, it branches to a fast path (possibly a helper call at `sub_1400135B8`, possibly an optimized encode).\n  - Otherwise, if **bit 0x1A is set**, it enters a longer loop that appears to unroll or restructure data before encoding.\n- For the main encoding path (bit 0x19 & 0x1A cleared or after preprocessing), it falls through to a loop that:\n  - Extracts 4-bit nibbles from `[X11+X12]`, presumably a lookup table or input data.\n  - Stores hex nibbles at `+0xF` offset relative to `X10+X8`.\n  - Decrements `X8` and loops until zero.\n- After preprocessing or if neither bit is set, it calls `sub_140011760`, passing:\n  - `X0` = buffer,\n  - `X1` = 1 (likely flags or length indicator),\n  - `X2` = `\"0x0123456789ABCDEF\"` (hex digit string, possibly misaligned by +0x10),\n  - `X3` = 2,\n  - `X4` = end of buffer area,\n  - `X5` = `-count` (negative length).\n- The helper `sub_140011760` is likely a **hex-dumping routine**, possibly writing `\"0x\"` prefix and hex bytes to the buffer.\n\n### Likely prototype:\n```c\nvoid bytes_to_hex(char *out, const void *data, size_t len);\n```\nOr more precisely:\n```c\nvoid bytes_to_hex(char *out, struct { uint32_t flags; const void *ptr; } *input);\n```\nWhere `input->flags & (1<<25)` and `input->flags & (1<<26)` control fast/optimized code paths.\n\nThe function uses `0x10` stack frame and follows standard AAPCS64."}
{"sub_1400164A8": "This function implements a spin-wait loop with exponential backoff and optional Windows `WaitOnAddress` synchronization, likely used for waiting on a shared state variable (at address `X0`). \n\n### High-level behavior:\n- Reads a byte at `[X0]` (the wait condition variable).\n- If the value is `1`, enters a tight loop with:\n  - A retry count initialized to `-0x63` (`0xFFFFFF9D`).\n  - At each iteration: `ISB`, reloads the condition byte, decrements retry count.\n  - If retry count reaches zero, jumps to `WaitOnAddress` path.\n- If the value is `0` at the top-level check, jumps to atomic store path (`STXRB`) to set it to `1` (acquire & set), then returns.\n- If `WaitOnAddress` is called:\n  - Called with timeout `0xFFFFFFFF`, flag `2`, and a local variable (`&var_21 = 2`) as address.\n  - If it returns failure (`W0 == 1`), calls `GetLastError`.\n  - After return, rechecks `[X0] == 1`; if still `1`, loops back to spin-wait logic.\n- If the retry loop exits without `WaitOnAddress` success, falls through to cleanup and return.\n\n### Summary:\nThis is a **hybrid spin-wait/sleep wait primitive**, likely implementing a lock acquisition or event wait with adaptive backoff: spins for a bit, then calls `WaitOnAddress` to sleep until signaled, then re-checks and may spin again if still not satisfied. Common in low-level synchronization (e.g., mutexes, barriers, or condition variables)."}
{"sub_140004B2C": "This function initializes and passes a structure to `sub_1400124BC`. Specifically:\n\n- Allocates a 0x50-byte stack frame.\n- Loads a pointer from `[X0]` into `X10`.\n- Prepares a 32-byte struct on the stack:\n  - `var_30`: points to stack address `SP` (captured as `X8`)\n  - `var_38`: address of `loc_1400013A8` (via `ADRL X9`)\n  - `var_40`: first two qwords from `[X10]`\n  - `var_50`: `X11` (address of `sub_140013BA0`) and `X8+0x4` (i.e., `X10+0x14`)\n- Passes `&struct` in `X2`, along with `X0,X1` loaded from `[X1,#8]`, to `sub_1400124BC`.\n- Clears up stack and returns.\n\nLikely a constructor or setup routine, passing a vtable-like pointer (`sub_140013BA0`) and data to a constructor function."}
{"sub_140016CC8": "This function implements a **jump table–based dispatcher** that indexes into two parallel lookup tables (`unk_14001924C` and `unk_140019258`) using the lower 8 bits of its first argument (`X0 & 0xFF`). It then constructs a pair of pointers (code and data or function/table addresses) on the stack based on the selected entry and the presence of a non-zero 5th argument (`X5`). Finally, it calls `sub_140016DAC`, passing a pointer to this constructed structure and `X7`.\n\n- **Dispatch mechanism**: Uses `X0 & 0xFF` to index into two tables — one for signed offsets to code (via `LDRSW`) and one for absolute addresses (via `LDR`) — forming a structured entry.\n- **Conditional handling**: If `X5 == 0`, one setup path is taken; otherwise, another (with more parameters on stack).\n- **Error/debug trap**: Ends with `BRK #1`, indicating this function is either incomplete, a stub, or used in a debug/development build.\n\nThe function signature likely matches:\n```c\nvoid dispatch_entry(uint8_t idx, void* a, void* b, void* c, void* d, void* e, void* f, void* g);\n```\nwhere `idx = X0 & 0xFF`, and `a..g` are passed as `X1..X7`."}
{"sub_140002608": "This function performs a conditional copy operation on data located at an address passed in `X0`, using values from a 16-byte structure pointed to by `X0` (interpreted as a base + count), along with parameters in `X1` (source pointer) and `X2` (count). Specifically:\n\n- It loads two 64-bit values from `[X0]` (base) and `[X0+8]` (count), computing a difference (`count = count - base`) and shifting `X2` right by 1 (i.e., `X2 >> 1`).\n- If `(X2 >> 1) > (count - base)`, or if `(X2 >> 1) == 0`, it branches to `sub_140016758`, likely a bounds/permission check or allocation routine — possibly verifying region overlap or preparing a buffer — then falls through to `memcpy`.\n- Otherwise, it performs `memcpy(X0+8 + base, X1, X2 >> 1)` and stores the new end pointer back to `[X0+8]`.\n- If the bit 0 of `X2` is set, it bypasses this logic entirely and calls `sub_1400124BC` (possibly handling unaligned or special case).\n\nIn summary:  \n**It conditionally copies up to half the word count (`X2 >> 1`) bytes from `X1` into a region starting at `*(X0+8) + *(X0)`, updating the end-of-region pointer, unless a bounds check fails or `X2` is odd, in which case it delegates elsewhere.**"}
{"sub_140015C18": "This function performs a **call-through loop over a table of function pointers**, potentially used for **registering or invoking a list of callbacks or initialization functions**.\n\n### Key observations:\n- Sets up a loop over a range of memory (from `unk_14001A870+8` to `unk_14001A878`).\n- Each iteration loads a function pointer (`LDR X8, [X19],#8`), checks if it's zero (`CBZ`), and if not:\n  - Calls `_guard_check_icall_fptr` (Microsoft CFG check).\n  - Indirectly calls the loaded function pointer via `BLR X15`.\n- Loop continues while `X19 < X20` (i.e., within bounds of the table).\n- stack frame and PAC（Pointer Authentication）support (`PACIBSP`/`AUTIBSP`) indicate security hardening.\n\n### Likely purpose:\nA **dispatch loop over a table of function pointers**, possibly:\n- **Module initialization/cleanup callbacks**\n- **Event handlers**\n- **vtable or interface dispatch**\n\nCFI-compatible (uses CFG) and PAC-safe — typical of modern Windows binaries compiled with security features."}
{"sub_140005564": "This function implements a UTF-8 to UTF-16 conversion and console write operation with validation logic. It:\n\n- Accepts two inputs: likely a UTF-8 string (`X1`) and a console handle (`X0`).\n- Validates the UTF-8 string length (first 12-bit check).\n- Performs `MultiByteToWideChar(CP_UTF8, ...)` to convert the UTF-8 input to UTF-16.\n- Writes the resulting UTF-16 string to the console via `WriteConsoleW`.\n- Validates the number of wide characters written.\n- If the write succeeds and length matches, returns 0 (success).\n- On failure, sets error code (e.g., via `GetLastError`) and prepares error values in `X19` for cleanup/return.\n- Contains fallback/error-handling blocks that branch to a redacted debug/error logger (`sub_140016DAC`, `sub_140016C8C`, `sub_140016E68`) and ultimately trigger a `BRK #1` breakpoint.\n\nAlso includes logic to handle surrogate pairs (CP 0x312 check with offset 0x2312), indicating robust UTF-16 validity checking.\n\n**Short summary**: A UTF-8 → UTF-16 console output function with strict error handling and debugging asserts."}
{"sub_140003994": "This function implements a thread-safe atomic decrement-and-test operation (likely `InterlockedDecrement` or similar) on a 64-bit counter stored in a TLS-related structure, with additional cleanup and error-handling logic.\n\n**High-level behavior:**\n1. Checks if the current thread is a fiber (`IsThreadAFiber`); if yes, jumps to a known exit (`loc_140003AAC`).\n2. Computes the address of a TLS-based control structure (using `TlsIndex`, indexed from `X18+0x58`).\n3. Loads the structure’s `+0x28` field (a pointer to a struct containing the counter at offset `+0x8`).\n4. If the counter pointer is null → sets `X19 = -1`, then later checks `X8` (loaded from `counter+8`) and stores `-1` there if it was zero (likely a failed CAS or placeholder).\n5. Nonzero counter path: decrements the counter (`X8--`), computes a function pointer at `counter+0x18`, calls it, then loops back if the counter is still nonzero.\n6. If counter becomes zero:\n   - Calls `HeapFree` on an allocated buffer (likely the original counter object).\n   - Resets the structure to a known state (store `0, 8` at `+0x0`/`+0x8`, and `0, 8` at `+0x10`/`+0x18` — possibly a free-list node or sentinel).\n7. Conditionally checks a 4th field (value at `counter+0x18`) against 3; if less, jumps to exit.\n8. Finally, if not jumping, performs a `DMB ISHLD` memory barrier and calls `sub_140002DFC` (likely an atomic lock acquisition or wait primitive).\n9. Exits cleanly after restoring callee-saved registers.\n\n**Observations:**\n- The `ADRL X0, \"<redacted>\"; BRK #1` block suggests an intentional crash or instrumentation point, possibly for debugging failed assumptions (e.g., unexpected null pointer, invalid state).\n- TLS usage indicates thread-local storage of state, likely part of a lock-free data structure (e.g., waitable object, semaphore, or reference-counted resource).\n- `sub_140002DFC` with `DMB ISHLD` is likely a low-level atomic wait/compare-exchange helper.\n\n**Summary:**  \n**Atomic counter decrement with cleanup and optional wait/lock; asserts validity via crash on error.**"}
{"sub_140007070": "This function performs **safe buffer advancement with length validation and redirection logic**, likely implementing part of a string/buffer parsing or decoding routine (possibly for a custom format or protocol). Key behaviors:\n\n- **Initial check**: If the buffer pointer at `[X0+8]` is null, return early (`loc_14000720C`).\n- **Setup**: Loads a buffer pointer (`X0+8`), a metadata offset (`X0+16`), and compares/computes remaining buffer length (`X1 - X20`).\n- **Validation & dispatch**: Calls `sub_140005768` (likely a parser or decoder function). Based on its return:\n  - Checks alignment (`&3`), possibly for 16-byte or 64-bit alignment.\n  - Branches on alignment and content (e.g., `#0x23` = `'#'`) to handle special markers or instructions.\n  - Dereferences function pointers and calls them (via `BLR X8`) — suggesting a jump table or vtable-based dispatch.\n  - Uses `memmove` to shift buffer contents after consumption.\n- **Error handling**: On invalid lengths (e.g., `X1 < X20` or unexpected data), it calls `sub_140016E68` (logging/redaction helper) and triggers `BRK #1` (debugger breakpoint), indicating assertions or sanity checks.\n\n**Likely purpose**: A core parsing loop for a structured binary or text format (e.g., a VM interpreter, scripting engine, or compression codec), where:\n- Buffer state is tracked in the struct pointed to by `X0`.\n- The function advances the read pointer (`[X0+8]`) by a computed offset (`X20`), adjusting the buffer content via `memmove` as needed.\n- Handles comments (`#`) or special opcodes via inline checks and indirect calls.\n\nAbbreviated summary:  \n**Buffer parser that validates and consumes data, shifts remaining bytes, and dispatches to handlers based on alignment and content.**"}
{"sub_140003BD0": "This function performs a conditional dispatch based on arguments, likely implementing a **dispatch or callback handler** for some structured data or event, possibly related to networking or I/O (given memory layout and string references like `sub_1400124BC`, `unk_140017CDC`, etc.). It:\n\n1. Reads a control structure from `X0` (likely a context or descriptor), checks a flag at offset `+8`.\n2. Depending on the flag (bit 0 or value 0/1), it either:\n   - Returns early with `0` (if first flag bit not set and no callback provided), or\n   - Sets up and calls a function pointer (e.g., at `+0x18` of some sub-object), sometimes with structured stack arguments (vectors, strings, function pointers), possibly interfacing with a subsystem (e.g., SSL, socket, async I/O).\n3. Contains multiple branches handling different cases (e.g., `W8 == 1`, `W23 == 1`, `X8 == 2`, etc.), suggesting it's handling different **command types or event kinds**.\n4. Uses `sub_1400124BC` and `sub_140012400` heavily—likely generic utility/submission functions (e.g., for submitting operations or reporting results).\n5. Always returns `1` (via `W19`) on success paths, `0` on early exit.\n\n**High-level summary**: A dispatch routine that interprets input parameters to decide which underlying operation to invoke—possibly part of an async I/O or event loop system (e.g., a completion port handler, or SSL record processor)."}
{"sub_140008854": "This function performs a structured data transformation and copying routine, likely part of a cryptographic or packet-processing operation. Here's the high-level summary:\n\n- **Prologue**: Saves callee-saved registers and allocates stack space (~0x1E0 bytes).\n- **Initial setup**:\n  - Calls `sub_140008738` twice — possibly to prepare or validate two input structures (passed in `X2` and `X3` as `X20`, `X19`).\n  - Sets up multiple stack-based temporary buffers (`var_160`, `var_1E0`, `var_120`, etc.).\n- **Main processing loop / conditional handling**:\n  - Reads fields (including `float128`/SIMD vectors via `Q0`–`Q2`, and scalar values like `X8`, `W25`) from a source structure at `[X23]` (likely an input struct).\n  - Copies/splits values into two destination buffers (`X20`, `X21`, `X24`) — possibly unmarshaling or decomposing structured data.\n  - Calls `sub_1400090F4` twice — possibly normalization, decoding, or validation steps.\n  - Loads more data and compares two flags (`W9`, `W8`) against `0xFF`:\n    - If either equals `0xFF`, branches to `sub_140008974`, where it appears to:\n      - Load SIMD vectors from a second source (`X19`) and store them in a destination buffer (`X21`, likely a structured output).\n      - Calls `sub_140008D0C` — likely finalizing or encoding the result.\n    - Otherwise, in `sub_140008938`, calls `sub_140008FF0` — likely a validation or mapping function — and if it returns zero, jumps to return-zero path.\n  - Loop back to `0x1400088AC` suggests iterative processing (e.g., over chunks, fields, or elements).\n- **Epilogue**: Restores registers and returns `XZR` (i.e., `0`) on most paths, except `0x140008974` falls through to same epilogue but likely returns non-zero (unobserved return in block — possibly `X0` is non-zero if `sub_140008D0C` sets it, or falls through to `loc_14000899C` returning whatever `X0` holds).\n\n**Conclusion**: This function takes two structured inputs (possibly binary formats or protocol messages), decomposes/sanitizes/copy-transforms them into standardized internal representations, performs validation or finalization calls, and returns 0 on success (or a code indicating failure/special case). Likely part of a network, crypto, or serialization library."}
{"sub_140001F50": "This function is a **virtual function dispatcher stub**, likely for an indirect call through a vtable or function pointer.  \n\n- It saves callee-saved registers `X19`, `X20`, `X29`, `X30`.\n- Loads two values from `[X0]` into `X20` and `X19` — likely the object pointer (`this`) and a function pointer (e.g., vtable entry).\n- Calls `_guard_check_icall_nop`, suggesting control flow protection (CFI) instrumentation (e.g., CFG/CFI).\n- Calls `sub_14000104C` with arguments `0x10`, `8` — possibly a lookup or validation helper (returning non-zero on success).\n- If the result is zero (`CBZ`), it falls through to `loc_140001F8C`, where it again calls `sub_140016BE4` (likely another validation or logging helper), then triggers a `BRK #1` — indicating an expected failure path (e.g., failed guard, invalid call), likely a trap/hard breakpoint in debug or instrumentation builds.\n- On success, it loads two more values from `[X0]` (`X8`, `X2`), sets up arguments for a tail call to `sub_140012400` — likely the actual target function being dispatched to (with `X0 = X1`, `X1 = X8`, implying reordering of arguments, possibly `this` and a method identifier or second argument).\n\n**Summary**: A guarded virtual function dispatch stub that validates the call (possibly via CFG/CFI or vtable bounds) and then tail-calls the target function; fails with a breakpoint on validation failure."}
{"sub_1400036B8": "This function appears to be a synchronization primitive wrapper—specifically, it's likely implementing `WakeByAddressSingle` (or similar) from Windows NT internals.\n\n### Key Observations:\n- **Conditional branch on `W10 == 0`**: If true, jumps to `loc_140003718`, otherwise proceeds through the first path.\n- Both paths prepare arguments and call:\n  - `sub_140006048` — likely initializes or sets up a synchronization structure (e.g., `InitializeConditionVariable`, or similar, possibly `__C SpecificConditionVariable` wrapper).\n  - `sub_1400013BC` — possibly a low-level RTL helper (e.g., `NtWaitForKeyedEvent`, `NtSignalAndWaitForSingleObject`, or internal Win32 condvar helpers).\n- In the second path (`W10 == 0`), it **also** calls `sub_140004538`, and then compares `W8` to `2`.\n  - If `W8 == 2`, it loads and calls `WakeByAddressSingle` indirectly via `BLR`.\n- Return epilogue cleans up the stack frame.\n\n### Interpretation:\nGiven the control flow, data references (`unk_140017B55` vs `unk_140017B2E`, likely pointers to keys/addresses), and the final call to `WakeByAddressSingle`, this function likely implements one of the `WaitOnAddress` / `WakeByAddressX` family — probably **`WakeByAddressSingle`**, used to wake **one** waiter on a memory address (typical in futex-like user-mode synchronization on Windows).\n\n**In short**:  \nThis function wakes one thread waiting on an address — a thin wrapper around `WakeByAddressSingle`."}
{"sub_14001104C": "This function appears to be a lightweight wrapper that prepares arguments and calls `sub_140011760`, likely a helper for some string or data operation (e.g., formatting, decoding, or validation involving the literal `\"0x0123456789ABCDEF\"`). It sets up:\n- `X0 = X1` (original first arg),\n- `X1 = 1`,  \n- `X2 = addr of \"0x0123456789ABCDEF\"`,\n- `X3 = 2`,\n- `X4 = X9 + 2 = (X29 - var_2 + X8) + 2` (likely a buffer or offset),\nthen calls `sub_140011760`. It restores frame pointer/return address and exits. Standard prologue/epilogue suggest it's a leaf or near-leaf helper."}
{"sub_1400061A8": "This function performs a Windows I/O write operation via `NtWriteFile`, then waits for the operation to complete using `WaitForSingleObject`. It checks for a specific NTSTATUS (`0x103`, i.e., `STATUS_PENDING`) after each call to handle asynchronous I/O completion. If the operation completes with an error (non-`0x103` and not `STATUS_PENDING`), it converts the NTSTATUS to a Win32 error code using `RtlNtStatusToDosError`, sets up a compound error value (`1 << 32 | win32_error`), and returns failure (`W0 = 1`). If the initial `NtWriteFile` returns `0x103`, it waits indefinitely (`0xFFFFFFFF`) on the handle passed in `X0` (stored in `X19`). If the wait completes with `0x103`, it falls through to handle the pending status again; otherwise, on success, it returns `0`. On a final error (including `0x103` after wait), it logs a \"fatal runtime error: I/O error\" message and triggers a breakpoint (`BRK #0xF003`), likely for debugging or crash reporting in a runtime (e.g., Rust panic-like behavior). The function preservescallee-saved registers `X19`, `X29`, `X30` and allocates stack space for local variables and a `OVERLAPPED`-like structure."}
{"sub_14000EBCC": "This function parses a string (pointed to by `X0`) for a specific prefix pattern, likely part of a base conversion or tokenizer. It checks for prefixes `\"K_\"`, `\"L_\"`, or invalid syntax, and either returns an error (with recursion limit or invalid syntax message), or calls a helper (`sub_14000EFD0`) after updating position/state in the input structure (`X0`).\n\n**High-level behavior:**\n- Reads a buffer (`X0+8`) and current index/length (`X0+0x10`, `X0+0x20`).\n- Returns early if buffer is null or index ≥ length.\n- Reads next character:\n  - `'K'` → increments index, calls `sub_14000E044`.\n  - `'L'` → increments index, calls `sub_14000DAE4`.\n  - Otherwise, checks if subsequent chars match digit/letter patterns, possibly for base-N parsing.\n- On error (e.g., invalid char at expected position, or recursion), sets error flag and returns 1.\n- On success, returns 0.\n\n**Likely purpose:**  \nA component of a parser (e.g., ASN.1, RLP, or similar), handling base-k/62-like encodings or special token prefixes (K/L) in structured data.\n\nNote: The presence of `aInvalidSyntaxR` and recursion checks suggests integration with a larger parsing state or recursive descent parser (e.g., Python-style)."}
{"sub_14000F0E8": "This function parses a string (likely a number or expression) starting at a structured input buffer (`X0`), checking for specific prefixes (`\"IB_\"`, `\"IE\"`, `\"BE\"`), and attempts to parse a base-62-encoded numeric value (`0-9`, `A-Z`, `a-z`), storing the result in the buffer. It handles recursion via tail call, enforces a recursion limit (0x1F4), and returns error codes on invalid syntax, overflow, or recursion depth exceeded.\n\n- **Return values**:\n  - `0`: Success (parsed number written to `[X0,#8]`, next index in `[X0,#0x20]`)\n  - `1`: Early success (e.g., matched `\"I\"` followed by recursion success)\n  - `2`: Error (invalid syntax, overflow, or recursion depth hit)\n\n- **Structure of input buffer (`X0`)**:\n  - `[0]`: pointer to start of string\n  - `[8]`: current index/offset (updated during parsing)\n  - `[16]`: recursion depth counter (incremented on recursive call)\n  - `[24]`: recursion limit or length (checked as `< 0x1F4`)\n  - `[32]`: optional callback/result buffer (used for error messages)\n\nThe function is part of a base-62 decoder with recursion support, used in a parser (possibly for Rust-style type or identifier syntax given string literals)."}
{"sub_14000BB4C": "This function performs a **case-sensitive search for the character `'E'`** (0x45) in a buffer pointed to by `X0+8`, starting at an offset stored in `X0+0x20`, up to a limit stored in `X0+0x10`. It returns a non-zero value if `'E'` is found *and* certain conditions on an external state (possibly a flag or pointer at `X0+0x28`) are met — otherwise zero.\n\n### High-level behavior:\n- Initializes loop with a pointer `X8 = *(X0+8)` (buffer), index/offset `X9 = *(X0+20)`, and limit `X10 = *(X0+16)`.\n- While `X9 < X10` (buffer bounds check), loads byte at `X8 + X9` and checks if it's `'E'`.\n- On match (`'E'`):\n  - Increments the offset (`X9+1`), stores it back at `X0+0x20`, sets return value to 0 (`X19 = 0`), and jumps to epilogue → returns **0**.\n- On loop exit (no `'E'` or early exit):\n  - Checks if `X0+0x28` (a callback/state pointer?) is non-zero:\n    - If yes, calls `sub_140012B6C(..., 2)` and returns its result’s bit 0.\n    - Else, calls `sub_14000E044(..., 1)` and returns its result’s bit 0.\n- Epilogue restores saved registers and returns `X19` (0 unless set earlier).\n\n### Key insight:\nThe function *increments the offset on match*, suggesting it’s part of a **stateful scan or skip operation** — likely used for tokenizing, parsing, or skipping up to/over the first `'E'`. Return value logic is conditional on auxiliary state, possibly signaling success or requiring external validation.\n\nIt resembles a utility function in a parser or tokenizer, possibly part of a larger structured data reader."}
{"sub_14000FA0C": "This function decodes a hex string (or partial UTF-8 byte sequence) from memory and writes the decoded byte(s) into a temporary buffer, then calls `core::str::converts::from_utf8`. It also maintains and updates two pointers/lengths in the input struct (`[X0+0x10]` and `[X0+0x18]`). The function returns an integer status code in `W0`:  \n- `0xFFFFFFFE`: unexpected condition early (length underflow).  \n- `0xFFFFFFFF`: invalid input (e.g., non-hex char, malformed UTF-8).  \n- Otherwise, a decoded value (likely partial UTF-8 codepoint or error indicator).  \n\nIt performs hex digit parsing, UTF-8 length/continuation validation (1–4 bytes), and buffer management, terminating in a panic (`BRK #1`) on internal errors or unreachable code."}
{"sub_140007C94": "This function implements a conditional dispatch or callback invocation mechanism, likely part of a runtime or virtual machine.\n\nIt operates on a structure pointed to by `X0`, which contains at least:\n- An 8-byte field at offset 0 (`X8 = [X0]`)\n- A pointer at offset 8 (`[X0+8]`)\n- And a metadata object at offset 0x18 (a function pointer table or similar).\n\n**Key behavior:**\n1. If the function pointer at `[X20+0x18]` is null, it logs a redacted message and breaks.\n2. Otherwise, it:\n   - Allocates a small stack frame.\n   - Increments a counter at `[X20+0x18]`.\n   - Loads and validates a value at `[X0+8]` — specifically checking if `(value & 3) == 0` or `(value & 3) == 2` (via `AND X9, X8, #3` and comparisons).\n   - If valid, it reads two fields from the structure at `X8`: one at offset 0xF (64-bit), one at offset 7 (64-bit).\n   - It conditionally calls a function pointer from `[X23]` with `X21` as the first argument.\n   - Then conditionally calls `sub_140001040` (likely a memcpy or structured copy) with parameters derived from `[X23+8]`, `[X23+0x10]`.\n   - Finally, calls `sub_140001040` again with a calculated address (`X20-1`), size `0x18`, and count `8`.\n3. Stores the final result (in `X0`) back to `[X19+8]`, then returns `X0 != 0`.\n\nIn short:  \n**This is a dispatcher that validates a tagged pointer/value, invokes associated callbacks or copy/move routines, and returns a success/failure flag.**"}
{"sub_14000C1A0": "This function parses a string starting at `X0+8` (a buffer) using a cursor stored at `X0+0x10` (length) and `X0+0x20` (current position), looking for a specific prefix pattern: `'G' '_'` followed optionally by more identifier-like characters (alphanumeric or `_`). It validates and consumes part of the input to parse what appears to be a syntax token, likely for a regex or language grammar. If parsing succeeds, it may recurse (via `sub_14000EFD0`) with a recursion counter at `X0`, and eventually returns via `sub_14000C744` (likely a continuation or handler). Error paths print diagnostic messages and return zero; success paths return 1 or propagate failure codes.\n\nIn short: **It parses and validates a specific token prefix `G_...` in a string, updating internal cursor and potentially recursing, returning 1 on success, 0 on failure.**"}
{"sub_14000FD78": "This function implements **Punycode decoding**, specifically handling the decoding of a Punycode-encoded string (commonly used for internationalized domain names) into a Unicode string.\n\n### Key Observations:\n- **Input**: Takes two arguments: `X0` (likely a pointer to a structure containing input string info, with fields at offsets 0, 8, and 0x18), and `X1` (saved as `X19`).\n- **String parsing**: Reads a byte string (likely ASCII-encoded Punycode), processes each character to determine ASCII letter/digit, and handles base-36-like decoding using `a-z`, `0-9`, and special offset handling (`-0x61`, `-0x30`, `-0x16`).\n- **Numeric accumulation**: Computes a cumulative value (`X6`) with base-dependent multiplication and overflow checks (`UMULH`, `MUL`, `CSINC`, `CCMP`), typical in variable-length integer decoding (e.g., for codepoint values).\n- **Overflow/error handling**: Uses `BRK #1` (debug trap) and error logging via `sub_140012400` (likely an error/reporting function) on unexpected conditions (e.g., invalid bytes, overflow, out-of-range codepoints). Output buffer overflow is checked at 0x80 (128) characters.\n- **Output**: Stores decoded 32-bit values (likely UTF-32 code units) into a stack buffer (`[SP + X9*4]`), and may call `sub_140012C2C` (likely a string output/UTF-16 encoding function) at the end.\n\n### Conclusion:\nA **Punycode decoder** with robust error checking and intermediate buffer management, returning 1 on success (`W0 = #1`) or handling internal errors via traps and logging."}
{"sub_140010F68": "This function initializes and calls `sub_140011760`, likely to initialize or configure some context (e.g., logging or crypto state), using a hardcoded hex string `\"0x0123456789ABCDEF\"` as input, possibly as a key or salt. It adjusts the stack pointer, saves/restores frame pointer and LR, and returns. The arguments passed (`W1=1`, `W3=2`) suggest configuration flags—perhaps enabling some feature or setting mode."}
{"sub_140012EC8": "This function (`sub_140012EC8`) appears to be a **conditional serialization or encoding routine**, likely part of a JSON or structured data serializer.\n\n### Key Observations:\n\n- **Prologue**: Allocates stack space and saves callee-saved registers (`X19`–`X24`, `X29`/`X30`). Uses `X0` as primary input (likely a context/object) and `X1`/`X2` as additional parameters (likely serializer callback and context).\n- **`X0,#9` bit 0**: Controls early branching — if set (`TBNZ`), skips some logic (possibly for a \"compact\" mode).\n- **Checks `X0,#8` bit 0** and **`[X1,#2]` bit 7** to determine formatting/indentation mode.\n- Calls a function via `[X8,#0x18]`, where `X8` is loaded from `[X21]` or `[X21,#8]` — likely a virtual method table (vtable) function pointer, possibly `serialize()` or `write_string()`.\n- Uses string literals:\n  - `\", :  {\\n,\\n((\\n\\n}),] \"` (verbose)\n  - `\"\\n}),] }\"` (minimal/trailing)\n  - `\",\\n((\\n\\n}),] }\"` (continued item)\n  - `unk_1400193A0` (likely a format string or helper data)\n- Branches conditionally based on return value of those calls (`TBNZ W0`), setting `W22` accordingly — possibly indicating success/failure or need for further handling.\n- Finalizes by storing flags back to `X0,#8` and `X0,#9`, restores stack, and returns.\n\n### High-Level Summary:\n\nThis function implements **conditional serialization logic**, likely for a structured data type (e.g., a JSON object), choosing between compact and pretty-printed output based on input flags. It coordinates with helper functions (via vtable) to emit formatting strings and delegate field serialization.\n\n(Nothing standard-library-specific is evident; context likely part of a custom or third-party serializer, possibly in a game/engine codebase.)"}
{"sub_140016C8C": "This function appears to be a stub or debug breakpoint, likely used for exception handling or breakpoint insertion in a debugging/telemetry scenario.\n\nKey observations:\n- Sets up a stack frame with `STP X29, X30` and allocates 0x30 bytes.\n- Constructs a structure on the stack containing:\n  - Original function arguments (X0, X1) saved at `[SP+0x10]`\n  - Address `sub_140013BCC` and a stack pointer offset (`var_28`) stored at `[SP+0x20]`\n  - The same data duplicated at `[SP+0x10]` again (likely two overlapping or aliased structures).\n- Calls `sub_140016DAC`, passing a pointer to the constructed structure (via `X0 = SP+0x20`).\n- Immediately executes `BRK #1`, indicating intentional halting—typical of a trap or crash stub.\n\nGiven the structure initialization and the `BRK #1`, this is almost certainly a crash handler or debug trap stub—possibly part of a telemetry or minidump mechanism where `sub_140016DAC` collects context before halting execution."}
{"sub_140010EF8": "This function sets up stack pointer-relative addressing to pass a string literal `\"0x0123456789ABCDEF\"` (via `ADRL X2`) and calls `sub_140011760` with arguments prepared using `X0`–`X4`. It preserves frame pointer and return address on entry, restores them before returning, and cleans up 0x20 bytes of stack.\n\nBased on the setup (X0=X1, W1=1, W3=2, X4 pointing 0x10 beyond adjusted SP, X2 to a known hex string), and typical usage of such calls, this is likely a helper to write or format that hex string (e.g., into a buffer pointed to by X0/X1), possibly part of a `printf`-style or hex-dump utility.\n\nHowever, without visibility into `sub_140011760`, the precise semantics remain tentative."}
{"sub_1400165F8": "This function checks whether a 32-bit value at offset `0x48` in the struct pointed to by `X0` is non-zero. If it is zero, the function returns immediately. If non-zero, it initializes a local structure (placing pointers and a value onto the stack), then calls `sub_140016244`, likely to perform some setup or initialization based on a string literal `\"<redacted>\"`. The function otherwise just cleans up the stack frame and returns.\n\nIn short:  \n**Conditional early return based on a flag at `*(X0 + 0x48)`; if set, invokes `sub_140016244` with a redacted string and stack-based struct.**"}
{"sub_140006060": "This function appears to be a **parser or validator for a structured data blob**, likely related to **debug symbols or metadata in a Mach-O/ELF-like binary format**.\n\n### High-level behavior:\n1. **Input parameters** (`X0`, `X1`, `X2`):\n   - `X0`: base address of the data blob (or header)\n   - `X1`: pointer to current position / data section\n   - `X2`: length or count — if zero, jumps to exit early.\n\n2. **Initial steps**:\n   - Saves callee-saved registers on the stack.\n   - Validates `X2` (non-zero); if zero, goes to clean-up/exit.\n\n3. **Calls `sub_140005768` (likely a symbol look-up or relocation resolver)** with arguments derived from inputs, and checks return value bit 0.\n\n4. **Conditional branching based on alignment (`X1 & 3`)**:\n   - Diverges on 0, 1, 2, or >2 alignment cases.\n   - Loads a byte/shortword (W9) either from `[X1]`, `[X1-1]`, or upper 8 bits of `X1` (via `UBFX`).\n   - Compares that byte to `0x23` (`'#'`). If not match → exit.\n\n5. **If `#` is found**, continues into a section that:\n   - Reads 16-byte aligned structs from `[X1+7]` and `[X1+0xF]`.\n   - Resolves and calls a function pointer from a vtable-like structure.\n   - May call `sub_140001040` (likely `malloc` or `memalign`) to allocate buffer.\n   - Processes further entries recursively or iteratively.\n\n6. **On error (e.g., invalid alignment, length check fail, or bad check)**:\n   - Triggers `sub_140016E68` (possibly a logging/assertion helper) with `\"<redacted>\"`, then hits `BRK #1` — a debugger/intentional crash trap.\n\n7. **Exit paths**:\n   - Sets `X0 = X1 = 0` or restores X1 from a saved register and exits cleanly.\n\n### Conclusion:\nThis function implements **validation and processing of a tagged data section**, where entries are optionally aligned, start with `'#'`, and contain function pointers and offsets used for further processing (e.g., debug info parsing, symbol relocation, or unwinding). The presence of `BRK #1` and redacted string suggests debug builds only.\n\nMost likely: part of a **debug symbol resolver** (e.g., for DWARF, compact unwind, or debug map parsing)."}
{"sub_1400083D8": "This function appears to be a **debug/exception handler or early crash trigger**, likely related to detecting invalid object states or calling conventions.\n\n### Summary:\n- It accepts three arguments (`X0`, `X1`, `X2`), possibly `self`, `arg1`, `flags`.\n- If bit 0 of `X2` is clear, it jumps directly to a function at `sub_1400124BC` (likely a standard dispatcher or logging call).\n- Otherwise, it dereferences `X0` and looks up a function pointer at offset `+0x18` of an object pointed to by `X0`. If that function pointer is non-null, it goes to a crash branch (`loc_1400084E0`) — specifically printing `<redacted>` and executing `BRK #1`, indicating a deliberate breakpoint-triggered crash/debug exception.\n- In the non-crash path, it performs some pointer/offset math, allocates stack space, calls `sub_140005AD0`, and later `sub_140001040`, ending with returning `1` (success).\n- The repeated use of `BL`/`BLR` with stack-manipulated arguments suggests callback invocation or method dispatch.\n- The crash path suggests this function is checking a condition (e.g., *“if this object is not in expected state, crash”*), and the `BRK` confirms it's a debug sentinel.\n\n### Likely Purpose:\nA **precondition/assertion wrapper** or **object validation guard** — calls a fallback function if a flag is clear, or crashes with debug info if a required function pointer is missing/non-null (depending on interpretation of the `CBNZ`)."}
{"sub_140002714": "This function performs a **thread-specific operation involving TLS (Thread Local Storage) management and exception-like handling**, likely part of a runtime or virtual machine setup (e.g., a debugger or profiler hook). Key behaviors:\n\n1. **Entry & prologue**: Saves callee-saved registers, allocates stack frame.\n\n2. **Conditional dispatch on `X1`**:\n   - If `X1 == 0`, skips to checking TLS slot at `qword_14001D0F0`.\n   - Otherwise, inspects an object pointed to by `X1`:\n     - Reads a function pointer at offset `+0x20` — if non-null, decrements a value at `+0x28` (possibly a refcount or depth), then jumps to common logic.\n     - If that function pointer is null, falls through to check TLS slot `qword_14001D0F0`.\n\n3. **TLS/Global state check**:\n   - Retrieves TLS pointer via `X18 + 0x58`, uses `TlsIndex` to index into a per-TLS-array slot.\n   - Loads a function pointer at offset `+0x58` from that TLS slot.\n   - Compares it to `unk_140017A72` (likely a sentinel or handler address). If equal, sets `X8 = 0`, else keeps it.\n\n4. **Fallback logic**:\n   - If `X8 == 0`, loads `unk_140017A9D` (possibly a generic handler stub), and `W9 = 9`.\n   - Otherwise, retains previous values.\n\n5. **Thread ID query**: Calls `GetCurrentThreadId()`. If result is zero, jumps to cleanup.\n\n6. **Atomic increment of TLS counter**:\n   - Loads TLS storage location (`qword_14001D0F8`), attempts atomic compare-and-swap to increment it using `LDXR`/`STXR`. Loops until success or exits on failure (`CLREX`).\n\n7. **Sets up stack for call to `sub_140007330`**:\n   - Zeroes vector registers (`V0.2D`), builds stack frame with addresses of handlers (`sub_140013BCC`, `sub_140004B2C`), and some constants.\n   - Calls `sub_140007330` (unknown, but likely a dispatcher or dispatcher helper).\n   - On success (`X0 != 0`), checks remainder modulo 4, and if `2 <= X0 % 4 <= 3`, or if zero, calls destructors/finis on cleanup objects (via function pointers loaded from `X0` fields), then calls a finalizer.\n\n8. **Error/exit paths**:\n   - On certain failures, triggers `BRK #1` (debugger breakpoint), indicating异常 handling or unreachable paths.\n   - `sub_140016E68` is called with redacted message if TLS version check fails (`X2 >= 0x201`).\n   - Other failure paths call `sub_140016484` then `BRK #1`.\n\n**Conclusion**: This function appears to be part of a **runtime entry/exit handler**, possibly for initializing/cleaning up per-thread state in a managed runtime or enclave-like environment — with TLS-based dispatch, atomic bookkeeping, and guarded cleanup. The presence of breakpoints and redacted strings suggests anti-debugging or release-mode safety mechanisms."}
{"sub_14000B974": "This function (`sub_14000B974`) is a **recursive parser/evaluator for a numeric expression**, likely handling base conversion or token parsing, with recursion depth limiting and syntax validation.\n\n### Key observations:\n- **Entry saves callee-saved registers (`X19`, frame setup)** and reads a context pointer in `X0`.\n- `[X0]` and `[X0,#8]` appear to be a **buffer/offset pair** (e.g., current pointer and end pointer).\n- `[X0,#0x10]`, `[X0,#0x18]`, `[X0,#0x20]`, `[X0,#0x28]` are used as:\n  - Start index / lower bound (`0x10`)\n  - Recursion counter / depth (`0x18`)\n  - Current position (`0x20`)\n  - A callback/function pointer or continuation state (`0x28`)\n- **`LDR X9, [X19,#8]!`** increments `X19`'s load target and checks if it's zero — likely a pointer to next token or buffer length.\n- Parses characters sequentially:\n  - Checks for `'_'` (underscore) as a separator or invalid character.\n  - Validates digits (`'0'–'9'`, `'A'–'Z'`, `'a'–'z'`) and computes numeric values — uses `UMULH`, shifts, and conditional additions — indicating **radix-based digit accumulation**, probably **base-62** or similar.\n- On encountering `'_'`, it resets or adjusts state — possibly handling negative signs or separators.\n- Upon overflow or recursion limit (`0x1F4 = 500`), it emits diagnostic messages via `sub_140012B6C`.\n- Calls `sub_14000DAE4` (likely a recursive subparser or secondary stage), preserving/restoring vector registers (`Q0`, `Q1`) — possibly SIMD operands.\n- Returns 0/1 in `W0` indicating success/failure or valid/invalid.\n\n### Summary:\nThis function **parses and evaluates a numeric token from a string**, handling:\n- Base-range validation of characters,\n- Digit accumulation with overflow checks,\n- Recursion depth limiting,\n- Syntax error reporting.\n\nMost likely part of a **numeric expression evaluator**, **tokenizer**, or **custom parser** (e.g., for a scripting language or bytecode interpreter)."}
{"sub_140015758": "This function verifies whether a given memory region (pointed to by `X0`) is a valid PE (Portable Executable) image in memory, and if so, determines whether it is a 32-bit or 64-bit PE file (specifically checking for PE32+ / `IMAGE_NT_HEADERS64`).\n\n### High-level logic:\n- Confirms DOS header magic (`\"MZ\"` at offset 0 → `0x5A4D`).\n-Locates PE header via `e_lfanew` (offset `0x3C` in DOS header), then validates PE signature (`\"PE\\0\\0\"` → `0x0000004550`).\n- Checks optional header magic: `0x20B` (PE32+) or `0x10B` (PE32); only `0x20B` is accepted here (32-bit PE headers use `0x10B`, 64-bit use `0x20B`—note: the check is for `0x20B`, so it's specifically verifying a PE32+ image).\n- If header is valid, extracts `SizeOfOptionalHeader` and `NumberOfRvaAndSizes` from the optional header.\n- Computes start of data directories (`OptionalHeader.DataDirectory`), using the formula:\n  - `base = OptionalHeader + 0x18`\n  - `entry_size = 8` (size of each `IMAGE_DATA_DIRECTORY`)\n  - total size = `NumberOfRvaAndSizes * 8`\n- Then iterates over data directories (likely looking for a specific one—possibly Export or Import), checking for validity:\n  - Compares `VirtualAddress` and `Size` fields against the input pointer `X0`.\n  - If a matching directory entry is found, checks bit 31 (highest bit) of its `Characteristics`/`Flags` field (e.g., ` Characteristics` in `IMAGE_OPTIONAL_HEADER64.DataDirectory[i].Size`? Actually, `Size` field is used — the bit check is on `W8, #0x1F`, i.e., bit 31 of the `Size` field).\n- Returns `1` if conditions are satisfied (valid PE32+ and matching directory with high bit set), `0` otherwise.\n\n### Summary:\n**Checks if the input buffer is a valid PE32+ (64-bit) executable image and if a specific data directory entry (likely based on index derived from optional header) has its most significant bit set in the Size field.**\n\nMost likely used for validating and identifying a specific kind of PE image (e.g., with certain characteristics such as `IMAGE_DLLCHARACTERISTICS_HIGH_ENTROPY_VA` or a custom flag stored in the `Size` field — though highly unusual). Given the `TBZ #0x1F`, and that only PE32+ (`0x20B`) is accepted, it may be part of a loader or verifier that checks for specific PE flags (e.g., ASLR compatibility or custom loader requirements)."}
{"sub_14000BF3C": "This function appears to be scanning or parsing a data structure (likely a buffer or string) pointed to by `X0`, looking for the ASCII character `'E'` (0x45). It maintains a counter (`X19`) of iterations and uses offsets within the structure (e.g., `[X0,#8]`, `[X0,#16]`, `[X0,#32]`) to track state.\n\nKey behaviors:\n- If `[X0,#8]` is null, it skips processing and returns `(0,0)`.\n- Otherwise, it enters a loop:\n  - Reads `[X0,#0x10]` and `[X0,#0x20]` as likely bounds/size fields (`X10`, `X9`).\n  - Compares `X9` (likely offset/index) against `X10` (limit); if out-of-bounds (unsigned ≥), jumps to cleanup.\n  - Reads a byte at `[X0,#8 + X9]` — if it equals `'E'`, it stores `X9+1` back to `[X0,#0x20]` (advance index) and exits early with `(0, iteration count)`.\n  - If not `'E'`, it checks optional side-action:\n    - If `[X0,#0x28]` is non-null, it calls `sub_140012B6C`, and if its return value bit 0 is set, it breaks out.\n    - Else, it calls `sub_14000DAE4`, and if its return value bit 0 is set, it breaks.\n  - After each iteration, increments `X19`, checks if `[X0,#8]` is still non-null, and loops.\n- Final return: `(X0 = X20 = 0, X1 = X19 = iteration count)`.\n\n**Conclusion**: Likely a scanning loop searching for `'E'` in a buffer while optionally triggering callbacks (via `sub_140012B6C` or `sub_14000DAE4`). Returns number of iterations performed (or up to some limit), and updates internal index in `[X0,#0x20]` upon finding `'E'`."}
{"sub_140010B40": "This function decodes UTF-16 encoded input (pointed to by `X0`) into UTF-8, writing the result to a caller-provided buffer (at `X1`). It handles surrogate pairs, ASCII, and multi-byte UTF-8 sequences, updating both the source and destination pointers and returning the number of output bytes. Likely part of a UTF conversion library (e.g., `utf16_to_utf8`)."}
{"sub_14000B574": "This function appears to implement a recursive or iterative division-like operation, likely part of a big integer or arbitrary-precision arithmetic routine. Here's the summary:\n\n- It takes three arguments in `X0`, `X1`, `X2` (possibly a structure pointer, divisor, and count).\n- It first checks if bit 0 of `X2` (the count?) is set; if not, it jumps to a helper (`sub_1400124BC`) that likely loads a global variable (address `unk_140018958`) and does some initialization.\n- If the bit is set, it:\n  - Right-shifts `X2` by 1 (halving the count),\n  - Loads a 128-bit value (likely a high:low big-endian integer) from `[X0+8]`,\n  - Subtracts the (halved) count from the high part (`X8`) and adjusts the low part (`W9`) with borrow via `CSINC`,\n  - Stores the updated 128-bit value back,\n  - Checks if the new low part (`W9`) is even (bit 0 clear).\n- If low part is even, it falls through to:\n  - Load a function pointer from `[X0]`,\n  - Recurse or tail-call into `sub_140012400` with updated args.\n\nThe structure suggests it’s performing repeated halving/subtraction steps, possibly implementing division or modular reduction of a big integer stored in `*X0`, guided by `X2` (likely a bit-count or iteration count), and possibly branching based on parity. The presence of recursion via tail-call to `sub_140012400` reinforces this.\n\nMost likely: **Big-integer division or right-shift with borrow handling, possibly part of division-by-power-of-two or generalized division routine.**"}
{"sub_1400108CC": "This function encodes a 32-bit value `W1` into a variable-length UTF-8–style byte sequence starting at memory location `[X0+8]`, using `[X0]` as a base pointer and updating it to point past the end of the written data. It writes 1–4 bytes depending on the value's magnitude:\n\n- If `W1 < 0x80`: write 1 byte (`W1 & 0x7F`).\n- If `0x80 ≤ W1 < 0x800`: write 2 bytes (`0b110xxxxx` + low 6 bits, `0b10xxxxxx` + next 6 bits).\n- If `0x800 ≤ W1 < 0x10000`: write 3 bytes (`0b1110xxxx` + top 4 bits, `0b10xxxxxx` + middle 6, `0b10xxxxxx` + low 6).\n- If `W1 ≥ 0x10000`: write 4 bytes (`0b11110xxx` + top 3 bits, `0b10xxxxxx` × 3 for remaining 18 bits).\n\nBefore writing, if `W1` exceeds a bound (computed via comparisons with `0x80`, `0x800`, `0x10000`), it may delegate to `sub_140016B04`—likely a helper that handles overflow or special cases (e.g., surrogate handling or error reporting). On success, it updates the pointer at `[X0]` and returns 0. Functionally, this is a UTF-8 encoder for Unicode code points."}
{"sub_140006A64": "This function implements a **looped retry mechanism to retrieve the current directory using `GetCurrentDirectoryW`, expanding the buffer size as needed if the buffer is too small (error 122 / `ERROR_INSUFFICIENT_BUFFER`, which corresponds to hex `0x7A`)**.\n\n### Key behavior:\n- Saves callee-saved registers and allocates stack space (~1KB).\n- Calls `SetLastError(0)` to clear any prior error.\n- Begins with a 0x200 (512)-unit buffer (characters, so 1024 bytes for wide chars).\n- Loops:\n  1. Calls `GetCurrentDirectoryW(buf_size, buffer)`.\n  2. On failure (`W0 == 0`), checks error via `GetLastError()`.\n  3. If error ≠ 122 (0x7A), branches to error paths (logs error, possibly asserts or returns).\n  4. If error == 122 (buffer too small), doubles buffer size (capped at some limit, possibly 0x20000000 or max safe value in `X28`), reallocates or reuses buffer (via `sub_140016758` or `sub_140001040`), and retries.\n- The buffer is stored at `[SP+#var_470]` (and possibly heap-allocated via helper functions).\n- On success (buffer large enough to hold the result, including null terminator), `GetCurrentDirectoryW` returns the length, and when `len == req_len`, the loop exits cleanly.\n- Cleanup: restores stack and registers, returns.\n\n### Observed calls:\n- `SetLastError`, `GetCurrentDirectoryW`, `GetLastError` (Windows API)\n- Helper subfunctions (likely allocation, error handling, or string utilities):\n  - `sub_140016758` — possibly a safe realloc or buffer-grow helper (called on buffer-too-small case)\n  - `sub_140001040` — likely a malloc or buffer realloc routine (called if pre-allocated buffer found)\n  - `sub_140010B40` — likely free (called at exit on some paths)\n  - `sub_140016D8C`, `sub_140016E68` — logging/assertion helpers (print redacted path, then `BRK #1`)\n\n### Conclusion:\nA robust **Windows `GetCurrentDirectoryW` wrapper with auto-sizing buffer allocation**, retrying until the buffer is large enough or an unexpected error occurs (asserts on unreachable paths)."}
{"sub_140013048": "This function appears to conditionally update a flag (stored at `[X0]`) based on whether a callback function succeeds. Specifically:\n\n- It reads a struct from `[X0]` containing (at least) two fields: a pointer to a `bool` (or byte flag) at offset 0, and a function table or vtable pointer at offset 0x10.\n- It checks if the boolean flag (byte at `[X20]`, i.e., `*[X0]`) is zero (`TBZ W8, #0, ...`). If it's *not* zero, it jumps to `loc_1400130A4`, skipping the callback.\n- If the flag *is* zero, it calls a function at `[X21,#0x18]` (i.e., third entry in a vtable/function table pointed to by `[X0,#0x10]`), passing `[X0]` as the first argument, and a hardcoded string `\"    <redacted>\"` as the second. The third argument is `4`.\n- If that call returns zero (failure), it jumps to the common exit at `loc_1400130A4`.\n- At the exit (`loc_1400130A4`), it sets the flag at `[X20]` (i.e., `*[X0]`) to 1 *if and only if* the input argument `W1` (original `X1` parameter, likely a status or result code from caller) equals `0xA`. Otherwise, it leaves the flag as-is (possibly zero).\n- Finally, it loads a function pointer from `[X21,#0x20]` (fourth vtable entry), restores the stack and callee-saved registers, and returns by jumping to that function pointer.\n\n**Summary**:  \nThis function attempts to invoke a vtable method (with a redacted string and param 4) *only if* a pre-check flag is clear. Then, based on whether the incoming argument `X1 == 0xA`, it sets the flag (at `[X0]`) to `true`. Finally, it delegates control to another vtable function. Likely part of an initialization or setup routine in a structured error-handling or object-oriented dispatch mechanism."}
{"sub_140002C1C": "This function performs a conditional memory alignment or adjustment based on the second argument (`X1`):\n\n- If `X1 <= 0x10`, it calls `sub_140007AE8` with `X0`, zero-initialized `W1`, and returns immediately.\n- If `X1 > 0x10`, it:\n  - Saves `X1` in `X19`,\n  - Calls `sub_140007AE8` with `X0+X1`, zeroed `W1`,\n  - If the return value `X0` is non-zero, computes a mask-based alignment adjustment:\n    - `X8 = ((X1 - 1) & X0) ^ X1` → effectively computes the next multiple of `X0` ≥ `X1`\n    - Then stores `X0` at `[X8-8]`, sets `X0 = X8`, and returns.\n\n**High-level summary**:  \nThis function attempts to align or reserve memory of size `X1` relative to `X0`, likely implementing an alignment helper (e.g., for allocation or buffer setup), delegating the core logic to `sub_140007AE8`."}
{"sub_1400105B4": "This function performs a conditional copy operation on a structure pointed to by `X0`, updating its contents based on `X1` and `X2`. Specifically:\n\n- It reads two 64-bit values (`base` at `[X0]` and `size` at `[X0+0x10]`) from a header.\n- Computes `range = size - base`.\n- If `X2` (a length) exceeds `range`, it calls `sub_140016B04` (likely a bounds-checking or validation helper), then falls through to copy.\n- If `X20` (i.e., `X2`) is non-zero, it copies `X2` bytes from source `X1 + base` to destination `base`, using `memcpy`.\n- Finally, it stores the updated end pointer (`base + copied_length`) back to `[X0]`, clears return value `W0`, and returns.\n\nIn short: **This function updates a buffer descriptor in-place and copies up to a specified length from a source pointer offset by the buffer’s base, after validating bounds via `sub_140016B04`.**"}
{"sub_140008AFC": "This function parses a null-terminated string (pointed to by `X0`) and determines an output size or type based on its contents and flags (e.g., presence of a `.` or directory separators like `/`/`\\`, and a type byte at offset `0x18`). It likely computes the length of a file extension or a canonicalized path segment, storing the result in the output struct pointed to by `X19` (original `X0`). \n\nKey behavior:\n- Reads a type byte at `[X1+#1]`; if `>1`, jumps to redacted error path.\n- Calls `sub_1400089BC` on input string — likely validates or canonicalizes it.\n- Based on the type byte (`W8`) at `[X20+#0x18]` (input string metadata), selects a size computation path:\n  - Type `0`: extension length = full length + 2  \n  - Type `1`: extension length = full length + 8  \n  - Type `2`: extension length = length + 4  \n  - Type `3`, `4`, `5`: specific behaviors (e.g., `.` handling, backslash handling, or fallbacks)\n- Computes final extension-related value into `W9`, stores into output (`[X19+#8]`), and stores computed lengths/pointers at `[X19]` and `[X19+#0x10]`.\n- Returns, effectively encoding an extension type/length and adjusted string pointer in the output buffer.\n\nShort summary: **Determines file extension properties or normalized segment length from a string, storing result in an output struct; error path triggers on invalid type or overflow.**"}
{"sub_140003ABC": "This function implements a dispatcher based on the two lowest bits of a value loaded from `[X0]`. It inspects `*[X0] & 3`:\n\n- **If the low bits == 1**:  \n  It jumps to `loc_140003B70` (not shown), likely handling a specific case (e.g., a tagged pointer or variant). *(Note: Block at 0x140003adc uses CBNZ to branch to that unknown location if `X9 != 0`, i.e., low bits = 1.)*\n\n- **If the low bits == 0**:  \n  It continues to `loc_140003AE0`, where it loads two values from `[X8+8]` (i.e., `X8 = *[X0]`, then `X0 = X8+8[0]`, `X8 = X8+8[1]`), prepares arguments, and **tailscalls** `sub_140012B6C`.\n\n- **If the low bits == 2**:  \n  It falls through to `loc_140003B04`, where it:\n  - Extracts upper 32 bits (`X8 >> 32`) into `X0`.\n  - Allocates a local struct at `var_48`.\n  - Calls `sub_140004580`.\n  - Then calls `sub_1400124BC` with several local arguments, including possibly a format string from `sub_140003BBC`, a function pointer from `sub_140013BB0`, and data buffers.\n  - Based on a later load (`[SP+var_38]`), possibly a result or flag, it may call `sub_140001040(1)` (a release/destructor?), then returns the original `X0` value.\n\n- **If the low bits == 3** (falls through from `B.GT loc_140003AFC`):  \n  Calls `sub_140014EB0` with a byte extracted from upper bits (`[X8 >> 33]`), masked by `0xFF` or `-1` depending on carry. Likely handles an error or extended variant.\n\n**Summary**: A polymorphic dispatcher that branches based on a 2-bit tag in `*[X0]`, handling variants for 0 (recursive tailcall), 1 (unknown tag), 2 (structured parsing/processing via locals + calls), and 3 (error or extended handling). Commonly seen in object method dispatch, deserialization, or type-tagged union handling."}
{"sub_140004580": "This function is a **structured exception handling (SEH) helper**, specifically preparing and invoking `FormatMessageW` to format an error message, likely for an exception or failure path, then handling or logging the result.\n\n### High-Level Summary:\n1. **Allocates stack space**, initializes part of it to zero via `memset`.\n2. Checks a flag in `W0` (bit 28, `0x10000000`):\n   - If **set**, clears that bit (`AND W20, #0xEFFFFFFF`) and sets `W0 = 0x1A00` (format flags: `FORMAT_MESSAGE_FROM_SYSTEM | FORMAT_MESSAGE_IGNORE_INSERTS`).\n   - If **clear**, calls `GetModuleHandleW` to get a module handle (likely for `FormatMessageW` source), uses it as input, sets `W0 = 0x1200` (`FORMAT_MESSAGE_FROM_HMODULE | FORMAT_MESSAGE_IGNORE_INSERTS`).\n3. Calls `FormatMessageW` with:\n   - Flags from `W20`,\n   - Optional module handle (if used),\n   - Buffer at `[SP+var_10]`,\n   - Buffer size `0x800`.\n4. If `FormatMessageW` fails (`W0 == 0`):\n   - Calls `GetLastError`,\n   - Stores `GetLastError` result and a function address (`sub_140013BB0`, likely a logging/handling routine) in stack slots,\n   - Branches to `sub_140010274` (likely a structured exception registration or error reporting function).\n5. If `FormatMessageW` succeeds:\n   - Calls `sub_14001064C`, possibly to validate or adjust the buffer,\n   - Tries to read a pointer pair `[X21]`, checks if `X8` (likely status) is `-1` (error),\n   - Processes string data: iterates backwards from end of buffer to trim trailing spaces, possibly normalizes case or does some pattern matching — but paths lead to a `BRK #1` (debug breakpoint), indicating this is **unreachable or invalid** in normal operation.\n\nThe presence of `FormatMessageW`, `GetLastError`, `GetModuleHandleW`, and the error-handling branches strongly indicate this is part of a Windows error-reporting or exception-handling routine — **likely a fallback error message formatter before triggering crash reporting or logging**.\n\nThe final `BRK #1` in the error paths suggests this code is either debug code or triggers only in unexpected conditions."}
{"sub_14000C090": "This function appears to be a **reversible parser or tokenizer for a quoted string literal**, likely handling escaped quotes (e.g., `\\\"`) inside a C-style string (delimited by single quotes `'`).\n\n### High-level behavior:\n1. **Input validation**: Checks if the input pointer (`X0`) is non-null; returns `0` if null.\n2. **Initial scan** for the closing `'`:\n   - Calls `sub_1400127D4`, presumably a helper to scan the next character or match a delimiter — likely scans until `'` or end.\n   - If the delimiter is found and not an escaped quote, returns `1` (success).\n3. **Escape handling** (for `\\\"` inside a `'...'` string):\n   - If an escaped quote is detected (e.g., `\\'` or `\\\"`? — but code uses `W20, #0x22` where `0x22 = '\"'`, and compares with `0x27 = '''`), loops scanning until end or unescaped `'`.\n   - Involves stack-allocated buffer (`SP`) and likely calls `sub_14000CB1C`, possibly for reading/parsing escape sequences or character validation.\n   - Compares byte values (`W23`, `W22`) — likely tracking current vs. previous/next character positions.\n4. **Special case for `CMN W1, #1` → EQ to `loc_14000C180`**: this path directly calls `sub_1400127D4` with `'` (0x27), suggesting the input ended exactly at an unescaped closing quote — success.\n5. Returns:\n   - `0` on error/failure (e.g., null input, malformed string),\n   - `1` on success (closing quote found/escaped sequence handled correctly).\n\n### Likely purpose:\n- **Parsing a single-quoted character literal or string**, including support for escape sequences (e.g., in a lexer/compiler frontend or configuration parser).\n- Uses `sub_1400127D4` as a utility for character scanning/matching, and `sub_14000CB1C` possibly for buffer initialization or escape-sequence decoding.\n\nKey clues:  \n- `0x27 = '''`, `0x22 = '\"'` used in comparisons.  \n- Stack-based state machine, branching on byte values.  \n- Return `0/1` like a predicate for valid string termination."}
{"sub_140005424": "This function implements a conditional buffer copy or memory operation, likely part of a string or data processing routine.\n\n**Summary:**\n- Takes a struct pointer (`X0`) and a count (`W1`).\n- If bit 0 of `W1` is clear, it calls `sub_140007260` — likely a cleanup/return or alternate path.\n- Otherwise, it reads two 64-bit values from the struct (at offsets `+0` and `+0x10`), treats them as a pointer range, and computes a length (`X8 = [0x10] - [0]`, `X20 = W1 >> 1`).\n- Compares `X20` (half of `W1`) to that length. If `X20 > length`, it branches to `loc_14000549C`.\n- If `X20 == 0`, skips copy and updates struct with new pointer/end.\n- Else, calls `memcpy` to copy `X20` bytes starting from `base+[0]` to destination `X2`.\n- In the overflow branch (`B.HI`), it calls `sub_140016758` — possibly a safe copy or bounds-checking helper — then loops back to `loc_14000546C` to retry or adjust.\n\nEffectively, it performs a conditional safe copy based on a half-size count and internal buffer metadata, likely handling overflow via a helper routine."}
{"sub_1400098F8": "This function parses and validates a UUID-like string (or similar binary/ASCII identifier) and performs structured error-checking, parsing, and validation logic, likely part of a diagnostic or verification routine.  \n\nKey observations:\n\n- **Stack setup & prologue**: Saves callee-saved registers (`X19`–`X21`, `X29`, `X30`) and allocates local stack space. Arguments: `X0`/`X1` (likely buffer & length), `X8` stored to `X21`.\n- **Initial call**: Invokes `sub_140014054` with `W3 = 6`, suggesting initial setup or header parsing (possibly a 6-byte header check).\n- **Branch on bit 0 of `[SP+var_A0]`**: Controls path selection—either enters validation or jumps to alternate parsing branch.\n- **Core validation logic**:\n  - Compares string contents with magic constants (`0x4E5A`, `0x5A5F`, `0x4E5A5F5F`, etc.), suggesting detection of format signatures (e.g., big-endian markers like `NZ__`, `NZ_`, `NZ`).\n  - Contains extensive UTF-8 decoding logic (`AND 0x3F`, `ORR`, `BFI`) to decode multi-byte UTF-8 characters into scalars and validate them (e.g., checks for `'E'`, digits, letters).\n  - Repeatedly compares decoded characters against ranges for alphanumeric or punctuation validation (e.g., `'0'–'9'`, `'A'–'Z'`, `'!'–'/'`, `':'–'&'`).\n  - Includes a loop that processes input byte-by-byte, advancing pointers and counting matches.\n- **Failure paths**: Multiple branches trigger diagnostic calls (`sub_140016C2C`, `sub_140016C8C`) with `<redacted>` message and `BRK #1`, indicating intentional soft-breaks for debugging/tracing on validation errors.\n- **Final setup**: Stores structured results to memory at `[X21]`, likely returning parsed fields (e.g., length, offset, validity flags) in a struct-like layout before epilogue.\n\nIn summary:  \n**This function validates a structured binary/ASCII UUID-like identifier string, parsing and checking format and content (UTF-8, alphanumerics), branching on header signatures, and breaking on validation errors.**"}
{"_ZN4core3str8converts9from_utf817he763c5b9963ab195E": "This function implements `core::str::from_utf8`, which validates that a byte slice is valid UTF-8 and returns a `&str` (represented here as a pointer + length pair written to an output location, likely via a result struct at `[X8]`).  \n\nKey observations:  \n- It performs early exit on empty input (`X1 == 0`).  \n- It uses vectorized SIMD-style checks (e.g., `LDP`, `ORR`, `TST #0x8080...`) to quickly scan 16-byte chunks for high bits (potential multibyte sequences), falling back to byte-by-byte validation for problematic regions.  \n- Validates UTF-8 byte sequences by inspecting the leading byte's format (`TBNZ #31` checks for continuation bytes), then verifies correct trailing byte counts (2, 3, or 4 bytes), and enforces overlong/illegal codepoint restrictions (e.g., rejecting `0xED A0..BF`, `0xF4 90..BF`, `0xF0 .. <0x90` or `>0xF4`).  \n- On error, it stores an error position (`X9`) and error info (`W13`, `W14`) to the result struct at `[X8]`; on success, it returns the original slice as a valid UTF-8 string.  \n\nThe function is a standard, optimized UTF-8 validation routine with hand-rolled vectorized fallback."}
{"sub_1400024DC": "This function appears to be a **heap memory allocator or reallocator**, likely implementing part of a **suballocator** or **pool allocator**, possibly for aligned blocks of size 0x18 with 8-byte alignment.\n\n### High-level behavior:\n- **Initial check**: If `*(*(obj+0x10) + 0) == -1` (i.e., a sentinel or invalid state), it jumps to a fast-path initialization code (not shown here) — otherwise proceeds.\n- **Conditional logic** based on bits in a flags word (`W2` from a vtable or heap metadata):\n  - **If bit 0 set**: Calls `sub_1400124BC` (likely a lookup or setup function) and then stores a 16-byte structure (likely a pair: pointer + size or two 8-byte fields) to the output buffer.\n  - **If bit 0 clear but higher bits set (i.e., X8 ≠ 0)**: Proceeds to compute `X8 = flags >> 1`, uses it in a size/calculation, then stores the same structure.\n- Stores result to `[X0]` (input buffer) and increments `X0` (likely returns pointer or updates state).\n- Finally, calls `_guard_check_icall_nop`, then `sub_14000104C` (likely `malloc`/`HeapAlloc`/`VirtualAlloc`), and if that returns zero (`CBZ X0`), hits `BRK #1` (assertion/failure — crash).\n- Otherwise, it calls `sub_140016BE4` (possibly cleanup/hook), then falls into `memcpy`-based copy from internal buffer to final destination.\n- Loop back via `loc_14000253C`.\n\n### Key observations:\n- Operates on a structure at `[X0]`, likely a `size/pointer` pair or a small object header.\n- Uses stack-based temporary storage (`var_20`, `var_30`, etc.).\n- Calls into unknown helpers (`sub_1400124BC`, `sub_14000104C`, `sub_140016BE4`, `sub_140016758`) — consistent with a custom allocator.\n- `memcpy` usage at the end suggests copying payload data into allocated space.\n\n### Summary:\n> **This is a heap allocation helper routine that prepares and allocates a small aligned object (e.g., 0x18 bytes), optionally initializing it, and copies data into the allocated region. It performs validation checks, uses guard calls, and likely serves as a low-level allocator backend (e.g., for a pool or slab allocator).**"}
{"sub_14000B048": "This function (`sub_14000B048`) is a **Unicode validation/decoding routine**, specifically handling UTF-8 to UTF-32 conversion with bounds and validity checks.\n\n### Key Observations:\n1. **Input Structure (`X1`)**:  \n   - Appears to point to a struct containing pointers to:\n     - A byte string (`[X1]` or `[X1,#0]`)\n     - End pointer (`[X1,#8]`)\n     - Current position (`[X1,#0x20]`, `[X1,#0x28]`, `[X1,#0x29]`)\n     - Additional metadata (e.g., flags, buffers at offsets `0x30`, `0x40`, `0x50`, `0x60`).\n\n2. **Control Flow Summary**:\n   - **Initial checks** on a flag byte (`[X1,#0x28]`) and current position vs. end pointer.\n   - If the input byte at current position is `< 0x40`, it branches to a redacted debug call (`sub_140016C2C`/`sub_140016C8C`) → likely a validation failure handler.\n   - Parses leading byte to determine UTF-8 sequence length (1–4 bytes) based on bit patterns (`0xE0`, `0xF0`, etc.).\n   - Accumulates continuation bytes (masked with `0x3F`, shifted) into a 32-bit codepoint (`W8`).\n   - Validates resulting codepoint against Unicode ranges (e.g., rejects surrogate halves, overlong encodings).\n   - Stores result (valid codepoint + length or error code) at output location (`X0`, `[X0]`, `[X0,#8]`, `[X1,#0x20]`).\n\n3. **Error Paths**:\n   - Overlong sequences, invalid bytes (`< 0x80`), buffer overruns, or invalid codepoints (e.g., surrogates) trigger:\n     - `ADRL ... off_140018830` (redacted string, likely `<redacted>`)\n     - Call to `sub_140016C2C` or `sub_140016C8C`\n     - `BRK #1` (debug break).\n\n4. **Output**:\n   - Returns in `W0`/`X0` a success/failure indicator (`W9 = 1` or `2`), and stores decoded length and codepoint in memory pointed to by `X0`.\n\n### Conclusion:\nThis is a **UTF-8 decoder** with robust error handling, likely part of a larger Unicode processing pipeline (e.g., JSON parser, text normalization). It decodes the next UTF-8 character from a stream, validates it, and writes results to a caller-provided struct."}
{"sub_140003F24": "This function parses a UTF-8 string slice (`&str`) and attempts to decode it as a signed integer (likely 64-bit), returning either the parsed value or an error code.\n\n### Summary:\n- **Input**: `X0` (result pointer), `X1` (UTF-8 string slice ptr — likely `&str` struct with `.ptr` and `.len`), `X2` (some context/auxiliary struct, possibly `core::str::ParseIntError` or similar).\n- **Key operations**:\n  1. Allocates stack frame and saves callee-saved registers.\n  2. Extracts `.ptr` and `.len` from the `&str` (`X22`, `X23`) and loads something from `X1+0x18` (possibly error metadata) into `X19`.\n  3. Calls `core::str::from_utf8` — indicating `X2` points to a byte slice (likely `[u8]`) being converted to `&str`. On success (`X8 == 1`), proceeds; otherwise (e.g., invalid UTF-8), sets error code `-2` (`0xFFFFFFFFFFFFFFFE`) and exits.\n  4. In the success path, the function appears to process the resulting UTF-8 string (possibly re-layout stack data, prepare for parsing).\n  5. At the end, calls `sub_140003BD0`, likely a standard library function for parsing a string into a signed integer (e.g., `std::str::parse_int_radix` or similar), passing the original `&str` and context.\n  6. On successful parse (implied by `X9 == 1` check), stores the parsed value (via `X8` loaded before `CMP X9, #1`) into the output location. On error (non-1), stores `-1` (`0xFFFFFFFFFFFFFFFF`) as error sentinel.\n  7. Restores registers and returns.\n\n### Conclusion:\nThis is a **UTF-8-safe integer parser**, likely the Rust `str::parse::<i64>()` entry point or a closely related helper — converting a string to a signed integer, with error handling for invalid UTF-8 and invalid numeric syntax."}
{"sub_14001266C": "This function acts as a conditional wrapper that:\n\n1. **Checks if `arg2 == -1`** (via `CMN W2, #1`; if true, jumps to `loc_1400126B0`).  \n   - If `arg2 == -1`, it jumps directly to the exit path.\n2. **Otherwise**, it loads a function pointer from `arg1+0x20`, calls it with `(arg0, arg2)`, and:\n   - If the return value is odd (bit 0 set), returns `1`.\n   - If even, falls through to the exit.\n3. **At `loc_1400126B0`**, if `arg3 == 0`, returns `0`.\n4. **All paths converge** to return `1` (if successful call returned odd), or `0` (if `arg3 == 0` or `arg2 == -1` and `arg3 != 0` — but wait: actually, if `arg2 == -1`, it goes to `loc_1400126B0`, which returns `0` only if `arg3 == 0`; otherwise, falls through to return `1`).\n\nActually, correction:  \n- `CMN W2, #1` → sets Z=1 if W2 == -1 → B.EQ jumps to `loc_1400126B0`  \n- At `loc_1400126B0`: `CBZ X20, loc_1400126D4` → if `arg3 == 0`, set ret=0; else fall through to ret=1  \n- So return semantics:\n  - `arg2 == -1` ⇒ return `arg3 != 0`\n  - `arg2 != -1` ⇒ call `*(arg1+0x20)(arg0, arg2)`, return 1 if result odd, else 0 (i.e., `(ret & 1) != 0`)\n  - Exit always sets `W0` to 0 or 1, then restores registers and returns\n\n**In short**: It conditionally calls a function pointer (if `arg2 != -1`) and returns whether the low bit of the result is set; if `arg2 == -1`, it returns `arg3 != 0`."}
{"sub_140007D78": "This function performs a **smart pointer or reference-counted object cleanup/decay operation**, likely related to a custom smart pointer type (e.g., `intrusive_ptr`, `weak_ptr`, or similar), and returns a boolean indicating whether the managed resource is still valid.\n\n### High-level summary:\n- Takes a pointer to a struct (in `X0`) that presumably contains a managed object reference in its first two fields (`[X0]` and `[X0,#8]`).\n- Calls an initial function `sub_140005F00` (possibly a decay/weaken or reference-count decrement function) on the managed object.\n- If the initial call returns zero (resource invalid), it jumps to exit with `0`.\n- Otherwise, it inspects the lower 2 bits of the pointer at `[X0,#8]` (likely encoding flags or type info).\n- If those bits indicate a specific tagged value (`== 2` or `== 3` based on the `SUB X10, X9, #2; CMP X10, #2; B.CC` check), or if untagged (`X9 == 0`), it again jumps to exit (setting the output to 0).\n- If the pointer passes validation, it extracts:\n  - `X23`: pointer to some internal data structure at `ptr+0xF` (sign-extended via `LDUR` — note offset `0xF`, unusual — possibly misaligned read or typo in analysis?),\n  - `X21`: value at `ptr+7`,\n  - Then reads a function pointer from `[X23]`, and calls it with `X21` as argument — likely a destructor or release hook.\n- Then, if `[X23,#8]` (second function pointer in the vtable-like structure) is non-zero, it calls `sub_140001040` with `X21` and two other arguments.\n- Then calls `sub_140001040` again with `ptr-1`, size `0x18`, and block size `8` — possibly freeing or reinitializing memory.\n- Finally, stores the resulting value back into `[X19,#8]`, and returns `X0 != 0`.\n\n### Likely purpose:\nDecay or invalidate a smart pointer — decrement reference count, call cleanup hooks, possibly deallocate or transition from strong to weak — returning true if the resource remains valid after operation.\n\nThis is consistent with a custom smart pointer’s `weak_from_this`/`decay`/`try_lock`/`unlock` pattern or similar, likely part of a homegrown RAII system."}
{"sub_14001064C": "This function decodes UTF-16 encoded input (pointed to by `X1`) into UTF-8 and writes the result to a buffer (likely provided by caller via `X0`), using a standard UTF-8 encoding algorithm with surrogate pair handling.\n\n- **Input**: `X0` = output buffer pointer (possibly from caller), `X1` = source UTF-16 data pointer, `X8` = some control/context value (saved as `var_70`).\n- **Behavior**:\n  - Checks if `X1` is zero → early exit with success.\n  - Calls `_guard_check_icall_nop` (control flow guard).\n  - Calls `sub_14000104C`, likely to validate or prepare a destination buffer; if it fails (`X0 == 0`), triggers `BRK` (debugger break / error).\n  - Main loop reads 16-bit UTF-16 code units:\n    - Detects surrogate pairs (`0xD800–0xDFFF`) and combines high/low surrogates into a 21-bit codepoint.\n    - Encodes codepoints into UTF-8 (1–4 bytes) based on value:\n      - ASCII (< 0x80): 1 byte.\n      - 0x80–0x7FF: 2 bytes.\n      - 0x800–0xFFFF: 3 bytes.\n      - Surrogate-derived (> 0xFFFF): 4 bytes.\n    - Uses `sub_140016B04` or `sub_140016BA4` for buffer overflow checks or writes (likely helper for safe string operations).\n  - Writes output length (in bytes) and null terminator or length info to output structure (at `X13` + offsets).\n- **Outputs** (on success at `0x140010860`):\n  - `Q0` (128-bit) = possibly `{utf8_buffer_ptr, length}` or `{length, reserved}` written to `[X13]`.\n  - `X8` = buffer length stored at `[X13+0x10]`.\n- **Error paths**:\n  - Surrogate validation failures (e.g., incomplete/invalid surrogate pairs) → sets error flag (`X8 = -1`, stores 0 byte, or breaks).\n  - Buffer issues → `BRK`.\n\nIn short: **UTF-16 to UTF-8 decoder with error handling and structured output**."}
{"sub_140014CC4": "This function implements a **UTF-8 decoder/validator** that processes a byte sequence (likely from a string) and advances a pointer while validating/decoding UTF-8 characters.\n\n### Key observations:\n- **Input**: `X0` = start of buffer (likely byte pointer), `X1` = end pointer, `X2` = context/state struct (contains function pointers at offsets `+8` and `+16`, and possibly more at `+0x18`, `+0x20`).\n- **Core loop**: Reads bytes (`LDRSB`), checks high-bit / UTF-8 lead-byte patterns (e.g., `0xE0`, `0xED`, `0xF0`), and adjusts pointer advance by 1–4 bytes based on UTF-8 rules.\n- **Validation checks**:\n  - Ensures continuation bytes are in range (`0x80–0xBF` implied by branching logic).\n  - Rejects overlong encodings (e.g., `0xED` followed by illegal high bytes).\n  - Enforces bounds (compares pointer against `X22`, which is `X0 + X1` initially).\n- **Callback/State Handling**: Calls functions via function pointers loaded from `X2`’s struct (possibly for error handling or character processing). Returns early with `W0=1` on failure, or continue processing.\n- **Output**: Advances input pointer (`X19` tracks current position), and if the entire range is successfully decoded, calls `sub_140011FA4` (likely a downstream handler).\n\n### Summary:\n> A UTF-8 character parser/validator that walks a byte range, decodes multi-byte sequences, validates UTF-8 rules, invokes callbacks on errors/decoding, and proceeds until end-of-input or error."}
{"sub_14000A190": "This function performs **UTF-8 string validation and optional decoding/conversion**, likely part of a Unicode or string parsing utility (e.g., `strtol`-like or UTF-8 validation routine). Key behaviors:\n\n- Parses a UTF-8 string starting at `X0+8` (or `X0`), verifying well-formedness of multi-byte sequences.\n- Handles prefixes like `+`, `-`, and possibly base-16 prefixes (`0x`/`0X`) or UTF-16 surrogate checks (indicated by `0xD800`, `\\u` hex parsing, and `var_74` storing `0xFFEF0000 | 0x800`, possibly a mask for UTF-16 validation).\n- Supports multiple number base parsing (likely octal, decimal, hexadecimal) based on content and prefix (e.g., `'h'` suffix triggers hex decoding).\n- Performs character-by-character validation, decoding UTF-8 code points (1–4 bytes), ensuring continuation bytes match `0x80–0xBF`, and computes scalar values.\n- Distinguishes between ASCII digits, hex digits (`A-F`, `a-f`), and uses `CMN #0x40/0x41` to classify letters (`'@'`, `'A'`).\n- Checks for `\"...\"` (dot/period), `\"$\"` (likely a special token), and specific 2-byte signatures like `\"SP\"`, `\"BP\"`, `\"RF\"`, `\"LT\"`, `\"GT\"`, `\"PL\"`, `\"PR\"` (possibly LLVM IR keywords or format specifiers).\n- On error or unsupported pattern, calls `sub_140016C2C/3C/48` — likely error-reporting functions (given repeated `BRK #1`), with redacted message strings (`off_140018830`).\n- Returns success/failure flag in `W0` (via `W20`).\n- Preserves ABI by saving/restoring callee-saved registers (`X19–X28`, `X29`, `X30`) and aligning stack.\n\n**Conclusion**: A robust UTF-8 string validator/decoder, possibly used to parse integer literals or special tokens in a language runtime or compiler backend (e.g., LLVM-based)."}
{"sub_14000CB1C": "This function (`sub_14000CB1C`) processes a character (passed in `W1`) and writes a formatted escape sequence (e.g., `\\n`, `\\t`, `\\\\`, `\\\"`, `\\'`, `\\0`, `\\r`, or a Unicode escape like `\\uXXXX`) into the buffer pointed to by `X0`. It returns early for common escape characters (like tab, newline, carriage return, quotes, backslash, null) by storing short escape strings (2 bytes), and for rarer cases (e.g., non-printable/control characters) it falls back to a generic `\\uXXXX` representation using a lookup table (`unk_140018AE8`) and bit manipulation to extract nibbles from the input character.\n\n### Summary:\n**Writes an escape sequence representation of the input character (in `W1`) into the buffer at `X0`.**  \n- If `W1` is 0, '\\t', '\\n', '\\r', '\"', '''', '\\\\', or in range [0, 12] → writes short escape like `\\0`, `\\t`, `\\n`, etc.  \n- Else if `W1` ≤ 0x2FF → calls `sub_1400150A4` (likely validates or normalizes); on failure writes `\"{\" + char + \"}\"` (fallback).  \n- Else → calls `sub_140014940` (likely a UTF-8/Unicode validation/decoding helper), then either writes fallback `\"{\" + char + \"}\"` or constructs `\\uXXXX`.  \n\nMost logic centers on emitting standard C-style escape sequences or Unicode escapes."}
{"sub_140005F00": "This function implements a **custom string parsing or validation routine**, likely for a **directive or comment starting with `#`**, with logic to handle misaligned pointers or invalid inputs by calling error handlers or debug traps.\n\n### High-Level Summary:\n- Accepts 3 arguments: `X0`, `X1` (likely buffer/pointer + size/offset), `X2` (possibly another pointer or flag).\n- If `X2 == 0`, jumps to early exit (`loc_140006004`, returns 0).\n- Otherwise, prepares and calls `sub_140005768` (likely a validation or setup function) with inputs `W0 = 0xFFFFFFF4`, and original args (`X0`, `X1`, `X2`).\n- Checks alignment of `X1` (`X1 & 3`) and branches accordingly:\n  - For misaligned pointers (`%4 != 0`), reads bytes at offsets (`-1`, `0`, or `+2` via `UBFX`) and checks if byte == `'#'` (0x23).\n  - If `'#'` found, proceeds to load and call functions from a structure pointed to by `[X1+0xF]`.\n- Performs secondary allocations/calls via `sub_140001040` (likely malloc-like).\n- On success, returns `0`; on error or invalid alignment, triggers `BRK #1` (debug trap) with a redacted error message.\n\n### Likely Purpose:\nIt parses or processes a special `#`-prefixed token or directive in a structured way (e.g., like preprocessor directives or metadata), verifying alignment and calling structured callbacks. This resembles handling of **assembly macros, symbol definitions, or debug directives**.\n\n### ABI/Control Flow Notes:\n- Saves callee-saved registers (`X19–X25`, `X29`, `X30`).\n- Uses a stack frame of 0x50 bytes.\n- Several early exits on `X1 == 0` or alignment/size failures.\n- One path leads to `BRK #1`, suggesting this is debug-enabled or assert-like behavior."}
{"sub_1400127E0": "This function performs **UTF-8 string parsing and validation**, with a focus on handling escape sequences and verifying code point validity. It takes a string (likely in `X0`) and a context pointer (`X2`), and returns 0 or 1 in `W0`.\n\n### High-level behavior:\n- Allocates a stack frame and saves callee-saved registers.\n- Calls a function (via `X20 = *(context+0x20)`) to check some condition (likely for quoting or special handling), passing `'\\\"'` as argument.\n- If that call succeeds (non-zero), jumps to error handling at `loc_140012AE8`.\n- Parses the string (pointed to by `X23` = `X0`) character-by-character, checking for:\n  - Valid UTF-8 sequences (1–4 bytes), decoding them to code points.\n  - Escape sequences: backslash (`\\`), double-quote (`\"`), or high-bit characters (≥0x80).\n- Validates decoded code points against expected ranges.\n- Performs additional checks using a lookup table (`[X9, X22]` / `[X23, X22]`, where `X23` is likely a signed-byte array), possibly for escape escape or control character handling.\n- On success: returns 0; on failure: triggers break (`BRK #1`) after calling a redaction/error-reporting helper (`sub_140016C2C`), suggesting this function is strict and aborts on malformed input.\n\n### Likely purpose:\nA **safe UTF-8 string parser/validator**, possibly part of a JSON or text-processing subsystem — likely used to verify or parse quoted strings with escape support. The repeated use of `BLR X25`/`X20` and conditional aborts implies integration with a larger parsing framework."}
{"sub_140002C7C": "This function frees a heap block via `HeapFree`. It first checks if the third argument (`X2`) is at least `0x11`; if not, it falls through to load a pointer from `X19-8` (likely retrieving a valid heap block address from a structure), otherwise it uses `X19` directly. It then calls `GetProcessHeap` to obtain the process heap handle, and finally calls `HeapFree` with that handle, zero flags (`W1`), and the block address (`X2`/`X19`). The function restores the stack and callee-saved register `X19` before returning via `HeapFree`’s return."}
{"sub_140015438": "This function implements the C runtime initialization and startup logic, likely `_mainCRTStartup` or similar. It:\n\n1. Initializes the CRT via `__scrt_initialize_crt`.\n2. Acquires a startup lock to prevent race conditions during initialization.\n3. Uses a global flag (`dword_14001D1C8`) to ensure initialization occurs only once:\n   - If not yet initialized:\n     - Runs `_initterm_e` and `_initterm` to call initialization functions.\n     - Sets up initialization state (flag = 1/2).\n     - Registers TLS callbacks and initializes arguments/environment.\n     - Calls user entry point (`sub_140001000`, likely `main`/`WinMain`).\n     - On success, uninitializes CRT and returns the exit code.\n   - If already initializing (recursive) or initialized:\n     - Jumps to cleanup or returns early.\n4. Handles error cases (e.g., failed initialization) by returning exit code 7 via `sub_140015AE0`.\n5. Properly releases the startup lock and restores the stack frame before returning.\n\nKey behaviors: single initialization, thread-safe startup, CRT teardown, and exit-code propagation."}
{"sub_140016654": "This function compares a 4-byte pattern (specified by a bitmask in `W2`) at an offset (`X1`) from a base pointer (`X0`) against the data at that location in a buffer, where the buffer is structured as a header containing a pointer and a 32-bit count (likely length), followed by raw data. \n\n- It first checks if `W2` is zero or misaligned (bit 0 clear → early return 0).\n- If the count in the header (`X8`) is ≤3, it does a manual byte-by-byte comparison using `X9` as the base of the pattern data (loaded from `[X0,#0x10]`) and `X10 + X12` as the data location (computed via leading-zero count on the masked `W2` and pointer from `[X0]`).\n- If the count is ≥4, it falls into a loop comparing 4 bytes at a time using load-register and compare instructions, updating the pointer with a stride and looping until the end.\n\nReturns `1` (in `W0`) if the masked data matches the pattern; `0` otherwise.\n\nIn short: **A pattern-matching routine comparing a masked 32-bit value (with bit position derived from leading zeros) against a buffer’s data, with optimized 4-byte comparison for larger payloads.**"}
{"sub_14001613C": "This function implements a lock acquisition routine with special handling for lock states encoded in a 32-bit word (likely a spinlock or futex-style construct), possibly part of a runtime or threading library. It checks if the lock is unlocked (`0x0`), already locked exclusively (`0x80000000`), or lockedShared (`0x40000000`), and performs atomic compare-and-exchange operations to acquire the lock.\n\n- If the lock is currently unlocked (`state & 0x3FFFFFFF == 0`), it attempts to atomically set it to `0x80000000` (exclusive lock).\n- If the current state is `0xC0000000` (exclusive lock held by another thread), it retries acquiring using `LDXR/STXR` in a loop until successful.\n- If it successfully acquires an exclusive lock (previously unlocked), it calls `WakeByAddressSingle` to wake waiters.\n- If it transitions from `0xC0000000` (exclusive) to `0x40000000` (shared) — indicating downgrade — it calls `WakeByAddressAll` to wake all waiters.\n- Asserts on invalid states (e.g., unexpected lock value), triggering a debug breakpoint.\n\nIn short: **a lock acquire function with support for exclusive-to-shared downgrade and conditional wakeups**."}
{"sub_1400064C0": "This function is a **debug symbol initialization routine**, likely for handling crash backtraces using `dbghelp.dll` on Windows.\n\n### High-level Summary:\n1. **Single-instance enforcement via mutex**:\n   - Checks if another instance is already running (via `qword_14001D130`), and if not, waits on a named mutex (`Local\\RustBacktraceMutex...`) to ensure only one backtrace handler runs at a time.\n   - Creates/uses a mutex to serialize execution.\n\n2. **Lazy loading and setup of `dbghelp.dll`**:\n   - Dynamically loads `dbghelp.dll` via `LoadLibraryA` if not already loaded.\n   - Resolves key symbols: `SymGetOptions`, `SymSetOptions`, `SymInitializeW`, `SymGetSearchPathW`, `EnumerateLoadedModulesW64`, `SymSetSearchPathW`.\n\n3. **Initializes symbol handling**:\n   - Calls `SymSetOptions(SymGetOptions() | 0x4)` (enabling `SYMOPT_DEFERRED_ADDS` likely).\n   - Calls `SymInitializeW` for the current process.\n   - Sets up search path for symbols.\n\n4. **Dump or prepare stack trace**:\n   - Calls `GetCurrentProcess()`, then likely invokes `SymInitializeW` or related functions to gather stack trace info.\n   - Uses `sub_140016758` (likely a helper to write minidump or fill buffer), then processes results.\n\n5. **Cleanup and return**:\n   - Releases the mutex, closes handles, restores registers, and returns.\n   - Returns `0` on success (symbol setup complete) or `1` (e.g., if mutexed or error path taken).\n\n6. **Error paths**:\n   - Fails to load `dbghelp.dll` → returns `1`.\n   - Internal assert/check failures (`len >= 0`) → triggers `BRK #1`.\n   - Mutex or symbol resolution failures lead to early exits with `1`.\n\n### Key Observations:\n- **Purpose**: Initialize debugging/symbol support (likely in a Rust panic/unwind/handler context).\n- **Thread-safe**: Uses mutex and atomic compare-exchange on `qword_14001D130`.\n- **Robust error handling**: Gracefully degrades or fails cleanly on missing dependencies.\n- **Crash-sensitive**: Uses `BRK` on asserts—likely runs in a crash path where a clean exit isn’t expected.\n\nIn short: **This is a thread-safe, lazy-initialized Windows debug symbol setup function, likely invoked during crash backtrace generation.**"}
{"sub_140004308": "This function appears to perform a conditional operation based on the value at `[X0+0x18]`, likely checking whether a pointer or count is zero or one. It prepares arguments and calls `sub_14000438C`, then conditionally branches to either `sub_140001040` (if `[X0+0x18] == 1`) or directly to epilogue.\n\n**Key behavior summary**:\n- Sets up stack frame.\n- Loads `[X0+0x18]` into `X20`; checks if it equals `-1` (`CMN X20, #1` → `EQ` if `X20 == -1`).\n- Calls `sub_14000438C` unconditionally (possibly a helper for handling the first case, e.g., copy/initialization).\n- If `X20 == -1` (`EQ`), jumps to epilogue.\n- Otherwise (i.e., `X20 != -1`), checks if `[X0+0x18] == 0`. If zero, jumps to epilogue.\n- If `[X0+0x18] != 0`, loads `[X0+0x10]`, calls `sub_140001040(X8, ...)`, then returns.\n\n**Interpretation**:\nGiven the structure and the specific checks (for `-1` and `0`), this function likely handles initialization or setup for some structured object, possibly:\n- A dynamic array/vector/list with `-1` meaning uninitialized/error, `0` meaning empty (no elements), and non-zero indicating size/validity needing further processing via `sub_140001040`.\n\nNo clear function name or high-level API semantics without cross-references, but it's a branchy initializer/validator helper."}
{"sub_1400090F4": "This function processes an input structure (passed in `X0`) that appears to represent a token or lexical element (likely in a parser or tokenizer), examining its type (byte at offset `+1`), content (bytes at offsets `+0`, `+2`, `+0x18`, etc.), and internal state (e.g., pointers/fields at offsets `+8`, `+0x28`, `+0x38`). Based on conditional checks on the type and content, it:\n\n- Updates fields in the input structure (`[X0,#8]` stores a pair of pointers/offsets—likely start and length of a substring).\n- Writes result codes to `[X0,#1]` (e.g., 0x1, 0x2, 0x3, 0xFF).\n- Writes status flags to `[X8]` (e.g., 0xFF, 0x9, etc.).\n- Performs various tail-recursive or loop-like processing on substring ranges, handling separators (`/`, `\\`, `.`), checking parity or specific bit flags (via `TBZ`, `CSEL`, `CINC`), and updating working registers (`X19`, `X21`, `X22`, `X27`, etc.) accordingly.\n- In several error or invalid-path cases, it logs redacted messages (via `off_1400187D0`) and triggers a `BRK #1` debug trap.\n\nIn essence, this is a **lexer/parser helper function** that normalizes or canonicalizes a token, handling various cases (e.g., path separators, extensions like `.`, etc.), and updates its internal state/representation accordingly—possibly part of a larger path/URI/token parsing routine."}
{"sub_140003218": "This function implements a **thread-safe reference-counted object initialization and finalization routine**, likely part of a synchronization or module initialization subsystem (e.g., for TLS-based state or static objects). Key behaviors:\n\n- Uses **atomic RMW operations** (`LDAR`, `LDXR`/`STXR`, `LDAXRB`/`STXRB`) to safely increment a counter (`dword_14001D008`) and manage state flags.\n- Checks a global flag (`dword_14001D050`) to short-circuit re-entry or redundant initialization.\n- Retrieves and validates a TLS slot via `TlsIndex` and verifies a cached pointer (`qword_14001D010`)—possibly a vtable or dispatcher.\n- Performs conditional cleanup/initialization: if a retry counter reaches 0 or validation fails, it **triggers a breakpoint (debug fail)** after logging a `<redacted>` message.\n- Implements a **spinlock using `LDXR`/`STXR`** to update `qword_14001D0F8`, likely an active instance or state pointer.\n- Invokes `sub_140007070` (likely a guard/validation routine) and further cleanup/constructor/destructor calls via `sub_140001040`.\n- On successful completion, decrements the counter and, if nonzero, may wake waiters (`WakeByAddressSingle`).\n- Falls back to `BRK #1` (debug trap) on unexpected error paths.\n\nIn short: **This is a guarded, thread-safe initialization/finalization routine for a module or object, with retry logic, TLS handling, and fatal error traps.**"}
{"sub_140002A40": "This function (`sub_140002A40`) implements a thread-local counter increment operation using atomic primitives (`LDXR`/`STXR`), possibly as part of reference counting or thread ID bookkeeping. It:\n\n- Validates input `X0` (which must point to a struct with specific layout).\n- Loads and compares TLS-related data to check for specific conditions.\n- Atomically increments a counter (likely per-thread) located in TLS.\n- On success, calls `sub_1400074D0` — likely a dispatcher or hook registration function — and processes its result.\n- If successful, extracts function pointers and context from the returned structure and calls them (destructor + setup function).\n- Cleans up the stack and returns.\n\nIf the atomic update loop detects an unexpected condition (e.g., counter underflow or invalid state at `0x140002b30`), it triggers a breakpoint (`BRK #1`) via `sub_140016484`, indicating a failure path or assertion.\n\nFunction appears to be a low-level initialization or resource acquisition helper, possibly related to CRT startup, thread-local storage setup, or exception handling registration (given use of TLS and structured cleanup)."}
{"sub_140001720": "This function implements a **non-blocking atomic test-and-set operation**, likely used for lock acquisition or signaling.\n\n- **Input**: `X0` points to a memory location (e.g., a lock flag).\n- **Logic**:\n  - It attempts to atomically store the value `1` at the target address using `STXRB` (exclusive store).\n  - If the location was already non-zero (checked via `LDAXRB`), it calls `sub_1400164A8` (likely a slow-path handler, e.g., spin-wait or futex syscall) and then returns the original value.\n  - If the store succeeds (zero → one), it returns `0`.\n- **Return value**:\n  - `X0 = 0` if the bit was successfully set (lock acquired).\n  - Non-zero (original value) if already set (lock contested; original value returned after slow path).\n\nThis is consistent with a low-level `atomic_test_and_set` or `spinlock_trylock` primitive."}
{"sub_140013304": "This function computes a parity-like or population-count-like result over an input buffer of `X1` bytes pointed to by `X9`, with additional preloaded vector registers used conditionally depending on buffer size:\n\n- If `X1 == 0`, return 0.\n- If `X1 > 0x20`, do a loop (0x140013594) that increments `X8` for each byte ≥ 0x41 (i.e., 'A'), via `LDRSB`, `CMN #0x41`, and `CINC GT`.\n- For sizes in [8, 0x20], it performs a vectorized reduction using additions and `ADDP` on loaded SIMD registers (`V0`–`V6`, `V16`–`V24`), ultimately moving the scalar result into `X8`.\n- Returns `X8`.\n\nIn short: **It counts how many bytes in the buffer are ≥ 'A', but uses vectorized reduction for medium-sized inputs (8–32 bytes), and falls back to scalar iteration otherwise.**"}
{"sub_14000B478": "This function encodes a Unicode codepoint (in `W1`) into UTF-8 bytes stored in a local buffer (at `[X29,#var_4]`), adjusts the write pointer of a buffer (in `X0`), and returns a status code (1 on success, possibly 0 on error or overflow). It handles 1–4 byte UTF-8 sequences based on input range:\n\n- If `W1 >= 0x80`: encodes as multi-byte UTF-8:\n  - 2 bytes for `0x80 ≤ W1 < 0x800`\n  - 3 bytes for `0x800 ≤ W1 < 0x10000`\n  - 4 bytes for `0x10000 ≤ W1 < 0x110000`\n- Otherwise (W1 < 0x80), writes single-byte ASCII (unchanged).\n- Computes required byte count in `W2`, places bytes in `var_4` buffer, subtracts byte count from buffer length (`[X0,#8]`) and increments buffer pointer (`[X0]`).\n- On success, calls `sub_140012400` (likely a writer or sink function) with the encoded bytes and remaining buffer info.\n- Returns 1 on success (via `loc_14000B4B0`/`loc_14000B52C`), 0 otherwise (e.g., overflow or invalid range via `loc_14000B538`→`loc_14000B4B0`)."}
{"sub_14000E6E8": "This function is a recursive parser for a domain-specific language (likely Rust-like syntax, given strings like `\" = 0x'..= | !nullboolcharstr()i8i16i32i...\"`, `\"< as >& mut *const ; (,dyn  is _falsetr\"`, etc.), starting with an initial check on a parsed value (type tag) returned by `sub_14000F0E8`.\n\n### High-Level Summary:\n- Parses a token stream (pointed to by `X0`, with state maintained in the struct at `X0`).\n- Validates the first token is `'p'` (0x70) — possibly for `pub`, `primitive`, or `pointer`.\n- Recursively parses expressions involving:\n  - `&mut *const` pointer constructs,\n  - `as` casts,\n  - types like `bool`, `char`, `str`, `i8`, `i16`, `i32`, etc.,\n  - possibly `dyn Trait` types (rustc-inspired).\n- Handles:\n  - Recursion depth via a stack variable,\n  - Parsing progress tracked via pointers (`[X19+#0x20]` = current offset, `[X19+#0x10]` = end offset).\n- On success (full parse with no errors), returns `1`.\n- On failure (unexpected token, syntax error, recursion limit), logs appropriate error messages (via `sub_140012B6C`) and returns `0`.\n\n### Key Control Flow:\n- Initial check: `sub_14000F0E8` returns a value `W0`; if lower byte ≠ 2 → error.\n- Then either:\n  - Parses `p`-prefixed type (e.g., `*const`, `&mut`, primitive), or\n  - Falls back to generic type parsing (`sub_14000DAE4`, `sub_14000E044`, etc.).\n- Uses helper calls:\n  - `sub_14000D1A4`, `sub_14000FD78` — likely internal parser helpers.\n  - `sub_140012B6C` — error reporting (prints to stderr? logs?).\n- Returns `1` only if entire input is consumed and parse succeeds; otherwise `0`.\n\n### Observations:\n- Struct at `X0` holds:\n  - `[0]`: ???,\n  - `[8]`: pointer or state (CLOBBERED/ADVANCED via `LDR X9, [X20,#8]!`),\n  - `[0x10]`: input end,\n  - `[0x20]`: current position,\n  - `[0x28]`: error callback or context.\n- Error messages indicate Rust-like type syntax (e.g., `\"& mut *const ; (,dyn  is _falsetr...\"`), but exact DSL is implementation-specific.\n\n**Conclusion**: This is a recursive descent parser for a typed expression or type syntax, returning success/failure (1/0) and consuming input as needed."}
{"sub_14000ED3C": "This function parses a string (likely a hexadecimal or base-62 identifier) from a struct (passed in `X0`), validating characters and possibly handling quoted strings. It attempts to parse a number from the string starting at position `[X0+8]`, allowing digits, lowercase letters, and underscores. If parsing succeeds and meets certain conditions (e.g., length, bounds checks), it continues; otherwise, it triggers error paths (including `BRK #1` and redacted error messages). The function returns 1 on success, 0 on failure.\n\nIn essence: **a robust identifier/number parser with strict validation and error handling, possibly part of a tokenizer or deserializer (e.g., for JSON, a custom format, or a REPL).**"}
{"sub_140004B9C": "This function implements a **string prefix-checking utility**, likely for validating or parsing ASCII/UTF-8 input — specifically checking whether a given byte slice starts with the `\"???\"` prefix (likely representing `<redacted>` in the disassembly). Here's a concise breakdown:\n\n- Takes two arguments: `X0` (a struct/ptr with metadata: `[0]` = data ptr, `[8]` = length, `[0x30]` = some flags/errno), and `X1` (a parameter, likely a context or suffix to use if needed).\n- First checks if `*(X0+0x30) == -2`; if not, jumps to a fallback or error handler (`sub_14000ADB0`).\n- If `*(X0+8)` (length) is zero, returns `0`.\n- Calls `core::str::from_utf8` to interpret the data as UTF-8; fails if result is not valid (cmp X8, #1).\n- On valid UTF-8, calls `sub_140012B6C`, likely a prefix-compare function (e.g., `memcmp` or `starts_with`) against `\"???\"` (i.e., `<redacted>` string), possibly with flags or case-sensitivity control (`MOV W1, #3`).\n- If the prefix does *not* match, returns `0`.\n- If prefix matches and length == 0, returns `0`.\n- If prefix matches and there's remaining data (`length > 3`), it updates internal state (possibly advancing data/length) and loops back to re-check — implying this may be handling multiple segments or a repeated pattern.\n- On successful match with remaining data, falls through to return `1`.\n- On all early exits (no prefix, invalid UTF-8, etc.), returns `0`.\n- On unexpected branch (`loc_140004C90` — likely a buffer overrun), redacts and triggers `BRK #1`.\n\n**In summary**: This function checks if the first bytes of a UTF-8 slice match the prefix `\"???\"` (3 bytes), returning `1` on success (possibly with data advanced), `0` otherwise. It handles UTF-8 validation, bounds, and edge cases (empty, partial)."}
{"sub_140004040": "This function implements a **dynamic import resolution stub**, likely for a protected or obfuscated binary. It:\n\n1. Loads a function pointer from `[X0]`, clears the slot (setting it to `NULL`), and checks if it's zero.\n2. If the pointer is zero (i.e., not yet resolved), it:\n   - Calls `sub_14000104C` (possibly a resolver or loader), passing `0x400, 1`.\n   - If that fails (`X0 == 0`), it proceeds to a failure path.\n3. If the pointer is non-zero, or after successful resolution, it:\n   - Calls `sub_140016E48` with a redacted string argument (likely an error message), followed by `BRK #1` (a debugger break), indicating an error/unexpected path.\n4. In the failure path (`CBZ` from resolver), it calls `sub_140016BA4` with same arguments and again breaks.\n\n**Interpretation**:  \nThis is a **lazy binding stub** — likely for a function imported via a PLT-like mechanism, where the actual target is resolved on first use. The repeated `BRK #1` branches indicate that **the resolution should have succeeded**, and falling through to those paths signals a critical failure (e.g., missing symbol, resolver bug, or tampering). The use of `_guard_check_icall_nop` hints at control-flow protection (e.g., CFG/CGC), further suggesting this is part of a hardened binary.\n\nShort: **Lazy-import resolver stub for a critical symbol, panicking if resolution fails.**"}
{"sub_1400014A8": "This function checks whether the byte at `[X0]` is equal to 1. If it is, control falls through to clean up the stack and jump to `sub_140003218`. If it is not, it logs a redacted message (via `sub_140016E48`) and triggers a debug break (`BRK #1`). The function behaves like a guard or assertion: it allows execution to proceed only when `*[uint8_t*]X0 == 1`, otherwise it crashes in debug mode."}
{"sub_14000F398": "This function parses a recursive expression grammar, likely from a DSL or small language, checking for valid syntax involving keywords like `'N'`, `'O'`, `'R'`, `'E'`, and handling recursion depth limits.\n\n### Summary:\n- Takes a context struct in `X0` (with fields at offsets: `+8` = data pointer, `+0x10` = end pointer, `+0x18` = recursion counter, `+0x20` = current index, `+0x28` = error/reporting callback).\n- Recursively parses tokens starting with `'N'`, `'O'`, or `'R'`, and expects `'E'` to terminate certain constructs.\n- Maintains recursion depth (limit 0x1F4 = 500), triggers error if exceeded.\n- Uses helper `sub_140012B6C` (likely an error/reporting function) on mismatch or recursion limit.\n- Returns 0 on success, 1 on error.\n- Behavior aligns with a recursive descent parser for a small expression grammar (e.g., boolean/NULL-aware expressions like `N`, `O`, `R`, `E` — possibly standing for `NOT`, `OR`, `AND`, `END` or similar).\n\nIn short: **Recursive descent parser for a small expression language with recursion limit and error reporting.**"}
{"sub_1400074D0": "This function performs **unchecked or unvalidated deserialization of a formatting trait object**, likely from a binary or serialized format, and returns the pointer if valid — or zero on failure.\n\n### High-level behavior:\n- Allocates stack space and saves callee-saved registers.\n- Calls `sub_1400124BC` with `X0 = stack buffer`, `X1 = X1`, `X2 = X2`, `X3 = X2`, and `X1` pointing to some metadata (`off_140018758`). The return value in `W0` is checked; if non-zero, it loads a pointer `X8` from the stack (`var_38`).\n- If `W0 == 0`, jump to cleanup (`X0 = 0`, then return).\n- `X8` is validated:  \n  - First, if `X8 == 0`, go to cleanup.  \n  - Then, check `X8 & 3 == 2` via `X10 = (X8&3) - 2; if (X10 <= 2)` → else fail.  \n  - Then if `(X8 & 3) == 0`, fail.  \n  So: only `X8 & 3 == 1` passes those checks.\n- If validation passes:\n  - Loads `X21 = [X8+0xF]`, `X20 = [X8+7]`, `X19 = X8-1`.\n  - Loads `X9 = [X21]`; if `X9 != 0`, call `X9(X20)` — likely a vtable method (e.g., destructor, cleanup, or trait dispatch).\n  - Then loads `X1 = [X21+8]`; if non-zero, calls `sub_140001040(X20, X1, [X21+0x10])` — possibly a conversion or serialization call.\n  - Finally calls `sub_140001040(X19, 0x18, 8)` — possibly copying or aligning something 0x18 bytes, size 8.\n- Returns `X0` (likely the deserialized object pointer, or 0 on failure).\n\n### Likely purpose:\nA **deserializer/parser for a formatting trait object** (e.g., `std::fmt::Debug`, `Display`, etc. in Rust-like semantics), used during runtime object reconstruction. Validates internal tags (`X8 & 3`) and calls associated vtable functions.\n\n### Notable:\n- Branch to `loc_140007510` triggers a `BRK #1` with a debug message `\"a formatting trait implementation retur\"` — suggests this is an **unexpected or unrecoverable failure path**, likely an internal assertion or invariant violation.\n\nSo: **`sub_1400074D0` is a deserialization routine for a formatting trait, returning a pointer or NULL, with strict tagging validation and vtable dispatch.**"}
{"sub_1400084F0": "This function encodes a 32-bit unsigned integer (in `W1`) into a UTF-8–style variable-length byte sequence starting at a buffer referenced by the first argument (`X0`, treated as a pointer to a struct with a buffer pointer at offset `+0`). It handles values up to 0x1FFFFF (21 bits), emitting 1–4 bytes depending on magnitude (similar to UTF-8 or base-128 varint encoding, but with custom bit layouts and header bytes `0x80`, `0xC0`, `0xE0`, `0xF0`). After encoding, it optionally calls cleanup routines via function pointers stored in a secondary struct (offset `+8` of the input), likely handling alignment or memory management. Returns non-zero on success (encoded bytes fit), zero on failure (e.g., `sub_140005F00` buffer overflow)."}
{"sub_1400098A4": "This function appears to be a lightweight wrapper around `sub_1400098F8`, likely performing initialization or validation on a struct passed via `X8` (stored in `X19`).  \n\nKey steps:\n1. Allocates stack frame (`0x60` bytes), saves callee-saved registers (`X19`, `X29`, `X30`).\n2. Passes `X8` as first argument and `SP` as second to `sub_1400098F8`.\n3. After return, checks if the return value in `X8` (loaded from `[SP+#0x20]` offset) is `-1` (`CMN X8, #1`; `EQ` if `X8 == -1`).\n   - If **not** `-1` (fallthrough to `0x1400098cc`): copies two 16-byte vector registers (`Q0`, `Q1`, `Q2`) from the stack to memory at `[X19]` and `[X19+0x20]`, i.e., likely storing two 32-byte chunks into a struct pointed to by `X19`.\n   - If **is** `-1` (branch to `0x1400098e0`): writes `0xFFFFFFFFFFFFFFFE` (i.e., `-2`) to `[X19+0x20]`.\n4. Restores stack and registers, returns.\n\nConclusion: The function initializes or updates a struct (via `X19`) based on the result of `sub_1400098F8`. A return of `-1` from `sub_1400098F8` sets a sentinel value (`-2`) in the struct, otherwise it copies vector register contents into the struct. Likely part of a SIMD/array or crypto operation with error handling."}
{"sub_140007B34": "This function implements a **memcpy-like operation with bounds checking**, likely part of a dynamic buffer or string growth mechanism (e.g., in a `realloc`-like context or vector expansion). Here's the concise breakdown:\n\n- **Inputs** (per calling convention):\n  - `X0`: pointer to a struct (likely a buffer descriptor), whose first field (`[X0]`) points to a struct containing:\n    - `[X21]` = `buf` (current buffer pointer),\n    - `[X21,#8]` = `size` or `capacity` (or similar),\n    - `[X21,#0x10]` = `capacity` (or end pointer),\n  - `X1`: source pointer,\n  - `X2`: number of bytes to copy (`len`).\n\n- **Logic**:\n  1. Load `buf` (`X20`) and `capacity` (`X8 = [X21,#0x10]`), compute free space: `X8 = capacity - buf`.\n  2. If `len > free_space` (`B.HI`), branch to growth logic:\n     - Calls `sub_140016758` (likely a realloc or grow function) with:\n       - `X0 = &buf+8` (address of size/capacity field),\n       - `X1 = src`, `X2 = len`, `W3=W4=1` (flags, e.g., zero-init/copy).\n     - Then jumps to copy logic (after reallocation).\n  3. If space is sufficient:\n     - If `len == 0` (`CBZ X19`), skip memcpy.\n     - Else, call `memcpy(buf + old_size, src, len)`.\n  4. Update stored buffer end/size (`STR X8 = buf + len`) and restore callee-saved registers.\n\n- **Conclusion**: The function safely copies `len` bytes from `X1` into a managed buffer at `X0`, reallocating/growing the buffer if necessary, and updates the buffer’s metadata. It behaves like a `buffer_append` or `dynamic_memcpy` helper."}
{"sub_14000438C": "This function is a **Rust panic handler** (likely `_ZN4core9panicking5panic17h...` or similar), responsible for formatting and reporting a panic message.\n\n**Key behavior:**\n- Initializes stack frame, saves callee-saved registers.\n- Loads panic arguments from input `X0`/`X1` (likely `&PanicInfo` or payload pointer), and `X2` (likely file/line info).\n- Checks first argument (`X8`) for tag:  \n  - **If tag == 1**: Processes a user-provided panic payload:\n    - Converts UTF-8 bytes to `&str` (`from_utf8`).\n    - Calls formatting logic (via `sub_140008738`, `sub_140008854`) and outputs panic string.\n    - Appends `\"\\\\\"` (likely escaping or formatting artifact) and calls external panic reporter (`sub_140014CC4`).\n  - **Else**: Handles unknown/legacy panic payload, sets default `<unknown>` message.\n- Ultimately calls `sub_140014CC4`, a platform-specific panic termination routine (e.g., abort or exception).\n\nThe presence of Rust mangled name `_ZN4core3str8converts9from_utf8...`, `PanicInfo`-like layout, and control flow pattern confirm this is Rust’s core panic infrastructure — not a general-purpose logging or error handler."}
{"sub_1400013BC": "This function performs a conditional check on its input pointer `X0` (likely a struct or object), and if aligned properly (low 2 bits zero), it reads function pointers from a vtable-like structure at offsets `0x7` and `0xF` (8- and 16-byte fields), then may invoke one function (`BLR X8`) and always calls `sub_140001040` with adjusted arguments (`X0 = original X0 - 1`, `W1 = 0x18`, `W2 = 8`).\n\nIt exits early (via fall-through to the epilogue) if:\n- `X0` is null, or  \n- `(X0 & 3) - 2` is not less than 2 (i.e., `X0 & 3` ∉ {0,1}), meaning unaligned input.\n\nIn short: **It validates alignment of a pointer and, if valid, calls two functions (one optionally) and then calls `sub_140001040` with a corrected pointer and fixed arguments.**"}
{"sub_140003474": "This function implements a **thread synchronization primitive**, likely a **futex-like lock or condition variable**, using atomic operations and Windows kernel primitives (`WakeByAddressSingle`). Here's the summary:\n\n- It accesses a **TLS slot** (via `TlsIndex`) to retrieve a per-thread state or context.\n- It maintains a structure (pointed to by `X19`) with:\n  - A **counter** (word at offset 0),\n  - An **expected value** (QWORD at offset 8),\n  - A **lock flag** (byte at offset 0x10),\n  - A **wait counter or pointer** (QWORD at offset 0x18),\n  - Additional bookkeeping (e.g., offset 0x20 used in helper calls).\n- It performs atomic compare-and-swap (`LDXR`/`STXR`) on a global address (`qword_14001D0F8`), likely a **generation counter** or **wait queue head**, used to manage contention.\n- If certain conditions fail (e.g., mismatched expected value or lock contention), it calls helper functions (`sub_1400164A8`, `sub_140007400`) — likely **parking or queuing** the thread.\n- On success or completion, it may call `WakeByAddressSingle` to **wake a waiter**, indicating it’s part of a **wait/wake coordination mechanism**.\n- Error paths trigger breakpoints (`BRK #1`), suggesting **debug-only assertions or violations**.\n\n**Conclusion**: This is a low-level synchronization routine (e.g., part of a runtime or threading library) implementing a **futex-style wait/lock**, likely used for implementing higher-level primitives like mutexes, conditions, or barriers."}
{"sub_1400014DC": "This function appears to be a **Windows synchronization/condition-variable wait routine**, likely implementing part of a Rust runtime or standard library synchronization primitive (e.g., `Condvar::wait` or similar). Here's the concise breakdown:\n\n- **Entry**: Saves callee-saved registers (`X19–X22`, `X29`, `X30`) and allocates stack space.\n- **Calls `sub_140004538`** (likely a mutex or lock acquisition helper), passes arguments via `X0`, `X1`, `X2`.\n- **Loads and checks a flag at `*[X0+0x10]` (byte)**:\n  - If `0`: skips to `loc_140001594` → calls another routine (likely spurious wakeup handling or alternative wait path).\n  - If `1`: goes to `loc_140001564` → calls same routine as above (maybe logging or debug hook).\n  - If `0xFF`: goes to `loc_1400015C4` (fallthrough → cleanup & return).\n  - If `2`: goes to `loc_1400015EC` → calls `WakeByAddressSingle` (Windows API for futex-like wake on address), then returns.\n- Most paths converge to `loc_14000158C`, where a function (at `X21+0x48`) is called via `BLR X8`. That function likely performs the actual OS-level wait (e.g., `WaitOnAddress`, `WaitForSingleObject`, or internal futex).\n- After the wait, `sub_1400013BC` is called — possibly a post-wait cleanup/hook (e.g., reacquire lock, bookkeeping).\n- Final cleanup blocks (`loc_1400015D8`) restore stack and registers and return.\n\n**Conclusion**: This is a **conditional wait implementation** — likely part of a Rust std/sync abstraction (e.g., `std::sync::Condvar` or `parking_lot`) that interacts with Windows native synchronization APIs (`WaitOnAddress`, `WakeByAddressSingle`)."}
{"sub_140001600": "This function appears to be a dispatcher or trampoline that conditionally invokes `sub_140001A90` (likely a generic handler or callback) based on the least significant bit of a value loaded from a vtable or struct. It sets up stack arguments for `sub_140001A90` (possibly a logging, debugging, or instrumentation hook), then hits a `BRK #1` (debugger breakpoint or trap). The control flow splits based on `TBNZ W9, #0`, leading either directly to the second call site (bit 0 = 0) or via an intermediate block (bit 0 = 1), suggesting a 2-way branch tied to a flag in the loaded value. Both paths ultimately converge and fall through, implying this is a small stub likely part of a larger dispatch or virtual call mechanism."}
{"sub_140010470": "This function implements a **UTF-8 encoding routine** that writes the UTF-8 representation of a Unicode code point (passed in `W1`) into a buffer (pointed to by `X0`, specifically at `X0+8`, with `X0` itself likely being a struct or object header, and `X0+16` holding the buffer’s current write position). It handles code points in different ranges:\n\n- **0x00–0x7F** (1 byte)\n- **0x80–0x7FF** (2 bytes)\n- **0x800–0xFFFF** (3 bytes)\n- **0x10000+** (4 bytes, via recursive call or overflow handling)\n\nThe function:\n- Checks buffer space (via `X0+16`), and reallocates/grows the buffer if needed via `sub_140016B04`.\n- Writes the UTF-8 bytes in big-endian order (highest byte first).\n- Updates the buffer pointer (stored back to `X0+8`) and returns `0` (success) in `W0`.\n\nKey clues:\n- `BFXIL`, `LSR`, `ORR` with magic constants (`0xC0`, `0xE0`, `0xF0`) match UTF-8 header byte construction.\n- Bounds checks against `0x80`, `0x800`, `0x10000`.\n- Use of `sub_140016B04` for buffer growth (likely `realloc` or similar).\n- Writes to `[X8, X20]` / `[X9, #n]` imply appending to a growing buffer at offset 8 from the struct base (`X19`).\n\nIn short: **UTF-8 encoder for Unicode scalar values, with dynamic buffer management.**"}
{"sub_140010A94": "This function performs a bounds-checked copy of a data segment from a buffer (pointed to by `X0`) into a potentially overlapping destination, based on flags in `X2` (bit 0 determines which path is taken).  \n\n- If bit 0 of `X2` is set, it calls `sub_1400124BC` (likely a memory allocation or buffer management routine), passing the buffer and other arguments.\n- Otherwise:\n  - It loads the buffer’s base (`X22`) and end (`X8`) pointers (from offsets 0 and 0x10).\n  - Computes `len = X2 >> 1`.\n  - If `len > (end - base)`, it calls `sub_140016B04` (likely a bounds-check or safe-copy helper), which updates `X22` (base) — then falls through to the copy.\n  - If `len != 0`, it copies `len` bytes from `base + buffer_offset` (loaded from `[X19,#8]`) to `base`, via `memcpy`.\n  - Finally, it stores the updated end pointer (`base + len`) back to `[X19]`, restores callee-saved registers, and returns.\n\n**In short**: A safe memory copy utility that handles overlapping regions or partial copies depending on a flag and length bounds, likely part of a string or buffer manipulation subsystem."}
{"sub_140016E14": "This function appears to be a **debug breakpoint or crash stub**: it sets up a stack frame, stores arguments (X0, X1), initializes some local data (including an address `sub_1400127B4` and a data reference `unk_1400192C2`), calls `sub_140016DAC` (likely a logging or exception-handling helper), and then immediately executes `BRK #1`, triggering a debugger breakpoint or fault. The control flow after `BRK` goes to `sub_140016E48`, suggesting post-breakpoint handling (e.g., crash dump or termination), but the `BRK` itself halts normal execution unless handled by a debugger."}
{"sub_140001A90": "This function implements a **thread-aware atomic increment with panic handling**, likely part of a reference-counting or synchronization primitive, possibly in a kernel or low-level runtime.\n\n### High-Level Summary:\n1. **Initial Setup**: Saves callee-saved registers and local stack frame. Accepts five arguments (X0–X3, W4), stores X0/X1/X2/X3.\n2. **Call `sub_1400054E4`**: Likely initializes or checks a context (e.g., thread-local state or runtime check). Returns a status in W0; checks if W0 & 0xFF == 2 → proceeds, else panics (`loc_140001BCC`).\n3. **Atomic Increment on Global Counter**:\n   - Loads a global word at `dword_14001D0D4`.\n   - Uses **load-acquire exclusive (`LDAXR`)** and **store-exclusive (`STXR`)** to atomically increment it.\n   - If concurrent modification detected (store-exclusive fails), branches to retry or panic.\n4. **Optional Dereference & Callback**:\n   - Checks a pointer at `qword_14001D0D8`; if non-null, calls the function at offset `+0x28` of the struct pointed by X0 (original first arg).\n   - Passes arguments from stack (including W19/W20, likely callback flags) to `sub_1400018C0` → possibly a registration or setup helper.\n5. **Error/Conflict Handling Paths**:\n   - If `sub_1400018C0` returns a value indicating an error (bit 31 set in W1 & 0xBFFFFFFF == 0x80000000), triggers panic via `sub_14001613C` with a message (likely from `unk_14001D0D0`), then continues.\n   - Otherwise, if W19 bit 0 is clear, calls `sub_1400054C4`, possibly a cleanup or unlock.\n6. **Failure Paths (`loc_140001BCC`, `loc_140001BBC`, etc.)**:\n   - All lead to calling `sub_140015F1C` (likely a panic/unwind handler) with a generic message (from `unk_14001D0D0`), or to `loc_140001C30`, which:\n     - Invokes a function at `X22+0x30` (likely a destructor or failure handler).\n     - Constructs arguments and calls `sub_140006048` → `sub_1400013BC` → `BRK #0xF003`, a hard panic.\n7. **Alternate Failure Path (`loc_140001BD0`)**:\n   - Builds arguments to call `sub_140006048` with a \"thread caused non-unwinding panic\" message, then panics.\n\n### Key Observations:\n- Uses **ARMv8-A exclusive access (LDAXR/STXR)** — confirms atomicity on global counter.\n- Multiple **BRK #0xF003** calls indicate intentional panic/abort on unexpected behavior.\n- Stack-based argument marshaling suggests integration with a higher-level runtime or thread library.\n- Likely part of **exception handling**, **thread-local storage**, or **reference counting** (e.g., `std::atomic<int>`-like semantics).\n\nIn short: **Atomically increments a global counter, handles thread context, and panics on failure or conflict.**"}
{"sub_14001699C": "This function performs a **safe 64-bit unsigned multiplication with overflow checking and optional callback invocation**, likely part of a secure runtime or exception-handling infrastructure.\n\n### Summary:\n- **Inputs**:  \n  - `X0` (result pointer), `X1` (unknown, likely context), `X2` (optional function pointer), `X3`, `X4`, `X5` (multiplicands and bounds).\n- **Core logic**:\n  1. Uses `UMULH` to compute the high 64 bits of `X4 * X3`.\n  2. If high part ≠ 0 → overflow → jump to error handling.\n  3. Otherwise, computes full product `X21 = X4 * X3`.\n  4. Checks if product > `(0x8000000000000000 - X5)` (i.e., `X21 + X5 >= 0x8000000000000000`) — likely a **bounds check against a mask or page boundary**.\n  5. If `X2 == 0` (no callback), calls `sub_140001044(X1, X8=X2*X4, X2=X5, X3=X21)`.  \n     Else if `X21 != 0`, calls `sub_14000104C(X21, X5)` after `_guard_check_icall_nop`.\n  6. Sets result at `[X19]` (which is input `X0`) and `[X19+8]`/`[X19+0x10]` with values depending on success/overflow.\n\n### Key Observations:\n- The function returns `0` or `1` in `W8` (via `X9/X8`), possibly indicating **success (1)** or **overflow/error (0)**.\n- Uses guard check (`_guard_check_icall_nop`) → likely designed for **CFG (Control Flow Guard)** environments.\n- Stores two 64-bit values at the result pointer:  \n  - `[X0]` ← either `0` (success) or error value (e.g., overflow product)  \n  - `[X0+8]` ← either `X20` (context?) or `X21` (product)  \n  - `[X0+0x10]` ← `X9`, possibly a status or remainder.\n\n### Likely purpose:\nA **checked multiplication helper**, e.g., for:\n- Allocating buffer size (`size = a * b + c`, with overflow check),\n- Memory safety checks,\n- Or crypto/ big-int arithmetic preconditioning.\n\nIt resembles the internals of `__C_specific_handler` or safe integer math wrappers in secure CRT implementations."}
{"sub_140002D90": "This function appears to be a **null-check and conditional initialization routine**, likely part of an object’s constructor or a lazy-initialization pattern.\n\n- It loads a pointer from `[X0]` (the object’s first field, often a vtable or state pointer) into `X19`.\n- It then loads another pointer from `[X19,#0x28]`. If that pointer is zero (`CBZ`), it skips to the epilogue (`loc_140002DB4` → `loc_140002DF0`).\n- Otherwise, it loads `[X19,#0x20]` as a function pointer, calls it (`BL sub_140001040`) with `X0` (unchanged) and `W2=1`.\n- After that, control falls through to a check: `CMN X19, #1` (i.e., test if `X19 == -1`).  \n  - If `X19 == -1`, it branches to epilogue (`loc_140002DF0`).  \n  - Otherwise (`B.NE`), also branches to epilogue — meaning **both paths converge** to the epilogue regardless of result.\n\n**Conclusion**: The function conditionally calls a function at offset `0x20` from an object’s state (retrieved via vtable-like access), but only if the function pointer at offset `0x28` is non-null. The `CMN X19, #1` test seems redundant or leftover from editing, as both branches go to the same epilogue. Likely part of a standard initialization path for an object (e.g., vtable fixup or lazy setup)."}
{"sub_140011494": "This function processes a character (`W1`) and associated flags (`W2`) to encode escape sequences or Unicode representations, writing results into a buffer pointed to by `X0`. It handles common escape characters (e.g., `\\0`, `\\t`, `\\n`, `\\r`, `\\\"`, `\\'`, `\\\\`), performs range checks (`0 ≤ ch < 0x300`), and for non-escape ASCII or low Unicode codepoints, delegates to `sub_1400150A4` or `sub_140014940`. If those fail, or for higher Unicode values, it formats the codepoint as a JSON-style Unicode escape (e.g., `\\u{XXXX}` or `\\uXXXX`), using a hex lookup table (`\"0123456789abcdef...\"`). Final output consists of 2–8 bytes written to the buffer, terminated with a 10-byte structure (possibly including padding), and the function returns after restoring callee-saved registers and sp."}
{"sub_14000176C": "This function implements a thread initialization routine, likely part of a runtime or engine setup, that performs the following key steps:\n\n1. **Stack allocation and register saving** — sets up the stack frame and saves callee-saved registers.\n2. **TLS-based function pointer retrieval** — loads a function pointer from TLS using a TLS index, then dereferences a constructed address (likely `NtCurrentTeb()->TlsExpansionSlots[index]`) to obtain a critical function pointer (`X11`), which may point to `main` or a core entrypoint.\n3. **Optimistic lock-free increment (compare-exchange loop)** — if `X11` is zero (null), it enters a loop to atomically increment a counter at `qword_14001D0F8` using `LDXR`/`STXR`, retrying on contention — this likely serves as a one-time initialization guard or refcount.\n4. **Early-exit if already initialized** — if `X11` was non-null (already initialized), it skips the loop.\n5. **Sets up vectored exception handling and stack guarantee** — registers a handler (`sub_140001C98`) via `AddVectoredExceptionHandler`, calls `SetThreadStackGuarantee`, then calls `GetCurrentThread`.\n6. **Calls the resolved function pointer** — retrieves a function pointer from `X19+0x28`, and calls it with original `X0` argument — this likely invokes the actual `main` or equivalent.\n7. **Conditional cleanup/exit path** — checks a global flag (`dword_14001D004`) and either returns directly or prepares for an error path: calls `sub_140016244` (likely a logging/assertion helper with a redacted string), then returns.\n8. **Error path (zero-initialized TLS)** — if TLS slot is still zero after the loop (impossible in practice, but handled), calls `sub_140016484` and breaks with `BRK #1`, indicating a fatal assertion.\n\nIn short: **This is a thread startup initializer that performs lazy one-time setup via atomic operations, installs exception handling and stack guarantees, then dispatches to a main-like function obtained from TLS.**"}
{"sub_1400130C8": "This function processes a buffer to parse or validate lines terminated by newline (`\\x0A`) characters, likely as part of a text-parsing or validation routine (e.g., parsing structured input like CSV, config, or protocol lines). It uses vectorized masking (with constants `0x2020...` and `0x8080...`) to efficiently scan for non-space/newline bytes and detect the end of lines, possibly in chunks of 16 bytes. When a newline is found, it invokes a callback function (at `X8 = [X0+0x18]`) with a slice of the input starting from the last newline (or start), passing the current line's length and possibly a flag indicating continuation or success. Control flow branches based on callback return value and position tracking (`X25`, `X26`, `X27`, `X28`, `X19`, `X21`). Returns `0` (success) or `1` (failure/error) via `W0`."}
{"sub_14000DAE4": "This function is a **recursive parser** (likely for Rust-style type syntax or similar), implemented in AArch64. It parses a string starting at `X0` (input buffer), tracking progress in a state structure (at `X0`), using recursion depth (`[X19,#0x18]`) and recursion limit checks (max `0x1F4 = 500`).\n\n### High-Level Summary:\n- **Entry**: Allocates stack frame, saves registers, initializes state pointers (`X19`, `X20` ← `X0`).\n- **Core Loop**:\n  - Reads current input pointer (`[X20,#8]`) and index (`[X20,#0x10]`) and bounds (`[X20,#0x20]`).\n  - Loads current char (`[X9,X8]`), where `X9 = current buffer ptr`, `X8 = index`.\n  - Decodes lowercase letter (`'a'`–`'z'`) via bit-test on magic constant `0x3BCFBBF` (likely a bitmask of allowed chars).\n  - For valid identifiers, looks up type/keyword string in a table (`unk_140018CB0`, `off_140018D80`) and dispatches to sub-parsers (`sub_14000C410`, `sub_14000CEE0`, `sub_14000EFD0`, `sub_14000F398`, `sub_14000DAE4` recursively, etc.).\n  - Handles operators/keywords like `'A'` (array), `'B'` (binary op), `'D'` (dyn), `'P'`/`'E'`/`'S'`/`'T'`/`'W'` (pointers, references, lifetimes, wildcards, `where`, `impl`?), and `'F'`/`'R'`/`'Q'` (function/impl trait?) via jump tables.\n- **Error Handling**:\n  - Returns early on recursion limit (`{recursion limit reached}?` message).\n  - Validates syntax; on mismatch or unexpected char, outputs `\"{invalid syntax}?\"` and fails.\n- **Exit**:\n  - On success, returns `0` in `W0`, clears error bits.\n  - On failure, returns `1`, sets error flags.\n  - Restores stack and returns.\n\n### Key Characteristics:\n- Recursive descent parser (self-calls `sub_14000DAE4` for nested types).\n- State stored in the input buffer struct: pointer, index, recursion counter, etc.\n- Uses a set of helper functions (e.g., `sub_14000C410`, `sub_14000CEE0`, `sub_14000F398`, `sub_14000DAE4`) to parse specific constructs.\n- Supports Rust-like syntax elements: `*const`, `&mut`, `dyn`, `impl`, `where`, arrays, lifetimes, etc.\n\nThis is likely part of a Rust compiler’s metadata/IR parser or a similar type-signature deserializer."}
{"sub_140016E68": "This function performs bounds checking on three input values (passed in `X0`, `X1`, `X2`) and calls `sub_140016DAC` with different argument configurations depending on the comparison results, followed by a `BRK #1` (debug breakpoint). Specifically:\n\n- First, it checks if `X0 <= X2` — if so, jumps to `loc_140016EB0`; otherwise proceeds.\n- If not, it sets up arguments and calls `sub_140016DAC` with `(X0, address_of_unk_14001934E, X3, ...)` and breaks.\n- At `loc_140016EB0`, it checks if `X1 > X2` — if so, jumps to `loc_140016EF4`.\n- Then checks if `X0 <= X1` — if so, jumps to `loc_140016EF4`.\n- Otherwise (i.e., `X0 > X1` and `X1 <= X2`), it calls `sub_140016DAC` with `(X0, address_of_unk_19326, X3, ...)` and breaks.\n- At `loc_140016EF4`, it calls `sub_140016DAC` with `(X1, address_of_unk_192EF, X3, ...)` and breaks.\n\nAll calls to `sub_140016DAC` follow the same stack-frame setup pattern, suggesting a debugging or validation hook, possibly part of a safety wrapper (e.g., assert or instrumentation) — the repeated `BRK #1` strongly indicates this is debug code, likely in a non-optimized or instrumented build.\n\nIn short: **A debug/validation routine that inspects ordering of three values and triggers a breakpoint after calling a helper with context-specific arguments.**"}
{"sub_140015938": "This function implements a **stack cookie update mechanism**, likely part of the security initialization or reseeding logic (e.g., `/GS` stack protection), using high-resolution timing and process/thread identifiers to generate entropy.\n\n**Key steps:**\n- Verifies the current stack cookie (`__security_cookie`) matches a known value (`0x2B992DDFA232`); if not, jumps to cleanup/exit.\n- Otherwise, gathers entropy:\n  - `GetSystemTimeAsFileTime()` → 64-bit timestamp.\n  - `GetCurrentThreadId()` and `GetCurrentProcessId()` → thread & process IDs.\n  - `QueryPerformanceCounter()` → high-res timestamp.\n- Mixes these values into a 64-bit intermediate using XOR and bit operations.\n- Uses the lower 48 bits of the result to conditionally update `__security_cookie`, bumping it to `0x2B992DDFA233` if it matches the old value (to avoid redundant updates).\n- Stores the one’s complement of the final result at a global location (`qword_14001D0C0`), then restores registers and returns.\n\n**Purpose:** Securely reseeds or updates the stack canary using system entropy to mitigate return-oriented programming (ROP) or stack smash attacks."}
{"sub_140016244": "This function implements a **robust once-initialization primitive**, likely part of a C++ `std::once_flag` or similar synchronization mechanism, using Windows `WaitOnAddress`/`WakeByAddressAll` for blocking waits.\n\n### High-level behavior:\n- Takes a pointer to a 32-bit state variable (`X0`) and parameters for initialization function (`X2`–`X4`).\n- Uses atomic loads/stores (`LDAXR`, `STXR`, `LDAR`) with explicit memory ordering.\n- Interprets the state as:\n  - **Bits [1:0]**: flags 0=unset, 1=in-progress, 2=completed, 3=error/poisoned (based on comparisons).\n  - **Bit 2**: wake/notify flag.\n- If initialization is already done (state == 2 or 3), returns immediately.\n- If in-progress (state == 1):\n  - Sets bit 2 (wake flag) if needed.\n  - Calls `WaitOnAddress` in a loop to wait for a change.\n- If unset (state == 0):\n  - Atomically transitions to in-progress (1).\n  - Calls an external helper (`sub_140006FC0`, possibly a guard function).\n  - Tries to commit result (set state to 2), waking waiters via `WakeByAddressAll` on success.\n- On failure to atomically update (e.g., race), retries.\n- On poison state (3), triggers a `BRK #1` breakpoint after logging an error string.\n\n### Key subfunctions called:\n- `sub_140006FC0` — likely a wrapper or guard around the user’s initialization callback.\n- `WaitOnAddress` / `WakeByAddressAll` — Windows futex-like primitives.\n- `GetLastError` — used if `WaitOnAddress` returns unexpected status.\n- `sub_140016DAC` — writes debug error message and triggers a breakpoint on poison.\n\nThis is a **thread-safe,wait-free (on fast path) once-flag implementation**, optimized for low overhead after initialization."}
{"sub_140011DE0": "This function appears to be a conditional helper in a JSON-like serializer or printer. It performs the following key steps:\n\n1. Saves callee-saved registers and sets up a stack frame.\n2. Calls a function (via `BLR X8`) on an input object (pointed to by `X0`) to determine some state (e.g., whether it’s a final/terminal node), storing the result as `var_47`.\n3. Calls `sub_140012D1C`, passing the stack-allocated buffer and several saved arguments — likely to print a prefix or intermediate structure (e.g., opening brackets/braces).\n4. Checks two flags: `var_48` (written by `sub_140012D1C`) and `var_47`. If either indicates completion or special handling, it branches to `loc_140011E8C`.\n5. Otherwise, inspects the input object's second byte (`[X8,#2]`). If bit 7 is set:\n   - It loads a string `\"})], \"` (closing delimiter), and calls a printer function with count 1 (i.e., prints a single character).\n   - Else (bit 7 clear): loads `\" }\"` and prints it with count 2.\n6. Finally, masks the return value to 1 bit, restores registers, and returns.\n\nIn short: **This function conditionally prints a closing delimiter for a nested structure (e.g., `}`, `]`, `)`) in a serialization routine, based on flags and metadata from a preceding call and the object’s type flags.**"}
{"sub_140008D0C": "This function parses a path string passed in `X0`, processing path components (handling separators `\\` and `/`), checking for special components like `.` and `..`, and normalizes or strips path segments based on context.\n\n### Key observations:\n- Loads a 32-byte vector from `X0` and inspects byte at offset +1 — suggests structure of a path object.\n- Compares that byte to `2`; if equal, enters a branch that parses path components recursively/iteratively.\n- Scans for directory separators (`\\`, `/`) and treats:\n  - `.` → \".\" (current dir) → handled specially (e.g., strips it if standalone).\n  - `..` → parent dir reference → logic may remove prior segment.\n- Uses helper functions `sub_1400089BC` (likely strlen or similar) and `sub_140008AFC` (possibly win32 path normalization or parsing).\n- In error/unexpected cases, triggers `BRK #1` after logging \"<redacted>\" — suggests internal failure assertions.\n\n### Likely purpose:\n**Windows-style path normalization or component extraction**, possibly stripping current/parent directory markers or preparing a path for further processing (e.g., canonicalization, validation, or relative resolution).\n\nMinimal assumption: computes normalized path segment offsets or length after removing `.` and resolving `..` relative to prior path, using recursive or loop-based segment scanning."}
{"sub_140004120": "This function appears to be a **UTF-8 string validation and processing routine**, likely part of Rust’s string handling (based on the mangled symbol `_ZN4core3str8converts9from_utf8...`). Here's what it does:\n\n1. **Initial Setup & Flags Check**:\n   - Sets up a stack frame and saves callee-saved registers.\n   - Reads two `u64` values (likely `*mut [u8]` or `&[u8]`) from `x0` and `x1`.\n   - Sets a flag in the first `u8` of the first struct to `1` (possibly a validity or in-use bit).\n   - Checks a bit (bit 0) in the second struct’s first field; if clear, jumps to a fallback (possibly reparse or error path).\n\n2. **Conditional Processing Based on Flags**:\n   - Loads addresses from offsets `+0x10`, `+0x18`, `+0x20`, `+0x28`, etc., suggesting access to fields like `data`, `length`, `flags`, `vtable`, `counter`, or `slice`.\n   - Tests flags (e.g., bit 0 at `+0x20`) to determine path:  \n     - If set → calls `sub_1400124BC`, likely a UTF-8 conversion/validation helper (possibly `from_utf8` internally).\n     - Else → clears data/counter fields (`strb wzr`, `str xzr`) and proceeds.\n\n3. **Main Logic Paths**:\n   - **Path A (normal/valid UTF-8)**:\n     - Calls `from_utf8` (mangled symbol `_ZN4core3str8converts...`), then validates result (returns `0` or `1`).\n     - Updates a counter at `+0x30` (`x8`, then `x9 = x8+1`, `str x9`), likely reference or usage counter.\n     - Sets success flag and returns.\n   - **Path B (invalid/unaligned/escaped UTF-8)**:\n     - Re-calls `from_utf8` again (possibly a retry or alternate path).\n     - If still invalid, clears flag and jumps back to re-check.\n\n4. **Validation/Encoding Helper**:\n   - Calls `sub_140004CA8` with a hardcoded string/len (`unk_140017DD8`, `0x1A`) → likely a known test or header string (e.g., `\"UTF-8\"` or protocol prefix).\n   - Depending on return value (`w0`), sets/clears flags at `+0x10` and either re-checks or proceeds.\n\n5. **Error Handling / Retry Path**:\n   - In some error cases, increments a counter at `+0x18` and jumps back (`b loc_140004154`) — possibly retry logic or state tracking.\n\n**High-level Summary**:  \nThis function validates or processes a UTF-8 string, likely from a slice or reference. It checks internal flags, may call `from_utf8` (possibly twice), handles errors or retries, and updates internal state (counters, flags). It’s part of a larger UTF-8 string infrastructure — possibly in `core::str` or a related module — with custom bookkeeping (e.g., refcounting, validity bit, retry counter)."}
{"sub_140012C2C": "This function encodes a 32-bit integer value from `w0` (loaded from `[x0]`) into a UTF-8-like variable-length byte sequence starting at `[x29,#var_4]`, and calls `sub_140011FA4` with the encoded bytes and length `w2`. It also checks a flag in `*(x1+3)` (bitwise AND with `0x18`), possibly for validation or early exit. The encoding logic resembles UTF-8/GB18030-style multi-byte encoding:\n\n- If `*(x1+3) & 0x18 == 0`, it checks if `w0 < 0x80` (1-byte ASCII range), stores it directly and sets length=1.\n- Else (`*(x1+3) & 0x18 != 0`), it handles larger values:\n  - 0x80 ≤ w0 < 0x800 → 2-byte encoding\n  - 0x800 ≤ w0 < 0x10000 → 3-byte encoding\n  - w0 ≥ 0x10000 → 4-byte encoding\n\nIt then calls `sub_140011FA4(x8, [buf], w2)` where `x8` is the original `x1` pointer, `w2` is byte count (1–4), and the buffer holds the encoded bytes.\n\n**Summary**: UTF-8/variable-length encoding of a 32-bit codepoint from `*x0`, based on a condition in `*(x1+3)`, followed by a call to an output function."}
{"sub_140007400": "This function appears to be a **debug assert or validation routine**, likely verifying that a given object or pointer conforms to expected formatting trait metadata in a C++/Rust-like runtime (given the “a formatting trait implementation retur…” string fragment). \n\n**High-level behavior**:\n- Allocates stack frame, saves callee-saved registers.\n- Calls `sub_1400124BC` (likely a hash/lookup or metadata accessor) with SP as context, returning a pointer `X8`.\n- If `X8` is zero or invalid (`CBZ`), jumps to clean-up and returns 0 (success).\n- If `X8` is non-zero:\n  - Checks `X8 & 3` (alignment tag) and rejects values ≥2 (likely ensuring valid tag for a formatting trait).\n  - Dereferences `X8` to read metadata (function pointer at `[X8+0x7]` and vtable/traits at `[X8+0xF]`).\n  - Conditionally calls the function pointer (at `X9 = *[X21]`) with `X20 = [X8+7]` as argument.\n  - Then calls `sub_140001040` (likely a debug print or assertion helper) with derived pointers.\n  - If any check fails, falls through to `BRK #1` (debug breakpoint/assertion failure).\n- Returns 0 in `X0` on all exit paths (after cleanup).\n\n**Conclusion**:  \nA **formatting trait validator/assertion**, probably checking that a type’s formatting implementation metadata is well-formed and calling custom validation hooks — failing with a breakpoint (`BRK #1`) on invariant violation. It mirrors patterns used in standardized pretty-printing/debugging infrastructure (e.g., Rust’s `Debug` trait validation, or custom type-traits)."}
{"sub_140015C80": "This function iterates over a null-terminated array of function pointers stored in `.data`, calls each one via indirect call (with Control Flow Guard check), and returns when the null terminator is reached.\n\n- Sets up prologue with PAC (Pointer Authentication) and standard stack frame.\n- Loads start (`X19`) and end (`X20`) addresses of an array of function pointers (likely from `unk_14001A880` to `unk_14001A888`, minus 8).\n- Loop: loads each pointer (`X8`), checks for null terminator (`CBZ` → exit loop if zero), otherwise:\n  - Performs CFG check via `_guard_check_icall_nop`.\n  - Calls the function pointer.\n- Repeats until end of array or null pointer.\n- Epilogue restores registers and returns.\n\nIn short: **calls all function pointers in a null-terminated array (with CFG support)**."}
{"sub_140002F44": "This function implements a **thread-safe, atomic buffer resizing or appending operation**, likely part of a dynamic buffer (e.g., `std::vector`-like or custom ring/buffer implementation), using TLS and atomic operations.\n\n### High-level behavior:\n1. **Initial check**: Reads a global flag (`byte_14001D100`) — if zero, jumps to early return (possibly no-op or fallback).\n2. **Performs TLS-based lookup**: Uses TLS index to access a thread-local structure, then computes a buffer pointer (likely a `buffer_header*`).\n3. **Checks buffer state**:\n   - If flag = 1: calls `sub_140015E58` (possibly a grow/reserve/initialization routine).\n   - Clears head/tail marker at `[X19]` and atomically checks for contention via `LDAXRB`/`STXRB`.\n4. **If contention resolved**, and `X0 & 1 == 0`, it calls `sub_140007260`, presumably to do the actual copy/resize using metadata from the buffer header.\n5. **Buffer update logic**: Updates length (`X22+0x18`), potentially copies data (`memcpy`), and cleans up.\n6. **Special case handling** (e.g., overflow) via `sub_140016758` in high paths.\n7. Returns 1 on success, 0 on failure or early exit.\n\n### Key components:\n- Uses **atomic load/store-exclusive** (`LDAXRB`, `STXRB`, `CLREX`) for safe concurrent modification.\n- Uses **TLS slot** to find per-thread buffer context.\n- Calls to:\n  - `sub_140015E58`: likely buffer initialization/growth.\n  - `sub_140007260`: core copy/append logic.\n  - `sub_140001040`: destructor/finalization or `free`-like routine.\n  - `sub_140002D90`: likely a memory barrier or atomic compare-exchange helper.\n  - `sub_1400164A8`/`sub_140016758`: advanced atomic buffer ops (e.g., grow, lock-free enqueue).\n\n### Summary:\n**This is a low-level, concurrency-safe append/resize function for a dynamic buffer, using TLS and lock-free primitives.**"}
{"sub_140008258": "This function encodes a 32-bit value (`W1`) into UTF-8 bytes (1 to 4 bytes depending on magnitude) into a stack buffer, then attempts to write them using a vtable function pointer. It validates pointer and reference-counting constraints before calling destructors/helpers on auxiliary data, ultimately returning whether the write succeeded.\n\n**High-level behavior**:\n1. Allocates stack space and saves callee-saved registers.\n2. Checks input size (`W1`) and encodes it as UTF-8:\n   - `< 0x80`: 1-byte sequence (direct)\n   - `< 0x800`: 2-byte (with `0xC0`/`0x80` prefix)\n   - `< 0x10000`: 3-byte (with `0xE0`/`0x80` prefix)\n   - else: 4-byte (with `0xF0`/`0x80` prefix)\n   Encoded bytes stored at `[SP+var_44]`.\n3. Loads a vtable-like struct from `X0` (arg0), gets a function ptr at offset `+0x18`.\n4. If that function ptr is `NULL`, aborts via `BRK #1` after logging `<redacted>`.\n5. Else, calls that function (at `sub_140005AD0`) with:\n   - arg0: stack address (`&var_8`)\n   - arg1: pointer to encoded bytes\n   - arg2: byte count (1–4 in `W2`)\n6. On success, checks ancillary data (`X0+8`):\n   - Validates low 2 bits (must be `0b10` or `0b00`, likely a tag/refcount pattern).\n   - If valid and non-zero, calls destructor/interceptor at `[X8+0xF]` (if non-null) and `[X8+0x7]`'s associated method (via `[X23,#8]` / `[X23,#0x10]`).\n7. Frees 24 bytes of memory via `sub_140001040`.\n8. Stores return value to `[X19+8]`, restores stack, returns `Z ? 0 : 1`.\n\n**Purpose**: A UTF-8 encoding + write helper, likely part of a serialization or string encoding routine (e.g., encoding a length or codepoint)."}
{"sub_140007260": "This function appears to be a **runtime type-checking and dispatch helper**, likely part of a formatting or trait-resolution system (e.g., Rust-style `Display`/`Debug` traits or C++ RTTI). Here's the summary:\n\n- It takes three arguments (`X0`, `X1`, `X2`)—likely an object pointer and two trait/function-table-related pointers.\n- It initializes a stack frame and calls `sub_1400124BC`, passing the stack pointer and a global address (`off_140018670`), possibly to resolve or validate a trait implementation.\n- If `sub_1400124BC` fails (returns zero), it proceeds to check for error conditions:\n  - If `X8` (return value from `sub_1400124BC`) is zero → early exit with `X0 = 0`.\n  - If `X8 & 3 == 2` or `3` → likely invalid pointer/tag → `BRK #1` (debug break) with error message about formatting trait failure.\n- Assuming valid input (`X8 & 3 == 0` or `1`), it:\n  - Loads `X21 = [X8+0xF]`, `X20 = [X8+7]`, and computes `X19 = X8 - 1`.\n  - If `[X21]` (first entry of a vtable/ftable?) is non-zero, it calls the function at `[X21]` with `X20`.\n  - Then, if `[X21+8]` is non-zero, it calls `sub_140001040` (likely a deallocator or cleanup function) with arguments from `X20`, `[X21+8]`, `[X21+0x10]`.\n  - Finally, calls `sub_140001040(X19, 0x18, 8)` — possibly zero-initializing or freeing `X8-1`.\n- Ends by restoring registers and returning `X0 = 0`.\n\n**Conclusion**: A **trait/dyntype cleanup/handoff routine** — it validates a type/structure pointer (`X8`), executes a method from its table, and cleans up associated resources, returning `0` on success (or early failure). The presence of `BRK #1` with a redacted string suggests this is a **debug build**, and the function may be a runtime assertion for trait correctness."}
{"sub_1400075A0": "This function performs a type-check and dispatch based on the lower 2 bits of a pointer stored in `obj->field_8` (where `X0` is the object pointer). If the field is zero or the lower bits are not 2 or 3, it returns early (no-op). Otherwise:\n\n- It reads two 8-byte values from offset 0xF and 7 of that pointer (likely loading a vtable or function table).\n- If the first entry in that table is non-zero, it calls that function with `obj->field_7` (or derived value) as argument.\n- Then, if a second table entry is non-zero, it calls `sub_140001040` with `obj->field_7` and two constants (`0x18`, `8`) as arguments.\n- Finally, it calls `sub_140001040` again with `obj - 1`, `0x18`, and `8`.\n\n**High-level summary**:  \nThis is a tag-and-dispatch pattern — likely part of a tagged-pointer or type-tagged object system — where it checks for a specific tag (2 or 3), dereferences a vtable-like structure, and performs two cleanup/finalization calls (via `sub_140001040`, probably a deallocator or destructor helper)."}
{"__InterlockedDetectSupportEH": "This function checks whether exception handling (EH) is supported by verifying two conditions:\n\n1. **First check**: It reads a value from `[X0]` and compares it to `0xC000001D` (STATUS_ACCESS_DENIED). If it does *not* match, it falls through (likely indicating EH *not* supported), but the control flow only shows one successor (the `RET #1` block), implying the `B.NE` must jump elsewhere — however, per given CFG, it proceeds to the same final block. This suggests either an incomplete CFG or an implicit fallthrough that’s not shown.\n\n2. **Second check**: If the first condition *does* match (i.e., `[X0] == 0xC000001D`), it proceeds to load from `[X2+0x108]`, dereference that, mask the result with `0xFFFFFFE0`, and compare against `0xD5384240` — the encoding of `MSR DAIFSet, #0` (a system register write instruction used in exception entry/exit). If the masked value matches, it means this instruction is present, indicating EH is supported.\n\nIn both cases (whether the first comparison fails or the second passes), the function returns `1`. However, based on the `B.NE` behavior and typical EH detection logic, it's more likely that:\n\n- If `[X0] != 0xC000001D`, it jumps to a block (not shown) returning `0`.\n- If `[X0] == 0xC000001D` and the instruction at `[X2+0x108]` is *not* a DAIF modification, it returns `0`.\n- Only if *both* checks pass does it return `1`.\n\nGiven the provided blocks only show the success path, the function likely returns `1` on success (EH supported) and `0` otherwise.\n\n**Summary**:  \nDetects whether exception handling is supported by checking for a specific status code (`0xC000001D`) and the presence of a `DAIFSet` instruction (used in EH entry/exit) in the code path. Returns `1` if both conditions are met, else `0`."}
{"sub_140015338": "This is a C runtime initialization function (likely `__scrt_common_main_seh` or similar), performing standard CRT setup for a Windows SEH-enabled executable on ARM64.\n\n- Saves registers and sets up the stack frame.\n- Calls `_set_app_type`, `_set_fmode`, `__p__commode`, and `___scrt_initialize_onexit_tables` to initialize runtime state (app type, file mode, threading locale, atexit tables).\n- Attempts to register cleanup code via `_atexit` (using a pointer to a trampoline at `sub_140015C80`) and to configure command-line arguments (`_configure_narrow_argv`); jumps to error handler on failure.\n- Sets math error handler via `__setusermatherr`.\n- Performs guard checks and final environment/thread locale setup (`_configthreadlocale`, `_initialize_narrow_environment`).\n- Calls `sub_140015AE8` (likely `__scrt_initialize_main_args` or equivalent); on failure, jumps to error path.\n- Final block calls `sub_140015AE0` with `W0=7`—likely the actual entry point dispatcher or startup routine (e.g., `__scrt_common_main`), passing 7 as flags (encoding argc/argv/envp/main options).\n\nIn short: this is a Windows ARM64 CRT startup/init stub that prepares the runtime environment before transferring control to the main application entry."}
{"sub_140011FA4": "This function performs **AVX-512-based vectorized computation on floating-point data**, likely part of a high-performance numerical routine (e.g., matrix operation, convolution, or SIMD-accelerated math). Key observations:\n\n- Saves many callee-saved registers and vector registers (D8–D10), indicating it's a non-trivial function preserving ABI compliance.\n- Starts by inspecting bits `[27:26]` (0x18000000 mask) and bit `#0x1C` (bit 28) of a control word at `[X0]`, branching to different code paths based on flags—typical for dispatching optimized SIMD variants (e.g., AVX-512 vs fallback).\n- One major path (`loc_140011FF4`) processes a 16-bit header (`LDRH W8, [X0,#6]`) and performs a loop computing a length (`X10`) via byte-level inspection and conditional increments—suggesting parsing or length calculation from packed data (possibly UTF-8, variable-length encoding, or instruction stream).\n- A second major path (`loc_140012078`) uses **AVX-512 vector operations** (`V0–V24`, `ADD`, `ADDP`, `FMOV`) to reduce vectors to scalar and store in `X10`, implying reduction operations (e.g., sum of elements).\n- Later, conditional branching on `W9 = UBFX(W22, #29, #2)` (bits 30:28) selects between variants: `W9==0` → copy full difference; `W9==1` → store full diff; `W9==2` → extract low 15 bits (`UBFX W23, W25, #1, #15`). This suggests variant selection based on operand size or alignment.\n- The core loop (`loc_140012364`, `loc_1400123D4`) calls function pointers from a vtable (`[X24,#0x18]`, `[X24,#0x20]`) with decreasing counters (`W23`, `W25`), retrying on success (bit 0 of return), typical of retry-based error handling in low-level math libraries (e.g., handling exceptions or partial completion in vector ops).\n- Returns either a computed scalar (`X10`) or `W19=1` on early exit.\n\nIn summary: **A dispatcher to optimized AVX-512 routines for vector arithmetic, with fallback paths, length/size analysis, and retry-based execution on vtable callbacks.**"}
{"sub_1400089BC": "This function performs a validation/check on a structure pointed to by `X0`, likely related to parsing or verifying some kind of protocol or format (possibly a network or file format header). It inspects:\n\n- Byte at `X0+0x0`: if bit 0 is set, fails (`X0 = 0`).\n- Byte at `X0+0x18`: must be ≤5 and ≠0xFF; determines subsequent logic.\n- Based on that value (0–4), it computes a pointer+offset (from fields at `X0+0x28`, `X0+0x38`) or returns a constant (e.g., `6`, `2`, `4`).\n- Then compares the computed result (`X0`) against `X1` (likely a length/boundary). If `X0 > X1`, triggers a crash (via `BRK` after `<redacted>` error).\n- Otherwise, checks specific byte patterns (`.` or `\\`) in the computed region and returns `1` (success) or `0` (failure).\n\n**Summary:**  \nA validation helper function that inspects a structured input (likely a message or record header), computes a derived pointer/sub-length, bounds-checks it, and validates trailing content (e.g., terminator chars), returning `1`/`0`. Failure paths either return `0` or crash with `BRK`."}
{"sub_140015F1C": "This function implements a **read-write lock (RwLock) read-lock acquisition with exponential backoff and explicit retry logic**, likely as part of a userspace synchronization primitive.\n\n### High-Level Behavior:\n- It operates on a lock variable located at `[X0 + 4]`.\n- The top two bits (bit 30 and 31) encode metadata:  \n  - Bit 31 (`0x80000000`): *writer-owning* flag.  \n  - Bit 30 (`0x40000000`): *writer-waiting* or *pending writer* flag.  \n  - Lower 30 bits (`0x3FFFFFFF`): *active reader count*.\n\n### Logic Summary:\n1. **Initial Check**: Verifies the current value of the lock word is not at its max reader count (`0x3FFFFFFF`). If it is, jumps to error handling.\n2. **Spin Loop**: If lock appears uncontended (reader count < threshold), attempts atomic increment using `LDAXR`/`STXR`:\n   - On success: sets return flag and exits.\n   - On failure (race/concurrency): retries.\n3. **Backoff / Wait Path**:\n   - If lock indicates contention (e.g., writer bit set, or count too high):\n     - Sets `0x40000000` bit (likely signaling intent to wait).\n     - Calls `WaitOnAddress` to sleep until wake-up.\n     - Checks for `GetLastError` after wait.\n     - Retries atomic increment after waking.\n4. **Backoff Loop**:\n   - Contains a retry loop using `CBNZ` on a decrementing counter (`0xFFFFFF9D`) with `ISB` in between — classic backoff pattern to reduce contention.\n5. **Error Handling**:\n   - If reader count reaches `0x3FFFFFFC` or `0x3FFFFFFE`, branches to `loc_140016120`, which triggers a hard crash via `BRK #1` with an assertion message: `\"too many active read locks on RwLockass\"` — likely a typo for “RwLock asserts”.\n\n### Key Details:\n- Uses ARMv8.1 `LDAXR`/`STXR` for load-linked/store-conditional semantics.\n- Manages backoff with `ISB` and repeated speculative loads.\n- Interacts with Windows kernel via `WaitOnAddress`/`GetLastError`.\n- Save/restore prologue/epilogue using standard AAPCS calling convention.\n\n### Purpose:\nThis is a **high-contention-aware read-lock acquisition routine**, designed to avoid livelock under heavy write pressure via backoff and OS-assisted waiting.\n\n--- \n\nLet me know if you'd like a control-flow graph summary or a pseudocode translation."}
{"sub_14000AF90": "This function (`sub_14000AF90`) is a lightweight dispatcher or guard that checks whether the value at `+8` offset of the dereferenced `X0` (i.e., `*(void**)(*(void**)X0 + 8)`) equals `1`.  \n\n- If equal to 1: it sets up a stack frame with some zeroed fields and a pair of values loaded from `[X8,#8]`, then calls `sub_14000D3F8`, presumably to perform a specific operation (e.g., a method dispatch or handler invocation), and returns.\n- If not equal to 1: it skips the setup and directly jumps to `sub_14000A190`, likely a fallback or alternative handler.\n\nIn short: **Conditional dispatch based on a tag/check value; either invokes `sub_14000D3F8` after stack preparation, or jumps to another handler.**"}
{"sub_140016A50": "This function performs a conditional buffer operation on a structure passed in `X0`, likely part of a string or memory buffer management routine.\n\nKey behavior:\n- It compares an input size (`X2`) against the difference between two fields in the structure (`[X0+0x18] - [X0+8]`), which likely represent capacity and current size (or end pointer minus start).\n- If `X2` ≤ that difference, it:\n  - Calls `sub_140007070` (possibly a validation or preparation function); if it returns non-zero, jumps to cleanup.\n  - Otherwise, uses `memcpy` to copy `X2` bytes starting at an address computed from `[X0+8]` and `X2`.\n  - Updates `[X0+8]` to point past the newly written data.\n- Else (if `X2` is too large to fit), it:\n  - Calls `sub_140006060`, passing `X19` (the struct), `X20` (size), and a flag `1`.\n  - Then validates the returned value: checks for specific conditions (`X0 & 3 == 2`, then checks high 32 bits == 6), and may zero `X0`.\n  - Stores `0` byte at `[X0]` (likely null-terminating or signaling failure/error).\n- Cleanup: restores callee-saved registers and returns.\n\nIn summary: **This function attempts to append or insert a buffer of given size into a managed structure, either via `memcpy` if space permits, or triggers error/handle-grow logic otherwise — and ensures null termination or cleanup in either path.**"}
{"sub_14000E044": "This function (`sub_14000E044`) is a **parser/decoder for a custom bytecode or text-based language**, likely part of a virtual machine or interpreter (e.g., for Rust-like typed expressions or a domain-specific language). It operates on a state object passed in `X0`, tracking input position (`X19+0x20`), input buffer pointer (`X19+8`), and recursion depth (`X19+0x18`).  \n\nKey behaviors:\n- Reads a byte (`LDRB`) from input (`[X10, X9]`) and dispatches via a jump table based on its value (`A–U`, `'n'`, `S/T/U`, etc.).\n- Implements handling for various constructs:  \n  - **Keywords**: `true`, `false`, `null`, `bool`, `char`, `str`, `i8`/`i16`…  \n  - **Type operators**: `&`, `*const`, `dyn`, `mut`, `as`, `:`  \n  - **Recursion check**: Error if depth exceeds `0x1F4`.\n- Calls helper functions (`sub_14000CFCC`, `sub_14000CD90`, `sub_14000BB4C`, etc.) to process sub-elements (e.g., types, values, closures).\n- Emits error messages (via `sub_140012B6C`) for invalid syntax or recursion limit reached.\n- Returns 0/1 in `W0` indicating success/failure.\n\nThe parsing appears recursive and context-sensitive, with heavy use of string formatting for diagnostics (e.g., `{invalid syntax}`, `{recursion limit reached}`)."}
{"sub_140002DFC": "This function appears to be a destructor or cleanup routine for an object (likely a C++ class or similar structure), operating on the object pointer in `X0`.  \n\nKey observations:\n- Saves `X19` and frame pointer/link register to stack (standard prologue).\n- Loads an object pointer (`X0`) and dereferences it to get a vtable or struct pointer at offset `+0x20`.\n- If that pointer is non-null (`CBZ X0, ...`), it proceeds:\n  - Loads a second pointer from offset `+0x28` (possibly a destructor or cleanup function pointer).\n  - Clears the object at `X0` (writing 0 to its first byte: `STRB WZR, [X0]`).\n  - If that second pointer (`X1`) is non-null, calls `sub_140001040` with `W2=1`, likely triggering destruction or finalization.\n- After potential cleanup, it checks if `X19 == -1` (via `CMN X19, #1` = `SUBS X19, #1` with inversion for comparison) and branches to epilogue if true; otherwise falls through to a branch-back-to-epilogue.\n\nThe function always restores `X29`, `X30`, `X19`, and returns. Given the clearing of the object (`STRB WZR, [X0]`) and conditional cleanup call, this strongly resembles an inlined destructor that:\n1. Zero-initializes the object,\n2. Calls an external cleanup routine if a callback pointer is set.\n\nSimplified behavior:\n```\nif (obj_ptr = *(obj + 0x20)) {\n    *(byte*)obj_ptr = 0;  // zero object\n    if (cleanup_fn = *(obj + 0x28))\n        sub_140001040(obj_ptr, ..., 1);\n}\nif (X19 == -1) {\n    // epilogue\n}\nret;\n```"}
{"sub_14000F56C": "This function performs a **bounded string search/matching routine** on input data pointed to by `X0`, likely part of a string processing or pattern-matching utility. Here's a concise breakdown:\n\n- It begins by checking a flag at offset `+9` of the input structure (`X0`): if bit 0 is clear, it immediately returns `0` (XZR).\n- Otherwise, it accesses internal pointers/counts (e.g., `[X0+#30]`, `[X0+#38]`) — suggesting a structure with a data buffer (`X22`), and min/max range bounds (`X24`, `X23`), and an index/length field at `+20`.\n- It loads a *search byte* (`W21`) from a computed location relative to `X0+#2C`, scaled by an 8-bit field at `+0x18` — likely an offset into a small inline buffer or lookup table.\n- Then it attempts to find this byte within a span of the buffer (between indices `X25` and `X24`), using either:\n  - A manual loop comparing bytes (`memcmp`-like logic for small sizes < 0x10 or < 0xF), or\n  - A call to `sub_140013E4C` (likely a optimized string search like `strchrnul` or similar).\n- If the byte is found, it advances the current index, possibly updates a pointer/length at `+0x10`/`+0x20`, and branches back.\n- If the search fails or exceeds bounds, it falls into error paths: some trigger `BRK #1` (debug break) with a `<redacted>` message, others just update state and return.\n\n**In short**:  \nThe function is a **string tokenizer or character scanner** operating on a custom buffer structure, scanning for a specific byte (possibly a delimiter or token terminator) within a bounded region, updating internal state (cursor/position), and handling edge cases with error breakpoints or early returns.\n\nNotably, it modifies the input structure (`X0`) in-place (e.g., setting flag `+9`, updating `+0x10`, `+0x20`)."}
{"sub_140007E20": "This function encodes a 32-bit integer (`W1`) into a variable-length UTF-8-like byte sequence stored in a local buffer (`[X29,#0x18+var_4]`) and writes the encoded bytes into a buffer managed by a structure pointed to by `X0`. The number of bytes (`X19`: 1–4) depends on the input value range:\n\n- **1 byte** if `W1 < 0x80`\n- **2 bytes** if `0x80 ≤ W1 < 0x800`\n- **3 bytes** if `0x800 ≤ W1 < 0x10000`\n- **4 bytes** if `W1 ≥ 0x10000`\n\nIt prepends appropriate UTF-8 lead bytes (`0x80`, `0xC0`, `0xE0`, `0xF0` masks/or combinations) and extracts 6-bit chunks via bit masking (`BFXIL`) and shifts.\n\nThen, it checks if the encoded length (`X19`) fits in the available space in a buffer (computed from a struct at `[X0]`, likely an output stream/queue). If space is insufficient, it calls `sub_140016758` (likely a flush/resize function), then retries.\n\nFinally, it uses `memcpy` to write the encoded bytes into the output buffer and updates the buffer pointer. Returns 0 (`WZR`) on success.\n\n**Summary**: UTF-8-like encoding of a 32-bit scalar into a growable buffer, handling overflow via a helper flush/resize function."}
{"sub_14000BCA8": "This function parses a string (likely a Unicode domain name or identifier) with support for Punycode encoding, implementing part of IDNA (Internationalizing Domain Names in Applications) logic.\n\n- **Entry**: Takes a pointer to a struct (in `X0`) containing string data (likely offsets/pointers to input buffer, current position, and possibly a decoded buffer or output).\n- **Core Logic**:\n  - Loads a pointer from offset `+8` of the input struct and skips if null (early exit to `loc_14000BF14`).\n  - Scans the input string looking for markers: `'E'` (likely `\"Encodings\"` or Punycode prefix `\"xn--\"` case?), `'s'`/`'_'` for punycode suffixes.\n  - Implements base-32 decoding (Punycode-style) using a loop with digit validation (`'0'-'9'`, `'a'-'z'`, `'A'-'Z'`) and accumulates a value (`X11`) via base-36-like decoding.\n  - Handles recursion-depth limiting and error states (`loc_14000BE8C` etc.), setting error flags (`W20`) and calling error-reporting functions (`sub_140012B6C` with strings like `\"{invalid syntax}\"`, `\"{recursion limit reached}\"`).\n  - Upon successful parse or error, it calls `sub_14000D1A4` (likely a helper to produce an error string or normalize), then `sub_14000E044` (possibly finalizing or validating the result).\n- **Exit**:\n  - Cleans up the stack frame.\n  - Returns `X20` (0 for success, 1 for error, based on error flags set in branches like `loc_14000BEA8`, `loc_14000BF18`).\n\n**In short**: A Punycode decoder/parser for domain name labels, with error handling and recursion limits."}
{"sub_140005CC0": "This function performs a **size computation for a data structure**, likely related to **packing or aligning 16-byte-aligned entries**, with branching based on a count (`X2`) and content analysis of an input array (`X1`). Here's what it does:\n\n- If `X2 == 0`, it returns 0 (early exit at `0x140005d1c` → `0x140005d20`).\n- Otherwise:\n  - It calculates how many 16-byte entries fit into a value derived from `X2` (via `LSL#4`, subtraction, rounding up) → stored in `X9`.\n  - It then scans `X1` (a pointer to an array) in 16-byte chunks, counting non-zero 32-bit values (`W11`) to compute `X0`.\n  - It computes the difference `X2 - X0` and takes conditional paths:\n    - If `X2 <= X0`, jumps to error/debug handlers (`loc_140005EB8`, etc., which invoke debug logging/`BRK#1`).\n    - Otherwise continues to more complex logic (possibly validating or modifying metadata structures pointed to by `X1`).\n  - At `0x140005d4c`, if certain conditions hold (e.g., pointer alignment, non-zero sub-structure), it calls `sub_140005768`, then inspects low bits of a returned pointer (`X1`) to determine how to extract a byte (`LDRB`, `LDURB`, or `UBFX`).\n  - If that byte is `'#'`, and further conditions on alignment hold, it reads fields from a metadata structure, calls a function pointer at `[X25]`, then possibly `sub_140001040` (likely `malloc`/`realloc`-like) to allocate/adjust buffer size.\n\n**Core behavior**:  \n> Computes and possibly validates the size of a packed buffer needed to hold a sequence of 16-byte-aligned entries (from `X1`), while applying special handling for entries that start with `#` and contain nested structures. Returns a computed size in `X0`.\n\nIn short: **size computation / allocation sizing function for a custom packed data format**, with debug guards and specialized handling for `#`-prefixed entries."}
{"sub_14000EFD0": "This function parses an integer literal (possibly signed) from a string (`X1`) into a structure pointed to by `X0`. It checks for syntax validity, handles invalid cases, and stores the parsed value or an error indicator in the output structure.\n\n### High-level flow:\n- **Prologue**: Saves callee-saved registers and sets up stack frame.\n- **Check for existing value** at `[X0+0x28]` (likely a pre-initialized field); if non-zero, goes to cleanup with `W21=0`.\n- Otherwise:\n  - Calls `sub_140012B6C` (likely a diagnostics/logging or error-reporting helper) with format string hinting at integer parsing (`\"...\"boolcharstr()i8i16i32i64i12...\"`) and `W1=1`.\n  - On error (`TBNZ W0, #0`), returns `W21=0` (success flag = 0).\n- **If no prior error**:\n  - If `X22` (second arg, likely pointer to input string or value) is null → error path, returns `W21=0`.\n  - Else:\n    - Loads first dword of `[X20]` (i.e., `[X0]`), subtracts `X22` (value to parse), checks signed carry → if `X22 > loaded_value`, jumps to range-error handler (`loc_14000F080`).\n    - Otherwise compares `X22` with `0x1A` (26):\n      - If `X22 < 26`, computes `'a' + X22`, formats it as a character, calls `sub_140012C2C` (possibly parses character literal), stores result.\n      - If `X22 >= 26`, stores `X22` on stack and calls `sub_140013BCC` (likely big-integer parser, e.g., for `i128`), stores result.\n- In all error cases, sets up error message (e.g., `\"{invalid syntax}\"`, `\"...recursion limit reached...\"`) and stores `W21=0` in result structure.\n- Epilogue restores registers and returns `W21` (0 or 1) as result.\n\n### Observations:\n- `X0` points to an output struct; offsets like `#8`, `#0x10`, `#0x28` suggest fields for value, type, and possibly a flag or recursion depth.\n- Calls `sub_140012B6C` for diagnostics/reporting; result determines whether error flow is taken.\n- Supports at least `i8..i64` and possibly `i128` (based on strings).\n- Range checks against stack variable and against 26 (likely deciding between char literal vs. multi-digit hex/integer parsing).\n\n**Summary**: A structured integer parsing function that validates and converts input strings or values into typed integer representations, with error handling and diagnostics."}
{"sub_140008144": "This function appears to be a **custom memory copy routine with validation and conditional branching**, likely part of a heap or buffer management system (e.g., `memcpy`-like but with alignment/size checks and potential callback invocation).\n\n### High-level behavior:\n1. **Prologue**: Saves callee-saved registers and sets up a stack frame.\n2. **Entry branch (`TBNZ W2, #0`)**:\n   - If bit 0 of `W2` (second argument, likely a flag/alignment request) is set, it jumps to the main logic (`loc_140008180`).\n   - Else, it falls through to call `sub_1400124BC` (likely a fallback path, possibly error handling or different copy mode).\n3. **Main logic (`loc_140008180`)**:\n   - Reads a struct from `[X0]`, extracts two fields (likely size bounds or pointers), computes min/max and difference.\n   - Compares `diff` with half of `W2` (`X23 = W2 >> 1`), selects smaller value into `X19`.\n   - Calls `memcpy(X0, X3, X19)` — copies up to `X19` bytes from source (`X3`) to destination (`X0`).\n   - Stores computed value into `[X20,#0x10]` (likely updating some state).\n   - If `diff >= X23`, it jumps to `loc_140008240` (returns 0); else, continues.\n4. **Post-copy validation (`loc_1400081c8` onward)**:\n   - Checks if `X8 = *(X21+8)` is non-zero.\n   - Validates alignment/flag bits via `AND X10, X8, #3`, `SUB`, `CMP`, and `B.CC`.\n   - If `X10 == 0`, it jumps to cleanup/failure (`loc_14000822c`).\n   - Otherwise:\n     - Loads and dereferences two offsets from `X8` (likely a function table or metadata).\n     - If a function pointer at `[X22]` is present, it calls it with `X20` (source?) as arg: `BLR X9`.\n     - If another function pointer at `[X22+8]` is present, it calls `sub_140001040` (likely a destructor or post-processing hook).\n     - Then calls `sub_140001040` again with `(X19, 0x18, 8)` — possibly cleanup or finalization.\n5. **Cleanup (`loc_14000822c`)**:\n   - Stores `unk_1400187A8` into `[X9,#8]` (possibly a sentinel/error marker).\n6. **Return path (`loc_140008240` or `loc_140008244`)**:\n   - Restores stack and registers; returns `W0`:\n     - `0` if branch taken from `loc_140008240`.\n     - Otherwise (likely non-zero or error status) from `loc_14000822c`.\n\n### Likely purpose:\n- Implements a **safe or specialized copy**, possibly in a reference-counted or metadata-managed buffer.\n- Conditionally invokes callbacks (via `BLR`) based on flags in the buffer metadata (`X8 & 3`).\n- Uses `sub_140001040` as a general-purpose destructor/allocator (signature suggests `realloc`/`free`-like behavior given args: `size=0x18`, `fill=8`).\n\n### ABI note:\n- Uses standard AAPCS64:\n  - `X0`: destination\n  - `X1`: source\n  - `X2`: size/flag\n  - `X3`: scratch or extra param (here aliased to `X2`)\n\nBut since `X3` is set to `X2` early, the function takes **3 arguments**: `(void* dest, void* src, size_t size_or_flag)` — with flag bit 0 determining path."}
{"sub_140007330": "This function performs **safe unwinding or cleanup of an exception or stack frame**, likely in a C++ runtime or similar environment supporting exceptions or RAII.\n\n### High-level behavior:\n1. **Initial setup**: Saves callee-saved registers (`X19–X21`, `X29`, `X30`) on the stack and allocates local stack space.\n2. Calls `sub_1400124BC` (likely a runtime helper, e.g., `__Unwind_RaiseException`, `__cxa_begin_catch`, or a personality function helper) with arguments derived from inputs (`X0`–`X2`) and a global offset.\n3. Checks the return value (`W0`). If zero, skips to `loc_1400073E8`; otherwise:\n   - If `X8` (return value) is non-zero, attempts to read data from the address at `X8`.\n4. Validates the value in `X8` by checking lower 2 bits (`AND X8, #3`) and comparing with values 0–2 — likely a tag or flags field (common in exception unwinding, e.g., `__cxa_exception` header fields).\n5. If validation passes:\n   - Loads `X21 = [X8+0xF]`, `X20 = [X8+7]` (likely loads exception object pointer or related metadata).\n   - If `[X21]` is non-zero, calls function pointer at `[X21]` (possibly a destructor or cleanup routine).\n   - Then if `[X21+8]` or `[X21+16]` are non-zero, calls `sub_140001040` (likely a memory deallocation or cleanup helper like `free` or `__cxa_free_exception`) with arguments derived from the exception data.\n   - Finally calls `sub_140001040` with `X8-1`, size `0x18`, and alignment `8`.\n6. Returns zero in `X0` on all exit paths.\n\n### Key observations:\n- `BRK #1` in one failure path indicates intentional crash/debug break — likely an assertion or error handler.\n- String `\"a formatting trait implementation retur...\"` suggests this may be part of a formatting library (e.g., `std::formatter`) but used in exception cleanup.\n- Memory layout (e.g., offsets `+7`, `+0xF`, `+8`, `+0x10`) matches common `__cxa_exception` or exception header structures.\n\n### Likely role:\nThis is **an exception handler or cleanup routine**, possibly `__cxa_end_catch` or a personality function helper that:\n- Cleans up an exception object,\n- Invokes destructors,\n- Deallocates the exception if needed.\n\nIt's defensive: validates pointers before dereferencing, and falls back gracefully on invalid input."}
{"sub_14000CFCC": "This function parses a null-terminated string (starting at `X8`, the first argument via `X9 = X1`) as a **number in base 10, 16, or possibly base 36** (digits `0-9`, letters `a-z`, underscore `_`), writing the resulting pointer to the end of the parsed portion and the count of characters parsed back into the struct pointed to by `X0`.\n\n### High-level behavior:\n- Inputs: `X0` points to a struct `{ ptr, len }`, `X1` points to a `struct { char* ptr; size_t len }` — likely `{ start, end }`.\n- Initial step: sets `start = X1->start`, `end = X1->end`, and `X9 = X1`. Then `X12 = max(start, end)`, `X11 = end`.\n- Loop (core block `0xCFF4` onward):\n  - Reads byte at current position (`[X8+X11]`), advances `X11`.\n  - Validates character:\n    - `'0'–'9'` → valid\n    - `'a'–'f'` (lowercase hex) → valid\n    - `'_'` → valid (e.g., for digit separator, common in Rust/C++20)\n  - If invalid → jumps to `0xD064` (see below).\n- After parsing digits:\n  - Checks `X3 = X11 - 1` against `start` and `end` (range bounds).\n  - If `X3 >= start` and `X2 > 0`, checks `*(start + X2 - 1)` is uppercase alphanumeric (`CMN W9, #0x40` checks `W9 + 0x40 >= 0`, i.e., `W9 >= -0x40`, which for ASCII means `>= '@'` → `A-Z` and `[\\`^_`@]`, but the preceding path ensures it's not a digit/`_`, so likely `A-Z` only).\n    - If not uppercase (or invalid), triggers debug break (redacted error string + `BRK`).\n- Exit paths:\n  - `0xD064`: On error or early exit → zero out the struct pointed to by `X0` (`strb wzr, [x0,#8]`, `str xzr, [x0]`) and return.\n  - `0xD07C`: Success: `*X0 = { X8 + X2, X10 }` (end pointer = start + parsed length, count = `X10` chars parsed), then return.\n\n### Purpose:\nThis is a **string-to-number prefix parser**, likely used to extract numeric identifiers (e.g., for symbol names, tokens, or base-N integers) with support for `'_'` as separator, followed by validation (possibly for identifier-like strings ending in uppercase letters). It handles partial parsing (returns pointers to end of parsed region), and on error writes zeros to output struct.\n\nGiven the debug behavior and redaction, it's likely part of a **debug/verification or symbol-parsing utility**, possibly in a compiler, debugger, or linker tool."}
{"sub_140008FF0": "This function performs a structured comparison of two data objects passed in `X0` and `X1`. Based on the control flow and memory accesses, the objects appear to be variable-length structures with:\n\n- A **1-byte type/length field** at offset `+0`\n- Possibly **pointers at offsets +8, +0x10, +0x18, +0x20**, used conditionally based on the initial byte.\n\n### High-level logic:\n1. Load and normalize the first byte (`W8`, `W9`) by subtracting 5 and clamping negative values to 0 via `CSEL HI`.\n2. Compare normalized values:\n   - If **unequal** → return 0 (not equal).\n   - If **equal**, proceed to compare deeper fields depending on the normalized value (`X10`), which represents `max(0, type-5)`.\n\n### Behavior by normalized type (`X10`):\n- **`X10 == 0`** (original byte == 5):\n  - Compare the full byte (`W8 == W9`) → if equal, compare values at `+8`, `+0x10`, `+0x18`, `+0x20` via multiple conditional `memcmp` calls.\n- **`X10 == 1`** (original byte == 6):\n  - Compare bytes at `+1` only.\n- **`X10 == 2`** (original byte == 7):\n  - Compare values at `+8`, `+0x10`, `+0x18`, `+0x20`.\n- **`X10 == 3`** (original byte == 8):\n  - Compare values at `+8`, `+0x10`.\n- **`X10 == 4`** (original byte == 9):\n  - Compare values at `+0x20`.\n  - If equal, fall through to compare `+0x18` and then `+8` (same as case 2?).\n- **Other values**:\n  - Return 1 (equal) early if `X10 != 0` and `X10 != 1`, but only if earlier comparisons passed — specifically, `CBNZ X10, loc_140009090` returns 1 if `X10 > 0`, unless it’s 1.\n\nWait — correction: `CBNZ X10, loc_140009090` → if `X10 != 0`, jump to `loc_140009090`, which does `MOV W0, #1; RET`. So:\n- For `X10 != 0`, return 1 (equal) *only if* earlier checks (e.g., normalized equality) passed.\n- For `X10 == 0`, proceed to detailed comparison.\n\n### Therefore, the function does:\n> **Return 1 (true/equal) if two structures are equal under a custom comparison scheme, 0 otherwise.**\n\nThe comparison scheme is:\n- Both objects’ first byte is normalized by subtracting 5 (clamped ≥0).\n- If normalized values differ → return 0.\n- If normalized value is **non-zero** → return 1 (simple equality).\n- If normalized value is **0** (i.e., original first byte = 5) → perform deep comparison:\n   - Compare bytes at `+1`.\n   - If equal, compare pointers at `+8`, `+0x10`, `+0x18`, `+0x20` via `memcmp` on pointed-to data or pointer equality, depending on path.\n\nBut wait — in the `X10 == 0` path, we see comparisons at `+0x20`, then `+0x18` data via `memcmp`, then `+0x10`, then `+8`. So this is for **type == 5** structures.\n\nHowever, other paths also compare deeper fields. Reconciling:\n\nActually, the most consistent interpretation is:\n\n- The first byte encodes a **type** (0–255), but is offset by 5.\n- `CSEL` ensures negative results become 0 → effectively: `norm = (type >= 5) ? type - 5 : 0`.\n- The function returns 1 if:\n  - `norm1 != norm2` → no, returns 0.\n  - `norm1 == norm2 == 0` (i.e., type == 5) → do deep comparison.\n  - `norm1 == norm2 != 0` → return 1 (no deep comparison needed).\n\nBut then why do cases like `X10 == 4`, `X10 == 3`, etc., have deeper comparisons? Because they’re *inside* the `X10 == 4` or `X10 == 3` branches — i.e., they’re conditional *after* the main `X10 == X11` branch.\n\nLet’s trace again more carefully:\n\nThe first `CMP X10, X11`:\n- If `X10 != X11` → branch to `loc_1400090E4` → return 0.\n- Else continue into detailed logic.\n\nThen:\n```asm\nCMP X10, #4\nB.EQ loc_1400090BC      // if normalized type == 4\nCBNZ X10, loc_140009090 // if normalized type != 0 and != 4 → return 1\nCMP W8, W9              // original bytes equal?\nB.NE loc_1400090E4      // if not → 0\nCMP W8, #2\nB.GT loc_140009098      // if >2, go to type-2/3/4 handling\nCBZ W8, loc_1400090BC   // if original byte == 0 → go to 8/10 compare\nCMP W8, #1\nB.NE loc_1400090A8      // if !=1 → compare +1 bytes\n// W8 == 1 → compare +0x20 pointers\n...\n```\n\nSo it's not a simple normalized-type dispatch. The logic is:\n\n1. Normalize both first bytes by subtracting 5 (clamped to ≥0). Call these `n0`, `n1`.\n2. If `n0 != n1` → return 0.\n3. If `n0 == n1`:\n   - If `n0 == 4` → compare `+0x10`, `+8`.\n   - Else if `n0 != 0` → return 1 (because `CBNZ X10` jumps to `MOV W0, #1`).\n   - Else (`n0 == 0`, i.e., original byte == 5):\n     - Then compare original bytes (`W8 == W9`), and based on that (and `W8 <= 2`), decide what to compare:\n       - `W8 == 0` → compare `+0x10`, `+8`.\n       - `W8 == 1` → compare `+0x20`.\n       - `W8 == 2` → compare `+0x20` and fall into `+0x18` and `+8`.\n       - `W8 == 3` → same as `W8 == 2`? Actually `B.EQ 0x90BC` does `+0x10`, `+8`.\n       - `W8 == 4` → compare `+0x20`, then fall through to `+0x18` and `+8`.\n\nWait — in `0x9024`, if `X10 == 4`, go to `0x90BC` (which compares `+0x10`, `+8`). But `X10 == 4` means normalized value is 4 → original byte = 9.\n\nThen for normalized 0 (original byte = 5), `CBNZ X10` is false, so continue.\n\nThen `CMP W8, W9` ensures byte values match (since normalized is 0, they’re both 5, so always equal). Then `CMP W8, #2`, etc.\n\nBut actually, if `n0 == n1 == 0`, then `W8 == W9 == 5`, so `CMP W8, W9` is always EQ.\n\nSo the real logic is:\n\n- **All cases where normalized types differ → 0**\n- **Normalized types equal and non-zero → 1**\n- **Normalized types equal and zero (i.e., both original bytes == 5)**:\n   - Compare more deeply depending on the value of `W8` (which is 5), but wait — if both are 5, then `W8 == 5` always. So why all the comparisons?\n\nAh! Critical insight: The original bytes are *not* both 5 — `X10 = max(0, W8 - 5)` and `X11 = max(0, W9 - 5)`, and `X10 == X11`. So if `X10 == X11 == 0`, that means `W8 <= 5` and `W9 <= 5`, and `W8 == W9` (since `CSEL` made them equal in normalized space, and then we compare original bytes only if `W8 <= 5`). Wait — no, in block `0x9030`, it does `CMP W8, W9` after confirming `X10 == X11 == 0`. So `W8 == W9` could be any value from 0 to 5.\n\nBut earlier, `SUB X10, X8, #5` + `CSEL X10, X10, XZR, HI` → `X10 = (W8 > 5) ? W8 - 5 : 0`.\n\nSo if `X10 == 0`, then either `W8 <= 5` *or* `W8 == 5 + 0` but clamped.\n\nBut then `CMP W8, W9` in `0x9030` ensures the *actual* bytes are equal. So `X10 == X11` and `W8 == W9` → full equality of the first byte.\n\nThen based on `W8`, it chooses what to compare:\n- `W8 == 0`: compare `+0x10`, `+8`.\n- `W8 == 1`: compare `+0x20`.\n- `W8 == 2`: compare `+0x20`, `+0x18`, `+8` (via `memcmp` at `0x905c` and `0x9084`).\n- `W8 == 3`: same as 2? `B.EQ 0x90BC` does `+0x10`, `+8`.\n- `W8 == 4`: compare `+0x20`, then fall through to `+0x18`, `+8`.\n- `W8 == 5`: this is the main case — compare `+0x20`, `+0x18`, `+0x10`, `+8` (via the long path).\n\nGiven the complexity and repeated patterns, the most concise summary is:\n\n> **Performs a conditional deep comparison of two structures, returning 1 if equal, 0 otherwise. The comparison depth depends on the value of a first byte field (after clamping and normalization), with non-zero normalized values short-circuiting to equality, and zero (i.e., original byte ≤5) triggering field-specific comparisons (e.g., `memcmp` on nested data at offsets +8, +0x10, etc.).**\n\nBut since all the detailed work happens when the first byte is 5 (the only value where `W8 - 5 == 0` and `W8 > 0`), and `W8 == W9` is enforced, and the paths diverge for `W8 <= 5`, but `0x9030` compares `W8 == W9`, so only `W8 == W9 <= 5` proceed, and their behavior is type-specific.\n\nGiven the audience is an expert, and the function signature is likely `int __fastcall compare_structs(void *a, void *b)`, the answer is:\n\n> **A comparison function returning 1 if two structures are equal under a custom scheme, 0 otherwise. Structures with first byte > 5 are compared only by `max(0, byte - 5)`, while those with byte ≤ 5 undergo deep field comparison (pointers at +8, +0x10, +0x18, +0x20), using `memcmp` on pointed-to data or direct pointer equality, depending on the first byte value.**"}
